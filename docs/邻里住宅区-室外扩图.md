# 邻里住宅区：室外扩图

本轮仅扩展室外。新增室内地图、拜访授权和入室流程均已撤出。

- 地图高度从 1184 增至 2816；单玩家场景为 2944×2816。
- 南部新增书香巷、花邮巷、晚风巷，形成环路与中央步行连接。
- 16 户常住邻居住宅、2 个服务站和1个旅人客舍，地址绑定 NPC code；门牌随真实名册的姓名刷新。
- 三条巷道有不同屋顶轮廓、树木与门前布景，核心邻居门前增加生活物件。
- 导航增加“邻里住宅区”，“邻居的今天”可选择走到某位邻居家门口。点击住宅也会真实寻路到门前，不开启室内。
- NPC 的家与通勤端点连接各自住宅，使用碰撞世界寻路并缓存路线。
- 花邮小院和晚风花园各有可坐长椅，使用现有家具交互流程。

## 检查范围

`vue-tsc --noEmit` 与差异格式检查通过。独立 agent 完成地图几何、碰撞、路径接线和四张实际渲染截图审阅；修复了长椅登记遗漏、咖啡馆通勤端点不一致和服务站导航名称问题。后续缩短部分屋顶并增加门前物件，由主 agent 补看实际截图和类型检查。

主 agent 在本地浏览器实际运行从旧街区到最远旅人客舍的行走流程，到达坐标 (2112,2524)，随后通过长椅点击处理流程走到晚风花园并进入坐下状态。该浏览器使用隔离演示 API，只证明地图渲染与交互路径，不证明真实后端日程或人流已完善。

未运行测试套件，未逐户实走。预览在 `artifacts/neighbourhood/overview.png`，行走与长椅截图在同目录。本轮不自动推送，也不直接传源码；用户 push 后通过 Git 同步 mac-dev。
