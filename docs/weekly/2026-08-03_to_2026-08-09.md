# 每周章程：2026-08-03 至 2026-08-09

状态：进行中
时区：Asia/Shanghai
最后更新：2026-08-03

## 本周主题

稳定线上数据链路、改善 AI 与桌宠反馈，并补齐部署和项目维护记录。

## 本周目标

- 验证目标、周计划、今日任务和洞察之间的数据一致性。
- 完成生产部署后的核心流程回归。
- 降低用户等待 AI 回复时的不确定感。
- 明确数据库远程访问与生产基础设施安全边界。
- 持续记录完成项、未完成项和关键技术决定。

## 已完成

- 汇总完整 MVP 与桌宠功能并同步代码，提交 `2ff263b`。
- 修复洞察按 UTC 计算导致上海凌晨任务归入错误日期的问题，统一概览、趋势和月历的用户时区口径，提交 `af25fc4`。
- 线上回归确认本周有效行动为 2、计划兑现率为 100%、累计行动经验为 11。
- 为 AI 流式回复增加 20 条随机思考过渡语、轮换提示和呼吸点动画；首个有效回复到达后自动停止，提交 `2f8d97d`。
- 完成 AI 过渡状态的类型检查、31 项前端测试和生产构建验证。
- 完成当前系统技术栈与“每日状态检查”现状梳理。
- 明确数据库远程访问优先使用 SSH 隧道，不共享应用账号，也不向公网开放无限来源的 3306 端口。
- 建立 `docs/weekly/` 每周章程目录和自然周记录规范。

## 尚未完成或待确认

- 为协作者创建独立的服务器账号、SSH 公钥和最小权限数据库账号；目前只有操作方案，尚未在服务器执行。
- 将生产 MySQL 端口绑定收紧到 `127.0.0.1`，并复核腾讯云安全组规则。
- 为“每日状态检查”增加后端持久化，并决定它是否影响任务排序、AI 上下文和洞察指标。
- 为生产环境配置 HTTPS 与安全 Cookie。
- 接入真实通知供应商和附件恶意文件扫描服务。
- 补充数据库备份、恢复演练和保留周期。

## 风险与关注点

- 服务器或仓库密码曾在对话中以明文提供，应轮换相关凭据，并避免写入代码、Git 配置和文档。
- 数据库若通过 `0.0.0.0:3306` 暴露且安全组来源过宽，会产生高风险公网入口。
- 每日状态数据若未来进入 AI 或洞察，需要先定义用户知情、保存周期和删除规则。

## 本周后续

- [ ] 收紧生产数据库监听与云安全组配置。
- [ ] 创建独立协作者账号并验证 SSH 隧道连接。
- [ ] 设计每日状态检查的数据模型和产品行为。
- [ ] 配置 HTTPS 与安全 Cookie。
- [ ] 制定并验证数据库备份恢复流程。
