# 每周章程：2026-08-03 至 2026-08-09

状态：进行中
时区：Asia/Shanghai
最后更新：2026-08-03

## 本周主题

稳定线上数据链路、改善 AI 与桌宠反馈，并补齐部署和项目维护记录。

## 本周目标

- 验证目标、周计划、今日任务和洞察之间的数据一致性。
- 完成生产部署后的核心流程回归。
- 降低用户等待 AI 回复时的不确定感。
- 明确数据库远程访问与生产基础设施安全边界。
- 持续记录完成项、未完成项和关键技术决定。

## 已完成

- 汇总完整 MVP 与桌宠功能并同步代码，提交 `2ff263b`。
- 修复洞察按 UTC 计算导致上海凌晨任务归入错误日期的问题，统一概览、趋势和月历的用户时区口径，提交 `af25fc4`。
- 线上回归确认本周有效行动为 2、计划兑现率为 100%、累计行动经验为 11。
- 为 AI 流式回复增加 20 条随机思考过渡语、轮换提示和呼吸点动画；首个有效回复到达后自动停止，提交 `2f8d97d`。
- 完成 AI 过渡状态的类型检查、31 项前端测试和生产构建验证。
- 完成当前系统技术栈与“每日状态检查”现状梳理。
- 明确数据库远程访问优先使用 SSH 隧道，不共享应用账号，也不向公网开放无限来源的 3306 端口。
- 建立 `docs/weekly/` 每周章程目录和自然周记录规范。
- 新增好友/社交模块（2026-08-03 完成，尚未提交，待汇总）：
  - 通过注册邮箱发送好友申请，支持接受、拒绝、取消与删除；双向重复申请自动互为好友；非好友访问他人资料返回 404。
  - 后端：`V10__friend_relationship` 迁移（`pair_key` 双向唯一约束防重、PENDING/ACCEPTED 状态机）；`social/FriendService`、`social/FriendController`；好友详情聚合对方等级、五维成长雷达、周概览指标、连续行动、角色等级、宠物与今日任务（按对方时区），宠物与指标均走无副作用轻量查询。
  - 前端：`FriendsView`（添加好友表单、收到/发出申请、好友列表）与 `FriendDetailView`（今日完成清单、echarts 雷达图、宠物舞台、徽章墙）；徽章判定抽为共享 `insights/badges.ts` 供洞察页与好友详情复用；路由 `/friends`、`/friends/:publicId`；桌面侧栏与个人页新增入口，移动端保持五项底栏不变。
  - 测试：后端 `FriendFlowIT` 3 个场景、前端 `FriendsView`/`FriendDetailView` 单测、真实浏览器 `friends-flow` e2e（桌面与移动）均通过。
- 修复 AI 目标一键填入后创建目标返回 409 的体验问题：按错误码映射明确提示（活跃目标上限、目标时长等），不再显示笼统文案。
- 修复个人页宠物在宠物舞台偏左上：改为 `inset: 0; margin: auto` 双轴居中，桌面与移动视口实测偏移均为 0。
- 修复既有 e2e 回归：欢迎引导弹层遮挡按钮（`ai-assistant`、`emotional-feedback`）、伙伴页好感度与物种按钮文案改版（`partners-rive`）、auth 页入场动画导致的横向溢出误报（`responsive`，改为轮询等待动画结束）。
- 将目标页的目标清单与周计划列表改为堆叠轮播设计（2026-08-03 完成，尚未提交，待汇总）：卡片层叠展示，点击左右箭头切换并显示 `1 / N` 计数，首尾箭头禁用；桌面端露出前后相邻卡（偏移缩放半透明），移动端隐藏侧卡避免横向溢出；非当前卡不可点击，`load()` 后重置到第一张。新增单测与真实浏览器 `goal-stack` e2e（桌面与移动，无横向溢出、五项底栏保持）。
- 上线好友私信与群聊（2026-08-03 完成，尚未提交，待汇总）：
  - 单聊：`V11__friend_message` 迁移；按好友邮箱发送文字与表情消息（62 个 Twemoji 本地化资产，CC BY 4.0，消息存 unicode 文本）、分页历史、会话列表（最后消息+未读数）、已读标记；非好友 404。
  - 群聊：`V12__friend_group` 迁移（群/成员/群消息三表）；选择 1-9 位好友发起群聊（含自己最多 10 人，成员均为好友校验）；群列表（未读+人数）、群详情（成员等级+群主）、群消息（带发送者名）、按成员已读。
  - 全局新消息提示：`GET /friends/unread-summary`（单聊+群聊未读总数与最近来源）；`GlobalUnreadBar` 全局轮询（6 秒），未读时顶部悬浮提示，点击跳转对应会话，查看（已读）后消失，可手动关闭。
  - 消息布局按微信式设计：对方消息左侧（头像+名字+气泡+时间），自己消息右侧（row-reverse），群聊显示各发送者；修复 Firefox/Safari 的 flex 收缩导致每字一行问题（`.msg-main` 改 `flex: 1 1 0`），并加固 nginx 缓存策略（index.html 不缓存、hash 资源长缓存）解决旧版页面残留。
  - 测试：`FriendMessageIT` 3 场景、`FriendGroupIT` 2 场景、`chat-flow`/`group-chat` e2e（桌面+移动）。
- 将「每日状态检查」从纯前端提示升级为后端功能（2026-08-03 完成，尚未提交，待汇总）：
  - `V13__daily_status_check` 迁移（用户+日期唯一，每天一条可更新）；`POST/GET /daily-status` 保存精力与可用时间，服务端计算建议：精力偏低或 <20 分钟 → 缩小任务（SHRINK）；精力充足且 ≥45 分钟 → 保持原计划（KEEP）；其余 → 轻量推进（LIGHT）。
  - 今日页按建议重排任务（SHRINK 按时长升序）、推荐任务带「今天先做」徽章、展示「今日执行建议」（时长/难度调整提示）、按钮自动聚焦首个推荐任务；刷新后回显当天状态。
  - 状态注入 AI 助手系统提示（精力/时间/节奏建议），安排建议优先遵循；`insights/overview` 新增本周状态统计（填写天数+建议/精力分布，保存后失效 Redis 缓存），洞察页新增「本周状态检查」区块。
  - `ScheduleView` 补充 `estimatedMinutes`/`difficulty` 供今日页展示与排序。
  - 测试：`DailyStatusPolicyTest`（建议规则边界）、`DailyStatusIT` 3 场景、今日页单测 2 项、`daily-status` e2e（建任务→提交→重排→回显→洞察统计，桌面+移动）。
- 已部署至生产服务器 `42.193.103.213`（docker compose 重建 backend/nginx，迁移 V10→V13 依次应用，数据保留；e2e 通过公网链路冒烟）。
- 修复 AI「生成目标草案」偶发 503（2026-08-03 完成，尚未提交，待汇总）：
  - 第一轮：模型偶发返回变体维度码（如 `emotional`、中文名、大小写）或 markdown 代码块包裹的 JSON → 解析失败。修复：`QwenHttpProvider` 剥离 ```json 代码块后再校验；`GoalTemplateService.normalizeDimension` 归一化维度码（中文名/英文别名映射到系统五维度）。
  - 第二轮：模型返回的 JSON 结构合法但数值轻微违反 schema（复现命中 `estimatedMinutes: 2` 低于下限 5，`json_object` 模式不保证字段约束）→ 解析失败。修复：数值钳制（分钟 [5,60]、难度 [1,3]、天数 [14,84]）、文本超长自动截断、`max_tokens: 2000` 防长对话截断；仅真正的结构错误（空任务数组、无法归一的维度码）才返回 503 提示重试。
  - 测试：`GoalTemplateNormalizeTest`（维度归一）、`GoalTemplateClampTest`（钳制+归一）、`QwenContractTest` 通过；生产端到端（真实对话+目标草案生成）验证通过。
- README 与《成长平台实施计划》已翻译为中文；Gitee 已同步（`master` 分支，删除冗余 `main`/`feature/complete-mvp`）。

## 尚未完成或待确认

- 好友/消息/群聊/状态检查等本周改动尚未提交与远端同步，待汇总提交。
- 好友申请暂未接入站内或邮件通知，对方只能主动刷新好友页看到申请。
- 为协作者创建独立的服务器账号、SSH 公钥和最小权限数据库账号；目前只有操作方案，尚未在服务器执行。
- 将生产 MySQL 端口绑定收紧到 `127.0.0.1`，并复核腾讯云安全组规则。
- 为生产环境配置 HTTPS 与安全 Cookie。
- 接入真实通知供应商和附件恶意文件扫描服务。
- 补充数据库备份、恢复演练和保留周期。

## 风险与关注点

- 服务器或仓库密码曾在对话中以明文提供，应轮换相关凭据，并避免写入代码、Git 配置和文档。
- 数据库若通过 `0.0.0.0:3306` 暴露且安全组来源过宽，会产生高风险公网入口。
- 好友消息与群聊内容属于敏感数据，需要明确留存周期、导出与删除规则（与删除流程联动）。

## 本周后续

- [ ] 汇总并提交本周改动（好友/私信/群聊/状态检查/轮播/文档翻译，迁移 V10-V13），同步远端。
- [ ] 为好友申请接入站内或邮件通知，并明确好友可见数据范围与隐私说明。
- [ ] 收紧生产数据库监听与云安全组配置。
- [ ] 创建独立协作者账号并验证 SSH 隧道连接。
- [ ] 配置 HTTPS 与安全 Cookie。
- [ ] 制定并验证数据库备份恢复流程。
