> 历史归档（截至 2026-09-07）。文中的计划、完成状态、分工和约定仅代表当时记录；当前范围以[新版 MVP 计划](../../01-requirements.md)为准。

# 更好的自己：系统技术架构图

> 本图依据当前代码库和 Docker Compose 部署配置整理，表示已经落地的系统边界与主要数据流。

```mermaid
flowchart TB
    USER["用户"]

    subgraph CLIENT["客户端"]
        WEB["Web 单页应用<br/>Vue 3 + TypeScript + Vite<br/>Pinia + Vue Router"]
        DESKTOP["Electron 桌面端<br/>桌宠窗口与桌宠设置"]
    end

    subgraph EDGE["边缘接入层"]
        NGINX["Nginx 1.27<br/>静态资源托管<br/>/api/v1 反向代理<br/>SSE 长连接转发"]
    end

    subgraph APP["应用服务层：Spring Boot 3.5 模块化单体（Java 21）"]
        API["REST API / SSE<br/>统一响应、参数校验、请求追踪"]

        subgraph SECURITY["认证与安全"]
            AUTH["Auth<br/>JWT Cookie 会话<br/>CSRF 双提交校验<br/>管理员 MFA / RBAC"]
            PRIVACY["Identity / Privacy<br/>个人资料、隐私、导出与注销"]
            SAFETY["Safety / Admin<br/>风险识别、危机响应、内容治理、审计"]
        end

        subgraph DOMAIN["成长业务模块"]
            GOAL["Goal / Planning / Task<br/>目标、周期任务、计划与任务模板"]
            EXEC["Execution / Daily<br/>任务状态机、每日状态、经验结算"]
            SOCIAL["Partner / Social<br/>桌宠、好友、群组与消息"]
            PROGRESS["Career / Achievement / Insight<br/>角色成长、成就称号、洞察与周复盘"]
            ONBOARD["Onboarding<br/>新用户初始化与奖励"]
        end

        AI["AI 应用层<br/>GoalTemplate / Suggestion / AI Service<br/>QwenProvider：Mock 或 HTTP Provider"]
        JOBS["后台作业与可靠性<br/>定时任务、Outbox、幂等、过期处理"]
        STORAGE["基础设施适配层<br/>ObjectStorage、ID 生成、时间与配置"]
    end

    subgraph DATA["数据与基础设施：Docker Compose"]
        MYSQL[("MySQL 8.4<br/>权威业务数据<br/>Flyway 数据库迁移")]
        REDIS[("Redis 7.4<br/>可丢弃缓存与加速")]
        MINIO[("MinIO<br/>S3 兼容对象存储<br/>附件与导出文件")]
    end

    QWEN["Qwen / SiliconFlow<br/>OpenAI 兼容模型 API"]

    USER --> WEB
    USER --> DESKTOP
    WEB --> NGINX
    DESKTOP --> NGINX
    NGINX --> API

    API --> AUTH
    API --> PRIVACY
    API --> SAFETY
    API --> GOAL
    API --> EXEC
    API --> SOCIAL
    API --> PROGRESS
    API --> ONBOARD
    API --> AI

    AUTH --> MYSQL
    PRIVACY --> MYSQL
    SAFETY --> MYSQL
    GOAL --> MYSQL
    EXEC --> MYSQL
    SOCIAL --> MYSQL
    PROGRESS --> MYSQL
    ONBOARD --> MYSQL

    EXEC -.->|缓存失败不阻塞主流程| REDIS
    PROGRESS -.->|指标读取加速| REDIS
    AI -.->|建议与目标草案持久化| MYSQL
    AI --> QWEN
    PRIVACY --> STORAGE
    STORAGE --> MINIO
    JOBS --> MYSQL
    JOBS -.-> REDIS
    JOBS --> STORAGE

    classDef client fill:#edf6f6,stroke:#3e7775,color:#173c3b
    classDef edge fill:#f6efe7,stroke:#a66a4b,color:#4b3024
    classDef app fill:#eef2f7,stroke:#5b708d,color:#24364d
    classDef security fill:#fff2e9,stroke:#c4784d,color:#4b2e20
    classDef domain fill:#f1f6ed,stroke:#708d58,color:#2f4025
    classDef infra fill:#f5f1e9,stroke:#8e806b,color:#413a31
    classDef external fill:#f4eef8,stroke:#84639d,color:#3d294c

    class WEB,DESKTOP client
    class NGINX edge
    class API,AI,JOBS,STORAGE app
    class AUTH,PRIVACY,SAFETY security
    class GOAL,EXEC,SOCIAL,PROGRESS,ONBOARD domain
    class MYSQL,REDIS,MINIO infra
    class QWEN external
```

## 主要链路

1. 用户通过 Web 端或 Electron 桌面端访问应用，统一进入 Nginx。
2. Nginx 托管前端静态资源，并将 `/api/v1` 请求和 AI SSE 流转发到后端。
3. Spring Boot 以模块化单体方式承载认证、成长业务、社交、AI、隐私和治理能力。
4. 所有核心业务数据写入 MySQL；Redis 只承担缓存和指标读取加速，故障时不改变主数据可靠性。
5. 附件和导出文件通过对象存储抽象层访问 MinIO，后续可替换为其他 S3 兼容服务。
6. AI 通过 `QwenProvider` 隔离模型供应商，当前支持 Mock Provider 和 Qwen HTTP Provider。
7. 定时任务负责指标重建、保留策略、过期处理和 Outbox 投递等后台工作。

## 部署映射

| Compose 服务 | 作用 | 对外端口 |
| --- | --- | --- |
| `nginx` | 前端静态资源、API 与 SSE 反向代理 | `80` |
| `backend` | Spring Boot 应用服务 | `8080`（Compose 内部及可选直连） |
| `mysql` | 关系型主数据库 | `3306` |
| `redis` | 缓存与加速 | `6379` |
| `minio` | S3 兼容对象存储 | `9000` / `9001` |

## 架构边界说明

- 当前后端是模块化单体，不是微服务集群；各业务模块在同一个 Spring Boot 进程内运行。
- MySQL 是系统的事实来源，Redis 中的数据可以重建或失效。
- AI 服务是可替换的外部依赖；自动化测试默认使用 Mock Provider，生产或本地联调可切换到真实模型 API。
- 管理员页面与普通用户端共享前端工程，但通过路由守卫、后端权限校验和 MFA 形成独立访问边界。
