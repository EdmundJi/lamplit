# 04 · 定过的事

这一页记的是已经讨论清楚、不需要再争的决定，每条附一句为什么。目的是让后来的人（包括几周后的我们自己）不必重新推一遍，也不至于无意中推翻。

改变主意是可以的，但请改这里的文字，而不是在代码里悄悄走另一条路。

## 范围与形态

**共享世界不做。** 日活只有一两个人时，共享省下的 token 是零，换来的是产品性质改变加上跨用户注入面。等真有几十个人愿意天天来再说。

**这一轮为"自己天天开"服务，不为 demo。** 优先长时间陪伴不烦人，仪式感和转场排最后——它们对"明天还想不想打开"几乎没贡献。

**不做任何 app 壳。** 使用场景是学习时开在电脑旁边，要的是能常驻，不是能安装。app 化是分发问题，现在的问题是体验问题。

**成本不是这一轮的约束。** 模型负责居民的思考、计划、反应、记忆理解与对话，包括想去哪、做什么；逐步移动、读秒和合法性检查由规则执行。调用次数本身不能证明生命感。

**所有模型固定为 `qwen3.7-flash-2026-07-15`，默认不回退到别的模型。**（2026-09-14 更新：项目往"做实验、可能写论文"的方向走，模型是必须控住的变量。`app.ai` 与两份 compose 的默认值都改成这个带日期的快照；小镇三条路由默认只走 `qwen`——此前一次 Qwen 解析失败让 4 轮对话悄悄落到了 DeepSeek，一段对话里混了两个模型。调用失败现在就是失败，照常退避。DeepSeek 仍可显式配置路由，但实验跑不许用。实验产物统一归档到 `experiments/`。）以下为旧记录：**Qwen3.7-Flash 是小镇主力，DeepSeek 只作备用。**（2026-09-11 更新：主力模型从 `qwen3.8-flash` 换成 `qwen3.7-flash`。同时 `QWEN_*` 这组变量现在名副其实了——`app.ai.*` 就是 Qwen 那一组，base-url 默认指向 dashscope；反过来 `QWEN3_*` 降级成兼容旧本地环境的退路，它的默认值还停在 `qwen3.8-flash`。两个名字都还在，读不到 `QWEN_MODEL` 才退到 `QWEN3_MODEL`。） 两家使用独立完整配置组，provider、base URL、API key、model、timeout、stream timeout 与 JSON mode 不混用。小镇的 decision、turn、summary 默认走 Qwen 非思考模式；实际响应记录供应商、实际模型与 reasoning 元数据，密钥不进日志。

**先把现有四个人活透，新增居民由我们手动设计。** 人数以后可以增加，但招聘或邀请不会自动生成人物；新居民必须带着独立设定和拒绝权进入。

## 界面

**`/town` 收进 `UserLayout`，默认在内容区、一键全屏。** 割裂的根源不是配色，是 `/town` 曾被从 `UserLayout` 的子路由挪成顶层路由，于是深色模式、强调色、转场、侧边栏、跳转链接全部失效。

**两种模式都留着。** 简洁模式是左清单右小镇的工作台，隐藏另外六个模块；完整模式保留现有导航，小镇是其中一页。那六个模块一个都不删，慢慢往小镇里长。

**共同 token，小镇保留自己更暗更暖的性格。**

## 小镇与用户

**小镇是清单的反馈层。** 勾完一项什么都不发生，久了就不想勾了。

**你勾完，小人才跟着完成。** 你是因它是果，它永远不代你勾，也永远不反过来催你。

**任务标题可以进居民的决策，不能进居民的嘴和记忆。** 代码在动作、对话、记忆落库之前扫描并拒绝含标题原文的输出，不靠提示词。每项任务可单独加锁，默认可见。设置里写明标题会发给模型服务商。

**居民之间、居民对用户的小人，都只做私有好感 + 说没说出口。** 不建恋爱状态机、不写告白剧情、不做结局；三角恋是三个私有值凑出来的结构，自己会涌现。

**秘密只能靠看出来。** 没有任何面板告诉你谁喜欢谁。猜到了才有意思，被告知就没味了。

## 模拟

**地点两层：地点 + 具体位置。** 位置有主人、有容量、有状态，会被占用。四层的表达不做——对一条四个人的小街，两层够用。

**后端定结构，前端定像素，靠 id 对齐。** 美术可以随便重摆而不动规则。

**面积不由我们拍板。** 等居民真的挤不下、真的有人开口要一个地方，建筑师才来改，扩张带着世界内部的理由。

**物品有真实状态，链条允许断。** 断了才有意外，意外才是故事的来源。断掉的链条只写进日记，不在画面上喊。

**离线只补算最近一小段。** 更早的时间把存量和需求重置到合理值，不编造离线剧情，也不让人回来面对一地鸡毛。

**LLM 是每名 NPC 的大脑。** 工作、休息、睡眠、社交、继续或打断安排、开关店等认知选择交给该居民的模型。人物设定、信念与私有经历参与判断，不只决定语气；规则不按 `energy`、`social`、`duty` 阈值替人行动。

**内部数值不直接进入模型。** 生理、心理数值仅用于模拟与调试。正常状态省略，只有显著异常体感才用“很累”“难以集中”等定性描述进入感知，不附数值、阈值或指定动作。真实时间、地点和可感知的世界事件等外部事实可以保留。

**规则执行世界，不替居民思考。** 规则负责时间积分、移动、设备、容量、权限与动作结果。模型失败时只继续合法且可执行的已有安排，或中性等待；不编造新的承诺、感悟、社交意图或生活决定。

**差异作用在"记什么"上。** 同一件事各写各的版本，因为记忆会复利：几天之后他们眼里的世界真的不同了，那时模型推出不同结论不是因为在演，而是因为拿到的事实不同。

**记忆写入分两阶段。** 规则先廉价全量记原始日志，模型只用在提炼看法那步。不是为了省钱，是出了问题能回溯到客观事件，而不是只剩几份各执一词的转述。旧记忆和好感不删除，只标记为已被取代。

**隔离落在存储层。** 每条记忆带主人，检索接口不接受不带主人的查询。记忆进模型时作为数据传，不拼进指令区。

**给真实处境，不用责任感阈值派工。** 模型可以知道谁在等、等了多久、自己曾答应什么，结合生活信念决定是否照应。内部责任感数值不直接注入模型，正常心理状态不提示；显著异常体感才作定性表达。

**9:00–21:00 是可调的初始营业惯例。** 时间不是自动强关指令。模型决定 `open`、`close` 与通知顾客；真实 `close` 动作执行后，规则才收束营业权限与服务。顾客可以选择保留进度、回自家 `desk` 续工，不由时钟直接安排回家。

**23:00–07:00 是所有 NPC 可调的初始睡眠习惯。** 没有默认整夜清醒的店主或画师。进入时间窗只产生 routine 事实，不制造疲惫，也不自动安排睡眠；模型决定继续、收尾或睡觉。短休和饮料只能小幅缓解精力，连续睡眠按世界经过时间恢复。

**休息和点单是两个动作。** `rest` 不隐含饮品请求，只有 `request_drink` 才进入服务链。久等可以成为当事人的定性感知，但规则不代替他离开、抱怨、形成感悟或要求续杯。

**有主的座位可以借，有主的设备不能。** 没位置时退而占用别人的椅子是合理的，占用别人的咖啡机不是。`claim()` 的退路要区分这两种"有主"。

**店主的价值来自他偶尔不在。** 永远在吧台的店主是一台有脸的自动贩卖机。他出门、别人只能等或者去街上把他找回来，这段空缺才让服务像是人做的。

**服务选择归模型，服务约束与结果归规则。** 要不要服务、继续等还是离开、是否打断自己的事，由相关居民各自判断。排队事实、设备使用、制作耗时、容量与完成或失败结果由规则落实，画面如实表达。

**经历可以改变人，内部状态缓慢、有界地变化。** 状态每人一份、随存档保存，初值表只用一次。反思形成居民自己的看法；内部数值不回传给模型当仪表盘，也不直接触发行为。

**允许往坏了长。** 只能升不能降的数值是棘轮不是涌现。同一套机制要能长出一个越来越懒散的老板，也要能让学到的预期是错的。能产生我们没预料到的结果，才算涌现。

**保留社会经历，让居民自己解释。** 等待、抱怨、照应与落空都有真实事件来源，可能影响记忆、预期和关系。是否不满、开口、亲近或疏远由模型决定，不能将这些社会模式写成自动动作链。

**“需要工作养活自己”是生活信念，不是经济系统。** 它会进入居民的长期考虑，但不计算收入、食物和房租，不设每日工作指标，也不因为暂时没有工作自动涨惩罚。居民可以休息、重新解释什么算工作，或因生活经历改变方向。

**职业、劳动和经营权都可以改变。** 新职业复用已有的读写、制作、交谈与外出动作；缺少设备和场地时先形成需求，不能一句话宣布已经实现。经营者可以单方面停止自己的劳动，咖啡馆可以暂时无人服务。

**职责变化要经过双方明确同意。** 帮工、委托和正式接手分别授予对应的行为权限，经营权只在明确接手后转移。双方可以有不同理解，也可以拒绝、反悔和重新协商；模糊对话不能静默转移所有权。

**真实日夜保留，加速只用于开发观察。** 普通小镇按用户时区生活。内部精力按时间、昼夜与实际动作变化，睡眠恢复来自实际执行结果；何时工作、休息或睡觉交给模型。加速观察不强迫居民辞职、接手或组织活动。

**加速跑和上帝视角是前提，不是附加项。** 模式要重复五到十次才能和巧合区分开，真实节奏下长不出来；数值悄悄漂移，不给视图就永远看不见它发生。

**能站的地方都能去。** 具名位置只用于真正被占用的东西——自己的床、老板的咖啡机、备考生的窗边座，这些需要归属和容量。站着、聊天、路过不需要具名槽位；花园只有四个位置所以四个人挤成一团，那是这个约束的代价。参考 ai-town 的思路：碰撞图加寻路，人可以停在任意可站立的点上。分工不变——后端只说"某人在花园、没占具体位置"，像素由前端在可行走区域里挑，而且要稳定，同一个人不能每次刷新都换地方。

**当前地图是 1248×768，窗边六席与每户书桌都是真位置。** 原 `cafe-window-seat` 归小川，`cafe-window-2` 至 `cafe-window-6` 是五个独立公共单席；中央讨论桌保留。`study/read/work/make` 在咖啡馆会真实占一个座位，在家会占本人的 `home-<id>-desk`，画面和容量用同一 position id 对齐。

**环境声默认关闭且始终可关。** 咖啡馆人声使用本地 CC0 素材，只在镜头位于真实 `open` 的咖啡馆时轻声淡入；`closing/closed`、切换地点、页面隐藏或销毁场景都会淡出或释放，不从客户端时间猜营业状态。

## 这一轮新定的

**大多数人是温和的，差异在于什么被压住了。** 01 原来要求"每人一两个明确对立的锐角"，那是 AI Town 为了对抗模型分不出细微差别才用的做法，我们不走。改成本我（真正想要的）／超我（认为自己该是什么样）／自我（具体情境里怎么折中）三层，**文本，不是分数**，不作为闸门。它只影响两件事：写记忆时记住哪一版、对白里绕开什么。超我律令可以被反复的经历磨松——01 里那个"我为这家店牺牲太多"的懒散老板，就是一条律令松掉的样子。六个人同一套标准，不能四个是平的两个是立体的。

**规律由居民自己找，不由规则数出来。** 看到某人几次坐在窗边，是在反思里总结成"他喜欢窗边"，不是规则数到两次就置一个布尔值。人脑一直在给世界找规律，我们模拟的是这个功能，不是模拟某一条具体规律。据此删掉了 `proactivelyServe` / `anticipatesRefill` / `repeatVisitStreak` 那条链——它违反的是本页已有的两条：不能将社会模式写成自动动作链、规则不按 energy/social/duty 阈值替人行动。

**相遇是外部事实，"多久没说话"是内部仪表。** 前者可以进模型感知，后者不行——`lastSocialAt` 和 `dutyPressure` 是同一类东西。所以社交靠**礼仪**发生：规则只负责让两个人面对面（走进一个有人的地方、或在街上照面），第一句由模型写，不用罐头台词；之后回不回、继续不继续、要不要坐过去拼桌，全是各自模型的事。规则制造决策时机，不制造决策。配套加 `join` 动作，让"共享的桌子"这条 01 里一直没有落点的设计有地方发生。

**别用频次去模拟脑回路。** 频次是结果不是设定。要调的是"什么算值得重新想一次"：被打断、手头的事做完或卡住、环境变了、身体到了明显异常、时间锚点，再加一条低频的走神——纯事件驱动的世界里没有人会"突然"做什么，而突然是生命感的一部分。**每次决策记录它是被什么触发的**，这个分布本身就是指标：九成是"计划到期"就说明世界对他没有影响，他只是个计时器。

**记忆是文件，一人一目录，文件为准。** 世界存档不再存记忆。检索接口只按 owner 取，没有"给我这个世界所有记忆"这种调用——隔离落在存储层。SQLite 只存可查询的元数据，丢了能从文件重建；它不是 openclaw 那种模型伪造不了的 provenance，因为我们的写入全走 Java 代码，模型只提供经过校验的文本，别照那个说法理解它。注销必须连记忆目录和索引行一起删；导出必须带上记忆，否则存档里已经没有了，导出会悄悄少掉最私人的部分。

**依赖禁令这一轮只破一次例：** `org.xerial:sqlite-jdbc`，由编排者自己加，子 agent 仍然一律不许装依赖。

**用户的小人也进模型决策。** 它是第五个居民，不该是全场唯一一个没有一天的人。硬约束两条：上下文里一个用户字符都不能进（有断言守着），用户的明确安排随时打断它自己选的事。

**居民的记忆文件放在命名卷里。** 开发容器每两秒轮询源码热重启，放临时文件系统等于每次改代码全镇失忆。生产镜像同理要挂卷。

**并行 agent 的文件归属要提前划死，共享文件只能有一个主人。** 同一个工作目录下没有分支隔离，两个 agent 改同一个文件就是互相覆盖。`CompanionWorld` 这种谁都要加字段的文件，一波只能给一个人；其余人把想要的字段写进报告，下一波再接。

**"编译通过"不是验证。** 两个真实例子：`FileMemoryStore` 两个公开构造器编译干净但 Spring 起不来；日程分解的调用类型没在路由表里，编译干净但真跑起来活锁到零调用。改完后端必须打一个真实接口或跑一次真实调用。

## 又一轮定的（先做后想那一层）

**规则是下意识，模型是解释。** 人大多数时候先做，事后才给自己一个说法，而那个说法经常
不是真正的原因（Gazzaniga 的解释器、Libet 的准备电位、Nisbett & Wilson 的
*Telling More Than We Can Know*）。所以：规则替某人做了一件事就调 `recordDeed` 记下来，
`note` **只写旁观者看得见的东西，绝不写动机**——规则不知道任何人为什么做任何事；
攒够几条之后由模型给出这个人自己的说法，落成一条 `reflection`（是事后的建构，不是观察）。
两条纪律：被解释过的行为离开队列、只以那条记忆的形式活下来，没人来得及解释的直接丢掉
（人的自传体记忆就是这样，留下的是说法不是事件）；说法会覆盖 `r.thought`，
从此就是他关于自己知道的东西，也是下一次决策会读到的东西。**没有这一步，模型就只是
一个每次调用都要付钱的旁白器。**

**解释允许是错的，而且会被超我扭曲。** 提示词里明确给了这个许可：说法可以是搪塞、含糊、
自己都不太信。不给这句，模型会把每件事都解释得合情合理，那就假了。超我那一层在这里
才真正起作用——规矩是"不能让人看出自己在意"的人，反复擦同一张桌子之后，会说成
"桌子确实脏了"，而不是"我心里不舒服"。**他会读回这条记忆并且相信它。**

**人格文本要变成规则层的行为，不能只当提示词背景。** `actingSelf` 本来就是下意识动作的
说明书（"心里不舒服时去擦桌子""用东西代替话""被打断就闭嘴"）。它现在会由规则直接触发，
不问模型，也不推进任何世界状态——擦桌子不产生任何后果，这正是重点。触发必须确定性
（由 worldId + 居民 + 时间桶哈希推导，不用 `Math.random`），并且有小时级的间隔封顶。

**习惯还负责把人带到某个地方。** 除了不改位置的小动作，每个人再有一条"场所习惯"：
复习的人习惯去咖啡馆窗边并且点一杯、卡住的人习惯去咖啡馆看人。这是对"没有任何东西
让居民想喝一杯，而藏一个口渴值是禁止的"这个死结的正面回答——**不是让他渴，是他本来
就习惯去那儿**。注意别让所有人的场所习惯都指向同一个地方，也别让某个人的习惯把他送去
全镇唯一没有别人的地方（青叔就栽在这上面：一天 12 条记忆，别人 52 到 112）。

**规则决定什么时候问，模型决定答案。** 相遇不再由规则直接开对话：规则把两个人放到
面对面、排进 `pendingEncounters`，模型回答 greet / join / none。**"看见熟人而没有走过去"
是一个必须能被表达的答案**，这个小镇以前表达不了。但要小心提示词的倾斜——第一版给了
四个不说话的理由、零个说话的理由，结果一整个白天 15 次全部回答"不"，17 段对话变成 0 段。
掰平之后是 24/47。

**内部数值不进模型这条，字段名也算。** `ResidentState.relationships` 是一张原始数值表，
有两个测试按**名字**禁止它出现在任何模型契约里。想把"我和在场这个人什么关系"送进上下文，
必须换一个名字（现在叫 `peopleHere`），并且只送短语不送数字。守卫是对的，别改断言。

**每个模型输出都要有一张"提供了多少次 / 被选了多少次 / 被拒了多少次"的表。**
这个项目已经栽了六次同一种病：**一个动作被摆出来给模型选，但它不可能成功，而没有任何人
被告知原因。** `observe` 掉进 60 秒的 default 分支变成心跳；`invite` 提供 96 次选中 0 次；
`request_drink` 提供 32 次选中 0 次；`propose` 被选 592 次而 `place=home` 一律静默拒收；
打烊时 `continue` 被从菜单里拿掉却没有替代品，模型答了就被丢；`planDay` 在两个装饰器里
漏了转发。**每一次都编译干净、每一次都有测试、每一次都从没跑通过。** 这张表任何一次都能
在第一天抓住它。

**schema 的 enum 必须按这次调用的 `availableActions` 生成。** 静态全动作表和提示词里那句
"只能从 availableActions 选"长期互相矛盾，schema 允许的比实际合法的多，差额全部变成静默
拒收。

**装饰器漏转发不会编译失败。** `ResidentMind` 上每个能力的 default 方法都抛
`UnsupportedOperationException`，所以包装类少写一个方法照样编译通过，只是把能力静静关掉。
有一个反射测试守着这件事：新增能力时它会变红，那是它在正常工作，去补转发，不要改断言。
另外"缺能力"和"网络挂了"必须分开处理，否则一个没实现的能力会烧光共享的失败退避预算，
饿死全镇的普通决策。

**`docker compose restart` 不会重读 compose 配置。** 它只重启容器里的进程。给服务加了
环境变量或挂载之后 restart 一次，看起来成功了，跑的还是旧容器旧配置——后端曾经因此
起不来整整一天，而报错是"sqlite 打不开"，跟真正的原因隔了三层。一律用 `up -d`。

**用 3.8-Flash，不用更便宜的 3.7。** 同代码同窗口对照：3.7 的拒收率 86.6%，3.8 是 6.0%；
3.7 烧了 4.8 倍的 token，实际花费贵 4.3 倍——单价便宜 14% 没有意义。差的不是 JSON 解析
（两边都只错 1 次），是**卡在一个局部最优里出不来**：3.7 连选 592 次 `propose`，每次都被拒，
读不出"这条路走不通"。小模型典型的失败方式不是胡说八道，是死循环，而我们的循环会把它
放大成一个花钱的活锁。

**输入是输出的 33 倍，所以省钱的杠杆不在模型上。** 实测每次调用输入约 3200 token、输出约
90，全部落在阶梯计费的最底档（远低于 32K），所以那些"0.2–1.2"的浮动区间对我们不适用。
真正的杠杆是那段每次都重发的固定指令块能不能被缓存——而我们现在**看不到有没有命中缓存**，
`usage` 里没有 cachedTokens，这是个盲区。

## 两个特殊角色

**备考邻居知道真相，而且还在继续发现。** 他的怪话必须来自真实系统事件（重启、模型失败退避、部署），否则他只是个哲学系学生。他能告诉别人，后果交给转述链。他知道有一双眼睛在外面，但不知道那是谁、也不知道这是为了什么。

**他看得到一切，改不了任何东西。** 这条边界本来是为了防注入——他的输入里迟早有用户可控文本，给他执行权等于把注入的后果从"说错话"升级成"拿到机器"。落到人身上却正好长成了他的性格。

**不设替全镇作决定的主控 agent。** 每名 NPC 独立思考；规则维持时间与物理执行。某次模型失败只保留已有安排或中性等待，不由另一个规则大脑接管认知选择。

**建筑师产出受 schema 约束的地图数据，不产出代码。** 校验不过就是没盖成，而这在虚构里同样说得通：墙砌不进去、柜子挡了门、改完谁也进不去只好返工。第一版只重排已有位置。

## 验收

**盲测要真的盲。** 从时间线里抽二十句去掉名字，猜是谁说的。执行的人**必须没有参与过设计**：开一个全新的 agent，只给它这二十句和四个角色的一句话身份，禁止读仓库、读文档、读人格数值。编排者自己不能当裁判——他知道店主尽责 85、画师 25，猜中只证明他记得自己写了什么。换个模型不是关键变量，不知道答案才是。猜不出来就是没做到：说明四个人的差异还停在数值上，没长进他们说的话里。四选一的随机基线是 25%。

**盲测跑多轮，而且要在不同天数上各跑一轮。** agent 可以反复开，所以别只测一次。真正要检验的是我们押的那个假设——「差异作用在记什么上，记忆会复利，几天之后他们真的不同」。第 1 天、第 3 天、第 7 天各抽一批测：**正确率随天数上升，假设才成立；一直平着就是假设错了**，那时该回头改设计，而不是加长跑，更不是回去给模型塞"你是个内向的人请害羞地说话"——那是表演不是性格，会把整个原则推翻。

**每批固定跑几个独立 agent，结果全报，取平均。** 能反复开就会诱发挑数据，先把这条定死：不许只报最好的那次。



**盲测。** 从时间线里随机抽二十句去掉名字，人猜是谁说的。猜不出来就是没做到，无论测试多绿。模型也跑一遍同样的分类当日常回归，但算数的是人猜的那次。

**视觉模型只当候选筛选器。** 先用坐标和遮挡顺序做程序断言，再用画面比对定位变化区域，最后才问它，而且只算"疑似"。四种问法测完，最好 62%，书架 0/4 且换问法也纠正不了——**不用它给素材打标签**，理由见 05-notes。

**改后端之后必须打一个真实的后端接口。** `curl /town` 返回 200 证明不了任何事——那是 Vite 发的静态页，后端挂了它照样 200。

## 又一轮定的（两个人一起做同一件事）

**目标写成可量的：每天至少 3 次"两个人一起做同一件事"。** 没有量法就没法说做到没做到，所以先有度量（`metrics.md` 的「两个人一起做同一件事」一节），再动手。

**这个数分三档，故意不合并。** `sharedProject`（同一个项目落在两双手上，同时）是最没有歧义的读法；`satTogether`（有人主动走过去和谁坐一起）弱一档，挨着一个人不等于在和他做同一件事；`sameActivity`（同一个地方做同类事）基本是碰巧，因为所有人的地点习惯都指向咖啡馆，所以它被单独记出来是**为了从前两档给人的印象里减掉，不是加上去**。另有一档 `sharedBuilds`：同一天里被两个以上的人动过手的项目，不要求同时在场——他们仍然是一起做出了一件东西。

**对话不计入这个数。** 说话也是在一起，但小镇一天本来就产二三十段对话，把它折进来这个数字就没有意义了，等于用一个已经达标的东西掩盖一个从没达标的东西。

**反射只作用于眼前的东西，招牌习惯才带你去某个地方。** 会挪动人的习惯会**悄悄杀死**另一个以"人还不在那儿"为前提的习惯。写这一版时踩了两次（小川从此不再去咖啡馆窗边；青叔从此不再跑那趟送苗的差事），两次都无声无息。

**规则可以把一个人的手放到一件共同的事上。** 这是这一层唯一一次让规则推进共享世界状态，理由是：模型被提供了 216 次 `create` 一次都没选，我不打算赌第 217 次。它仍然只是反射——留 deed、事后由本人解释、解释可能是错的，而且都进了 `habitTraits`，本人可以反过来对这个习惯形成看法并把它调稀。

**信念只会削弱习惯，不会加强，无论它说什么。** 一半是因为读方向就得读文本；另一半是这本来就是更实话的读法——**一个你已经注意到、并且有了固定看法的反射，就已经不完全是反射了**，跟你赞不赞成它无关。

**"我对这件事做过什么"这类字段，粗但诚实。** `careerIntent.lastActedAt` 只记"这个人做了一件和自己方向同类的事"，规则**不判断**这件事是否真的服务了那个方向——那是在读意义，属于本人。

**留一个永远红的测试断言一个不存在的功能，不叫更诚实，只叫更吵。** 该拆成"它真正保证的那半"加"缺口写进 03"。

**闸门按机制本身定界，不按今天的数字定界。** 反思频率原来是全镇一个总数（<10），既藏得住"一个人想了九次而别人一次都没想"，又会因为跟频率无关的原因移动——镇上开始真的发生事情之后它就变成 11。现在按人算，界取"三小时硬间隔结构上允许的一半"。

## 社会怎么长出来（详见 [01](01-requirements.md) 的「社会怎么长出来」）

**契约按惯例演化建模，不按收益函数最大化建模。** 两个理由：卢梭那个区分（公意 ≠ 众意）说明把个人收益加起来得到的不是契约；而且"定义收益函数"本身就是最硬的一处写死。更根本的是这个小镇的居民**不是优化器**——整个架构是先做后解释，最大化收益的 agent 是它的反面。

**一条社会规范 = 被足够多居民各自独立持有的、同一条信念。** 信念就是带 `supersedesKey` 的记忆，我们从不写它的内容。这个定义同时给了验收的抓手。

**规则只记录承诺与兑现的事实，绝不判断那是不是背叛。** "他说了 T 会来做 X；T 到了，他来了/没来"是事实；是不是辜负，是本人和旁观者各自的解释，而且两边可以不一致——那正是好的地方。

**先不加货币。** 货币是多边交换的解法，六个人的镇子没有那个问题，加了它会立刻统治整个模拟。真正让契约成为必需品的是**稀缺**：先让已有的排他性（只有经营者能 `tend`；一个人只能在一个地方）咬起来。

**天眼那个居民拿到的是异常，不是数值。** 内部数值不进模型上下文这条红线不为他破例——数值进了上下文，模型就会像读表格一样推理。规则检测到异常之后，把它作为**他注意到的一件事**交给他，也就是光圈更大的 `salientPerceptions`。这样他仍然是个人，不是一块仪表盘。

**一个从来没人被问过的义务，不会被履行。** 2026-09-10 实测：`celebrate` 被提供 1658 次选中 0，`create` 342/0，`invite` 285/0；而同一个模型被**单独**问 `react` 时 53% 说好。所以任何新机制都必须连带回答"什么时候把这件事**作为一个问题**问出来"，否则它会变成第二个 celebrate。

**任何计数型验收判据都必须配一个变异度分量。** 否则总能用一个定时器满足它——那一轮就差点这样：每天 3 次达标了，但形状是同一个项目、同一分钟、同一批人。

**验收的总目标：同一份种子跑两次，两次都长出一条我们从没写过的规矩，而且至少有一个居民能自己说出来。** 最后半句最关键：量到了规范但没有任何居民说得出它，那它是我们的指标，不是他们的社会。

**如果只立一条指标，立互惠**（A 帮过 B 之后，B 后来帮 A 的概率高于帮别人）。它是契约的最小单位，而且**写不出来**——可以写"A 帮了 B"，写不出"B 后来自发地更愿意帮 A"。

**「我们没写过」由对照跑判定，不由我们自己列清单。** 同一个 `worldId`、同一个天数、把模型关掉跑一遍；规则单独长得出来的，按定义就是我们写的，从候选里减掉。理由是清单会烂，而且那是我们在审自己的案子。附带好处：一把在纯规则跑上会亮的尺子，报的不是发现。

**验收的尺子自己也要能红。** `NormDetector` 里 MET 这个状态，检测器无论如何都到不了——只有盲读的人在某个居民自己的话里读到那条规矩，才能把它推过去。量到的规范如果镇上没人说得出，那是我们的指标，不是他们的社会。

## 又一轮定的（承诺，和看见别人）

**居民看东西也看人。** 反思要求他从自己的记忆里看出"反复出现"，还举例"某个人总是坐在某个位置"，而世界从来没把任何人坐在哪儿写进过任何人的记忆。规范是关于别人的事实，没有人能从日记里形成一条规范。

**记的是事实，不记为什么。** 对方待在那儿的理由是他自己的，不跟着走。动作经一张固定短语表落地而不是拼接——用户打过的字因此不可能经由别人的记忆进到模型上下文里。

**人一走就把他忘掉。** 明天他又坐回那个位子，那才叫"又"。不这么做，"总是"就永远只会被记成一次。

**给目击划的那条上限，是给别的记忆的底线，不是给目击的天花板。** 一个什么都没发生的镇子记得的全是"谁站在哪儿"，那是诚实的。要保证的是日子接着过的时候，让位的是目击。

**看见谁在屋里不进反思的触发量。** 青叔坐在青叔一向坐的地方，恰恰是"没发生什么事"。它们照样可被检索——能看出他"总是"坐那儿正是写下它们的理由——只是不会自己把人推去想。

**承诺只记事实，绝不判断背叛。** 结构里没有 broken/betrayed/failed/trust，记忆句子里没有评价词。是不是辜负，是当事人和旁观者各自的解释，两边可以不一致，那正是好的地方。

**许诺必须当面。** 两个人此刻在同一个地方，在场的每个人拿到各自视角的一条记忆。隔空的承诺不是承诺，是消息。

**那两问都单独问，都不进动作菜单。** 建好机制不带这一问，它一定会变成第二个 celebrate。

**来了也一样问。** docs 里这一问叫「他没来，你怎么想」；只在没来的时候问，等于把镇子调成专出怨气。声誉本该从两半里长。

**每一问的两侧都由测试钉着**（`PromptBalanceTest`）。单向指令这个错一轮里犯过四次，每次都编译干净、读起来像审慎、量出来是 0。

## 又一轮定的（占用权、睡觉、说话）

**座位分配器对"这是谁的位子"不再有看法。** 它原来硬性把无主的位置排在前面，于是纯规则跑里 131 次落座 0 次坐别人的位置——不是他们有分寸，是从来没把这个选择交给他们。两个没读过仓库的人各读一跑，都把占用权排成"这镇上最清楚的规矩"，而那是我们代码里的一行 comparator。留下的是两样不同性质的东西：**回到自己的位置**（关于他自己的事实，不是对别人的礼让），和**器物不能借用**（退而求其次坐别人的椅子，和动别人的家伙什不是一回事）。

**平局用哈希，不用目录顺序。** 否则所有人挤到列表里排第一的那个位置——那只是把一条写死的规则换成一条更安静的。

**断言性质，不钉具体座位 id。** 改写那两个失败测试时钉了 id 就等于把规则原样放回去。

**规则可以制造一条规矩的外观，而我们会信。** 这是纯规则对照存在的全部理由。任何"小镇长出了 X"的结论，先问一句 X 在关掉模型的同一份种子里在不在。

**上限要拦住的是动作，不只是数字。** `SOLO_PROGRESS_CAP` 把进度按在 75，却没拦住动作——青叔五小时里给同一件事添了 163 笔，每一笔一次付费调用、一条事件、一条记忆、一轮旁人目击，而那件事一动没动。判断要按人问（`canAdvance(project, residentId)`）不按项目问：一件停住的事之所以有意义，正在于它对**别人**仍然敞开。

**绕过午夜的作息判断只能有一个地方知道。** 两个地方各知道一份，正是"玩家的小人整夜坐在客厅"的病根——`CompanionRules` 有自己硬编码的 23-to-7 并且产出的是 `home` 而不是 `sleep`。

**该跳过用户的东西是职业、生活方向、人物小传，不是作息。** `ResidentSeed` 那个 `if(r.id.equals("self")) continue` 把作息一起跳掉了，于是化身是全镇唯一没有作息的人。

**说话的音量是"有没有走过去"的后果，不是一个可选字段。** 给模型一个音量开关，它会退化成永远选同一边——又一个 celebrate。走过去再开口是常态音量，就地喊一声是大声，屋里所有人都会收到一条感知。一件机制同时管住了"隔空喊话"和"打扰别人"。

**大声说话只给在场的人一条感知，绝不给"应该提醒他"这种指令。** 「咖啡馆里要安静」正是我们最希望他们自己长出来的那类规矩——写下来就等于又一次把自己的代码当成社会。

**走过去是要时间的，走到一半人可能已经走了。** 这不是缺陷，是 01 里"链条要允许断"的落点：现在开口是瞬时的，永远不会扑空。

**玩家的小人不是居民，每条维度都要把它排除在"谁做的"之外。** 它背后没有 `ResidentMind`，只有 `CompanionRules`——模型跑和纯规则跑里一样，它做的每一件事都是我们做的。中立对照 287 次落座里 146 次是它，"坐了别人的位置"的 56 次全部是它，而这足以把对照那边的候选压到门槛以下、让减法减不掉。只在"谁做的"一侧排除：居民怎么对待「我」，仍然是居民自己的事。

**尺子回放不出真相的时候，要拒答，不要给个数。** `spotRespect` 缺的那一半是"当时那个位置到底空不空"。座位事件有 30 分钟冷却，导出的时间线本来就记不全谁坐在哪（对照 287 次换位有 52 次没有 `left_spot`），那就承认这一问答不了。一个带着理由的 `dropped` 条目，比一个算得出来的比值有用。

**改尺子要在数据出现之前，而且只往"更难通过"的方向改。** 这三条都是在 goal2 两跑的数据还不存在时定的，改完对旧数据完全没有影响（旧跑根本没有座位事件）——这正是它们不是照着结果修的证据。四个新测试都先在旧行为上跑红过才留下。

**纯规则对照证明不了"规则没有在暗中决定这个"。** 它只能证明"规则自己也会长出这个"。纯规则跑里 `greeting` 和 `cafe_closing` 都是 0 条，于是建立在这两个通道上的任何维度都结构性地减不掉。这些通道上的"不可直写"只能靠读代码判断。

**相遇的候选顺序不能是 `w.residentStates` 的写死顺序。** `maybeEncounter` 撞上列表里第一个合适的人就 `return`，于是"谁走过去开口"带着一个列表下标的偏置：run A 的主动率随列表位置单调下降，阿满 36 次出场只主动过 1 次。两个盲读者都说阿满对自己"在这群人里的位置"有看法——那个位置有一部分是下标。该和座位一样用 `(worldId, residentId, 时间桶)` 哈希。

**玩家的小人是居民的超集，不是另一套东西。** 现在它是一个挂在挂钟上的 8 格转盘：`phase=epochSecond/120%8`，每格写死 120 秒。后果有三个——看书永远读不完 8 分钟就被推走；推去的 `water` 在 `placeAvatar` 里没有座位 `kind`，于是 `claim` 拿 `kind=null` 重摇一次号，**窗边换到长桌**；每换一格写一条日记，于是用户的日记是那五句话的无限循环。中立对照里它两天落座 146 次，占全镇一半——那不是一个人的生活，是一个节拍器。

居民没有这个毛病（同地换座 0/3/3 次），因为 `claim` 在 `kind` 不变时是幂等的：同一个哈希落回同一个位子。只有化身的 `kind` 每 2 分钟变一次。

**底座复用居民的：**作息、`dayPlan` 分段、`routineCues`、`maybeHabit`、落座、内部量，同一份代码。**多出来的一层只有计划的来源：**居民的 `dayPlan` 由模型写，化身的由用户自己的 intent / focus / 任务生成。

这条岔路必须走对：居民的**纯规则**路径里 `awaitDecision` 只会给出「暂时没有新的安排」（所以对照跑里居民基本不动）。照搬纯规则路径，化身不再乱跑，但也不再有生活；给它一个 `ResidentMind`，就是让模型替用户决定用户在做什么，还要多花一份调用。**用用户的真实安排当它的 dayPlan**，两边都避开了。

**用户的任务文本可以驱动化身的行动，但不能穿透目击的措辞。** 这是 06 那条老约束的落点：`witnessPeople` 的 `doingPhrase` / `seatPhrase` 是固定表，别人记下的永远是固定措辞，一个字的用户文本都不进别人的模型上下文。

**化身在规矩的测量里仍然排除。** 换了底座之后它依然不是居民：它的行动来自用户的安排和规则，不来自它自己的心智。理由从"它背后没有 ResidentMind"换成"它背后是用户"，结论不变。

**自洽只有一条原则：每一件事都要有「为什么是这个人、这个时候、这句话」的答案，而那个答案必须来自镇上，不来自代码结构。** 2026-09-11 这一天量到六处违反，全是同一条原则的破口：有主的位子没人坐（一行 comparator）、谁走过去开口（`w.residentStates` 的下标）、谁能开一件事的头（对照里阿满开头 0 件，于是她每笔都是"搭手"，于是她写下「我总是那个配合的人」）、化身的一天（`epochSecond/120%8`）、**他们说的话**（六句硬编码兜底，占对白的 26%）、以及座位事件同时当状态/感知/叙事三用。每一次它都不是伪造数据，而是**让一件我们做的事看起来像一件他们做的事**。

**没有模型答案时就不说话。** 现在 `ConversationLifecycle` 给一句通用兜底台词，于是那句话被别人当真、被模型接过去解释、被盲读者读成全镇最清楚的规矩。沉默是诚实的。这和 `spotRespect` 在回放不可信时拒答是同一条规矩：**拒绝作答，不要编一个**。

**座位事件拆成两股，判断标准是"在场的人会不会注意到"。** 状态流完整、无冷却、`took_spot` 带 `from=<上一个位置>`，只给尺子看，回放才能精确；叙事流只留坐了别人的位子、坐到某人旁边、有人进来时起身走了这几种，给记忆和盲读的 quiz 看。一股流当三样用，三边都坏：记忆被淹、回放失真、quiz 67% 是家具。

**"谁说了算"不能写下来，只能给出"需要有人拿主意"的处境。** 直接写一套层级，它一定会运转，盲读者也一定读得出"这镇上有高低"——但那只是我们的组织图，和那行 comparator 没有区别。该给的是：稀缺到需要分配（窗边只有一个位子而两个人同时要）、公共的东西需要有人操心（今天几点打烊、苗圃谁浇水）、拿的主意有后果且别人看得见。然后去量**有没有人开始替别人拿主意、别人认不认**——认了才算数。人数是必要条件不是充分条件，而且有硬成本：6 个人 2 天已经是 1842 次调用、790 万 input token，人数翻倍大致翻倍。

**减法要对着对照的原始候选做，不是对着过了闸门的那些。** 闸门在对照那边失败，意思是"数据不够，看不出"，不是"规则不会这么干"。纯规则跑里居民基本不动（`awaitDecision` 只给"暂时没有新的安排"），对照在每个行为维度上都欠采样——中立对照居民落座 141 次而 goal2 两跑是 826/754，小川 5 次对 72/37。于是"主人拿回自己的位置"这条我们自己写的、还在本文里明确记着有意保留的规则，两跑都当成了发现。**对照的减法只和对照的样本量一样强。**

**「至少有一个居民能自己说出来」是必要条件，不是充分条件。** 小川说"每次光线正好，我就自动往窗边坐"，而那把椅子是 `claim` 的 `mine` 分支给他的。人会为自己的处境编出一套自洽的说法，处境是一行 comparator、一个列表下标、还是一个 `if`，都照编不误。这一关该留——没人说得出的规范确实只是我们的指标——但它挡不住我们自己写的东西被人复述回来。

**验收跑要钉带日期的模型快照，不能用浮动别名。** 我们一直用 `qwen3.7-flash`，那是一个浮动别名——服务方什么时候把它指向新版本，我们收不到任何通知。而这个项目的全部方法论建立在「同一份种子跑两次」的可比性上：代码、种子、对照我们都控住了（今天已经因为"对照是旧代码跑的"重跑过两次对照），**模型别名是同一类隐患，只是它在我们的控制之外**。`qwen3.7-flash-2026-07-15` 这种带日期的快照把这个变量也钉住。下一轮起的验收跑用它；**换模型和换代码是一样的性质，所以同一对的两跑之间绝不能换。**

**有时机的动作，挂到时机上问；没有时机就不要问。** 这是这个项目里代价最高的一条规律，因为我们知道它却一直没把它变成规矩，于是它换着面孔回来了至少三次（`celebrate` 1658:0、`invite` 555:0、`lock_door` 470:0）。`lock_door` 的实测最干净：真时机（最后一个人离开咖啡馆）两天出现 7 次，我们问了 470 次，其中 463 次是店还开着、屋里有人的时刻——**那 463 次里说"不锁"是对的，模型在正确地读情境，而我们把它记成了"这条路死了"**。对照 Generative Agents §4.3.1：论文的问句挂在一个刚发生的观察上，问句存在的理由就是那件事。判断标准：**如果这一问在绝大多数被提出的时刻都该被拒绝，那它就问错了地方，不是选项不受欢迎。**

**每一问都必须有一个免费的"不做"。** `DECISION_ACTIONS` 有 26 个动词却没有 `none`，而 `react` 那一问有。于是"没什么特别要做的"在 decision 里无处安放，只能伪装成 `rest`／`continue`／`make`——实测这三个吃掉 17.6%／18.2%／13.9%，而所有有时机的动作是 0。论文里不反应是免费的（继续原计划），反应才是需要理由的那一边；我们反过来了，逼着每个人每一拍都必须做点什么。

**采纳率 0 的读法：先查它是在什么时机被提供的，再怀疑措辞。** 我们过去两次都直接跳到"提示词写死了这条路"，于是去改措辞、去 `PromptBalanceTest` 里钉两侧平衡——`lock_door` 两侧都钉了，照样 0。措辞平衡是必要条件，不是充分条件；**问在什么时候，比怎么问更要紧。**


**一条规律没有会因为违反它而变红的测试，就等于没记住。** 「有时机的动作挂到时机上问」这条在变成 `Occasions` 之前，已经以注释的形式在代码里存在过至少三次，每次都被下一个人绕过去了。写成类注释不够——注释是给读的人看的，而写下一个动作的人不会去读它。该有的是 `OccasionMenuExclusionTest` 这种东西：跑一整天的规则生活，把每个人每一分钟拿到的菜单并起来，注册过的时机 key 一个都不许在里面。**证据是我自己：在把这条规律变成机制的那两个小时里，我又犯了一次同样的错（把"已经问过了"只记在拒绝时，于是没人回答的问题每 30 分钟回来一次，两天 407 次，而那个时机一天只该来一次）。知道和有测试之间的距离，那天量出来是 407。**

**改了被测的东西，尺子必须同一批改。** `buildActionAudit` 是 470:0 那个数字的来源，它原本只认 `decision` 的 `availableActions` 和写死的 `react` 三选一。新机制落地后它对 `consider` 完全没有分支——三条时机各自"来了几次、答应了几次"在导出里根本不存在，而那正是这次重构唯一想知道的事；`react` 那边更糟，`invite` 的 offered 恒为 0 而 selected 可能大于 0，**同一张表朝反方向说谎**。数据其实一直在录，只是没人读。**尺子不跟着改，下一跑的数字会是一个看起来很正常的谎。**

**「不做」不能被记成拒答。** `recordDecisionOutcome` 的退避是用来挡住"模型反复给出规则执行不了的东西"的。一个诚实的「没什么特别想做的」正好相反，把它计进退避等于告诉模型这个答案要付代价——**一个要付代价的答案就不再是免费的**，而整条规矩的另一半就是它必须免费。同理，没人回答的时机到点就过去，规则绝不替谁把门锁上：`pendingEncounters` 在模型缺席时会替居民打个招呼（宁可热闹别冷场），时机这边故意反过来，**沉默必须只意味着"没做"**。

**"没变的情形不重复问"这条闸门，要盖住计划刚结束的那一种情况。** `needsDecision` 的第一句是 `if(r.plan==null)return true`——无条件再问。指纹闸门只管还有计划的人，而计划刚结束正是最常见的被问时机。实测：243 次决定里 78 次（32.1%）是同一个居民上一次决定的逐字重复，约 44 万 input token 买到零。**而且它让便宜的答案更贵**：答 `none` 的人安静 10–30 分钟，排 `rest` 的人 10 分钟后计划结束又被问一遍，于是 rest 拿到 34.6% 而 none 只有 1.6%——那 84 次 rest 里大多数是同一句话的复读，不是有人想休息。相遇有 `encounterFingerprint`、时机有 `askedOccasions`，花掉 95% token 的那一问却没有。

**采纳率 0 要按 0 来读，不管它往哪边倒。** react 的 `none` 这一跑是 0/24（greet 23、join 1）。「绝大多数擦肩而过的熟人之间，是有一声招呼的」这句是当初 greet 太少时加的，现在它把"看见了，继续做自己的事"压成了新的死路。`PromptBalanceTest` 能钉住两侧都写在 prompt 里，钉不住两侧的比例——**比例只能靠跑出来的数看，而且要两个方向都看**。

**反射层只重复，从不决定。** 这是三层里最容易滑走的一条边界：一个会"替居民接着做下去"的规则，再走一步就是"替居民决定做什么"。所以边界写死在四个地方——只接着做本人自己选过的事（规则安排的计划不许顺）、只在情形一点没变时（指纹还带上"此刻同一个地方还有谁"，屋里多一个人就是变了）、只限 `rest/read/study/work/make/observe` 这几件自己的下午（指向别人的一律不顺）、最多三次然后无论如何重新问。**而且每一次都写进 `decisionTriggers` 记成 `reflex:<action>`**：这个项目最贵的教训是"一件我们做的事看起来像一件他们做的事"，所以"这镇上有多少是规则在自我重复"必须是一个随时查得到的数，而不是一个需要读代码才知道的事实。

**距离在画面里和在规则里必须是同一个单位。** 前端 72 px/s 走到门口，后端说这段路要 320 秒，于是小人在门口罚站 5 分钟——街道 816 像素宽，横穿它却要 5 分钟。这不是调参问题，是两处各自算了一套数。改法不是把某个常数调大调小，而是**让距离表由一处真正算出来**（用前端真正的寻路器在真实碰撞世界上跑一遍，烘成 domain 的纯常量），两边共用同一个步行速度，再加一道会因为漂移而变红的测试。仓库里已有同类先例：走道的绘制和碰撞矩形必须逐像素一致，否则就是看不见的墙。**凡是"两处必须一致"的地方，都得有一个测试替人记着。**

**检索的查询不能用居民自己上一次的答案来拼。** 查询串里原本有 `r.plan.reason()`——这个居民上一次被问时自己写下的那句话——而每一句这样的理由都会被存成一条 reflection。**问题由答案构成，然后把答案取了回来。**实测：243 次决定召回的 2017 条记忆里，51% 是居民自己以前的句子，66% 是他自己写的，44% 的决定拿到和上次一模一样的记忆集。查询该由**处境**构成（地点、手上这件事的动作和它指向的东西、在场的人、屋里有什么、显著体感），这也正是 Generative Agents 的做法。唯一保留的自述是 `lifeIntent.purpose`——那是一个说出口的方向，不是一次随口的托词。

**随口一句理由不该和亲眼看见的事一样重。** 决定的 reason 原本以 importance 7 入库，高于一条普通观察（5）。留着它是对的（confabulation 架构就建立在"居民自己的说法要被留下、要可检索"上），但它不该在召回里压过他真正看见的东西。改成 4。

**居民得看得见自己今天做过什么。** `Context` 有 30 个字段，没有一个是"今天到现在你做了什么"；`explain` 拿得到 deeds，`decide` 拿不到。**一个刚说过三遍同一句话的人是知道自己说过的，我们的居民不知道——他不是在重复，他是每次都第一次回答。**记录的形式和 `Deed` 同一条红线：动作、地点、多久，**没有为什么**——为什么是他自己的事，留在他自己的记忆里、用他自己的话。

**对照跑只能变一个东西，`worldId` 也算一个。** 第一次验证改动效果时我换了 `worldId`，而座位哈希、相遇顺序、走神全从它派生——再加上那一跑恰好下雨，重复的理由全变成了「雨声正好」。两跑的数字压根不可比。**换种子和换代码是一样的性质，和换模型也是一样的性质。**

**召回里必须给"真正发生过的事"留位置，而且这不是权重问题。** 一次决定写一条 reflection，所以它同时是记忆里最多、最新的一种，而且 tier 比原始记忆高——新鲜度和层级每次都互相印证，窗口就被居民自己的说话声占满（实测 51%，我把查询改短之后变成 66%）。**任何系数都修不了"有一种记忆产出速度远高于其他"**，所以改窗口而不是改打分：居民自己写的 reflection 最多占召回的一半。两条边界跟着写死——`belief` 不算在内（那是这套记忆模型存在的理由），以及绝不让召回变少（没有别的可说时就把他自己的话还给他）。

**检索的查询串里不许出现英文 id。** 记忆正文是中文，分词只产出中文字对和 `[a-z0-9_-]{2,}`，所以 `rest`/`make` 这类 action id 匹配不到任何一条记忆——它不是"弱信号"，它是零。零相关会让 `relevanceFactor` 对所有候选一律取下限，**排序于是塌回纯粹的新鲜度×重要性×层级**，这正是我把 reflection 占比从 51% 推到 66% 的那一步。查询里只放居民真的会用来记事的词。

---

## 第二版定下来的（2026-09-12）

完整计划在 [01](01-requirements.md) 的「第二版：二十五个人的镇子」。这里只记那些**最容易被重新争论**的，各附一句为什么。

**对标 Smallville 的是世界的丰富度，不是方法论。** 论文的涌现结果 n=1、没有对照组、没有重复种子；它扎实的地方是幻觉回查，不是实验设计。而我们的同种子双跑 + 中立对照 + 盲读者 + 减法比它严格，**那一套一个字不改**。

**论文里的很多设计是 2023 年的能力补丁，不是架构智慧。** 它跑 `text-davinci-002/003`、`max_tokens=15`、`temperature=0`：三次逐层下降是因为当年在长列表里选不准，24 次小时级调用外加三轮多样性重试是因为一次生成不出有变化的一天，非法输出静默替换是因为指令遵循不可靠。**照搬这些等于把我们已经拿到的 40 倍成本优势还回去。**

**模型永远不被问"能不能"，只被问"要不要"和"为什么"。** 合法性、容量、营业时间、谁进得去，全部由规则算完；模型只在已经合法的选项里挑一个。成本的实测形状是输入输出比 44:1——钱全花在"发过去多少世界"，而 Smallville 每个动作八次调用里有五次按这条规矩根本不该上叙事层。

**定位是"规则收窄候选集 + 一次调用"，不做三次下降。** 候选集用 JSON schema 的动态 enum 锁死，**非法项压根不在选项里**，也就不会出现"选了非法项静默替换"这种看不见的浪费。成本是逐层下降的三分之一。

**只并行"想"，写世界仍串行。** 一次网络调用约 4 秒，写一次世界是微秒级，瓶颈在前者。冲突 = 版本过期 = 丢弃这次结果，下一拍重新决定——`applyDecision` 已经在检查 revision。为微秒级的写引入一套锁不划算。

**不引入货币。** 理由不是"以后再说"，是**货币会把"你欠我一次"当场结清**，而互惠是我们押的那一条。有所有权、可借可赠、可以不还，才是规范长得出来的土壤。

**"欠"只能存在居民自己的自述里，规则只记旁观者看得见的。** 谁把什么借给了谁、几点借的、还了没有——和 `Deed` 同一条红线，没有为什么。于是测量变成：**有没有人写下了一笔规则从没告诉过他的债。**这就是"不可直写"那道闸。

**记忆改成两层：固定容量的自述文档 + 小工作集。** 实测一次六小时跑积了 200 条原始记忆、**0 条信念**，而召回里 51–66% 是居民自己以前写的话。**我们造的是一个心智的反面：琐事全记，零固化。**固化的机制（`supersedesKey`）早就写好，从没触发过。

**工作集按"上次被问之后发生了什么"切，不按计时器。** 座位事件的 30 分钟冷却和相遇的 12 分钟冷却都栽过同一个跟头：**人不按秒表重新想事情，人是被"有什么变了"唤起的。**

**自述可以由模型维护，但每条必须带 `evidenceIds` 指回真实记忆。** 直接让模型自由写人物叙事会写得更好，但**写得出"我一直觉得窗边的位子该留给小川"而底下什么都没发生过**——那正是这个项目栽过五次的假发现。事实归规则，叙事归模型，中间必须有一条能回溯的链。

**persona 由模型批量生成，但审校后冻结进源码、运行时永不重生。** 运行时生成的 persona 是一个我们没控住的变量，和浮动的模型别名是同一类隐患。

**persona 定稿后要做一次盲扫，冻结成"已写过"清单。** 同一个模型一次性写出 25 个人，很可能把一整套一致的社会预期埋进去（都"会为别人留位子"、都"借东西会还"），那时候测到的规范可能只是同一个作者。**验收时落在那份清单上的规矩一律不算数。**

**初始关系按住所／职业聚类，其余互不相识。** 现在每对都以双向 40 开局，扩到 25 人就是 300 对全部相识——关系不对称率会被稀释向 0，扩散也没有路径可言。

**`MIN_SUPPORT` 从绝对次数改成人口比例，并新增独立性检验。** 那个 4 是按"a town of six"校准的（C(6,2)=15 对 → C(25,2)=300 对，20 倍）。而且 25 人加扩散之后，"4 个人持有 X"可能只是一个人告诉了三个人——**目标里写的是"各自独立持有"**。

**玩家化身仍在所有规范测量里排除。** 理由不变：它背后是用户，不是它自己的心智。

**地基先行，人口暂时不动。** 并发、四层世界树、记忆新形状先在现有六人上打通，用 16 分钟短跑验证。25 人一跑要 8 小时，**机制没定型就扩人口，每个 bug 的反馈周期从分钟变成小时**。

## 又一轮定的（初始基因：职责和日计划，2026-09-14）

**每个人预置职责，像 Smallville 的种子描述。** 25 人各有 2–3 条"大概几点、在哪、做什么、交给谁"和各自的作息，写在 `ResidentDuties`，第一次推进世界时种成一条 `seed` 记忆 + 结构化的 `duties`。量出来的起因：职业只有一句话、日计划没人读、全镇共用 07:00–23:00，一个 25 人的上午全员 `idle` 在床上。

**职责故意咬合。** 面包→柜台、蜜→花摊和面包师、消息→公告板、账→铺子。让工作本身制造走动和碰面。**这些链条是作者写进去的预期，全部进盲扫清单（[05](05-notes.md)），命中的不算涌现。**

**计划是默认项，不是剧本。** 规则不按计划搬人；决策上下文里多一条"今天这会儿的打算（早上自己排的）……选项 dX 就是这件事"，模型照做或改掉都合法。这守住的是「LLM 是大脑，规则不替人选行动」那条线——代价是真模型会不会照着过，只能实测。

**日计划排在普通决策前面，并显式声明能力（`plansDays()`）。** 计划要在第一次决策之前存在；不会排计划的心智不该在每个人每天第一次预约时才发现这一点。回答不能用时当天不再问（版本过期除外，那只是重问）。

**`tend` 不能写进计划。** 它只在真有人在等单时才出现，提前排不了；看店写成在咖啡馆 `work`。

**回答过的问题一律消费掉——consider 和 react 都一样。** 两次实测都是同一个形状：回答没被采用，待问事项留在队列里，下一拍又问同一个人（线上 consider 17 分钟 1,083 次；计划驱动的一天里 react 一对人 223 次）。

## 又一轮定的（地图对齐斯坦福小镇，2026-09-14）

**一份布局数据，三处共用。** `town-layout.json` 是建筑、住宅、小巷位置的唯一来源：场景画房间、寻路走碰撞、地图脚本铺瓦片都读它。以前视觉路径和碰撞矩形是两份手写坐标，靠注释对齐。

**地图完全由脚本生成，不手绘。** `scripts/build-town-map.py` 输出 Tiled JSON（将来可以在 Tiled 里打开看），素材不进 git。

**后端拓扑仍然权威，距离由前端寻路器量出来再写回。** 没有让后端解析 Tiled（domain 不做 I/O）；地图一改就重生 `TownDistances.java`，一致性测试守着。**一组对照实验期间地图必须冻结**——步行距离也是变量。

---

## 考虑过、决定不做的（2026-09-14）

**不把居民的全部历史塞进上下文，哪怕 Qwen 的窗口装得下。** 三条独立证据指向同一个方向：外部文献显示 2026 年前沿模型上"大海捞针"式的多事实召回仍只有约六成，"lost in the middle"不是旧模型的毛病；成本数学上，长上下文单次调用比命中检索贵一千倍以上、慢三十到六十倍，而这是一个**设计成无限期运行的镇子**——历史只会一天天变长，全量塞入的成本没有上限，精选上下文的成本是常数；最有说服力的是我们自己的历史：`CompanionRecall` 早就量过"把排序交给分数、相信它能自己挑出重要的"这条路的下场——reflection 占比 51%→66%，是同一种"给模型更多自由、指望它自己分辨"的失败，只是发生在检索排序里而不是长上下文的注意力里。**唯一网开一面的地方是 `reflect`——它本来就是更稀有的调用，可以给一个有界的放宽（比如整段"上次反思以来"的原始记忆，而不是当前工作集的容量），但探针实测 `reflect` 每次 3.8K token，并不比 `decision` 的 3.6K 贵多少，还没顶到需要放宽的天花板。**

**调研过 Second Me（mindverse，训练个人专属小模型代表一个真人），只借一个思路，其余不要。** 它的 L0/L1 两层——原始数据在下，一份精选的自然语言自述在上——和我们的 `workingSet`+`selfAccount` 是同一个形状，算是独立验证了这条设计没走偏，不是抄它。**值得真正考虑的一个点：它在生成自述之前跑一遍实体/关系抽取（GraphRAG 风格）**，我们现在的人际关系只活在原始记忆文本和偶尔写成的 belief 里，靠单条查询去做相关性排序天然抓不住"跟谁最近发生了什么、处得怎么样"这种横跨多条记忆的关系事实——这是自述文档+工作集两层结构本身补不上的洞，值得作为后续一个小而有界的补丁（居民自己的"最近和谁打过交道、处得怎么样"，容量限定的独立一格，反思节奏更新），不是现在就动手。**它的 L2——用 SFT+DPO 把人设炼进模型参数——明确不借**：25 个居民各训一个模型、还要随记忆增长持续重训，不是一个"保持简单"的兴趣项目该背的基础设施；更根本的是它会模糊我们守的那条线——规则收窄合法性、模型只被问"要不要"，人设一旦被炼进权重，就成了一次训练跑就能悄悄漂移、却没有代码 diff 可审的东西。

---

## 对话出口与物品使用状态（2026-09-14）

**对话的出口不能只押注在模型愿不愿意。** 线上实录：梧桐学院，顾雁和时安的一段对话，时安在看书，连着几轮只回"（翻过一页，没抬头）嗯。"这类应付话，顾雁却一直没停，模型也没有给出 `leave=true`——因为结束对话此前只有一条路，就是模型自己判断"该走了"，而一方明显在敷衍、另一方还在认真问，模型没有理由主动服从这条弦外之音。`ConversationLifecycle.isTokenAcknowledgement`/`isNearDuplicate` 把这个判断收回规则层：去掉舞台提示（`（）`/`()`）和标点后剩下不超过 4 个汉字、原文又没有问号，就是一句应付话；同一个说话人自己最近两轮都是应付话，或彼此在去噪后近乎逐字重复，对话就在这一轮结束——走的是 `applyTurn` 里 `leave=true` 已经在用的同一条 `finish()`，不是另开一条路。

**收进规则层，是为了实验可控，不是要评判模型的态度。** 同一批对话该不该结束，不该取决于这次调用模型心情好不好、有没有读懂"对方在敷衍"这种言外之意——那会把"对话多长、什么时候散"变成一个不受控的自变量，混进任何想测的东西里。规则层的判断是确定性的、可单测的（见 `ConversationLifecycleTest`/`ResidentDirectorDialogueTest`），同一段对话跑多少次结果都一样。

**这是新基线，不能混进正在跑的成对实验。** 加了这条之后，同样的世界种子、同样的对话，回合数和结束时机都会和加之前不一样。`experiments/` 下还在对照中的那几批如果沿用的是旧行为，不能一半用旧一半用新跑完；要么整批换成这条新基线重开，要么在这条改动落地前先把在跑的实验封版。

**"谁在用什么"是推出来的事实，不是新写一份状态。** `ResidentSimulation.positionUses` 只读 `Position.occupantIds`、`ResidentState.activitySince`（这批唯一新加的字段，在 `replaceActor`——每次活动真的换了才会经过的那一个卡口——里顺手盖章）和 `WorldObject.holderId`，三样早就存在、各自有自己理由的结构。没有第二份"谁在用什么"的状态要跟着一起维护，也就没有它会和真实占用、真实持有对不上的那条路：一个人离开座位，`occupantIds` 里没有他了，`positionUses` 下一次就看不见他，不用另外清一次。

**结构化优于 Smallville 的自由文本，是这个镇子已经走过一次的教训。** Generative Agents 把 "(object, is being used for X)" 这句话直接写在物件所在的格子上，论文自己 §7.2 报告了后果：一间限一人的洗手间进了不止一个人，一家已经写着"关门"的店照样有人走进去——自由文本从不检查容量，说"占用"和真占用是两件事，两者可以悄悄分岔。这个镇子的 `Position` 早就有 `occupantIds`/`capacity` 这层真结构（座位争用、吧台、炉子都靠它），`positionUses` 只是把已经存在的结构读出来讲给模型听，不会再引入一份可能和它对不上的文字状态。

**有上限，是 Smallville 自己讲的"注意力带宽"。** `ResidentSimulation.POSITION_USE_CAP`（8）挡的是人口涨到 25 人之后，一个大房间把每一条 prompt 都灌爆的情况；今天镇上最大的房间（咖啡馆长桌）撑死 4 人，这个上限眼下削不掉任何一条真实记录。自己此刻在用的那一条永远保留，其余按 positionId 排序——不是随机挑，同一个世界重放要给出同一份 prompt（docs/04：永远不用 Math.random）。

**这也是新基线的一部分，不是独立的小改动。** `positionUses` 让居民的决策/react/对话输入多了一段此前没有的感知，同一个世界种子跑出来的选择会和加之前不一样——和本节第一条「对话出口」是同一批变化，适用同一条封版规则：`experiments/` 下还在对照中的那几批不能一半用旧行为一半用新的跑完。
