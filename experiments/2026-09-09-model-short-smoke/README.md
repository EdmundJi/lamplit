# 加速跑真模型冒烟（2026-09-09）

- **数据**：`data/model-short/`（真模型，`qwen3.8-flash`，worldId `qwen-model-short`）、`data/rule-sample/`（纯规则样本）
- **来源**：原在 `output/town-life-review/`，同目录其他子目录是 UI/美术 QA，不是实验，未归档。
- **结论**：只证明加速跑能接通真模型并导出完整产物；不是行为结论。
- **限制**：模型是 `qwen3.8-flash`，与 2026-09-14 起的固定模型不同，不可直接比较。
