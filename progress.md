# 当前进展

当前分支 `master`。这一页只记两件事：**量出来的现状**，和**知道但还没做的**。
定过的原则在 [docs/04](docs/04-decisions.md)，测法和教训在 [docs/05](docs/05-notes.md)。

## 对话出口 + 物品使用状态 + 头顶动作 emoji：进行中（2026-09-14 下午，未提交）

用户在线上看到学院里顾雁和时安聊了几轮"（翻过一页，没抬头）嗯。"，问物品状态怎么更真实、斯坦福小镇怎么做。调研结论（agent 读源码+论文，主线抽查）：
- 我们：对话是否结束全靠模型自愿 `leave=true`，8 轮硬顶；"不照抄上一轮"挡不住换词复述；搭话 prompt 明确说"对方在看书不是不打招呼的理由"（保留，它治的是没人开口）。
  居民动作与物件是两套数据：`Position` 有 occupantIds/capacity/condition 但居民看不到；前端家具纯装饰；没有"谁在用什么"。
- 斯坦福小镇：每个格子挂事件四元组（物件, 正在…），动作选定物件后写回、下一步清回 idle；感知按视野半径取最近几条进记忆；
  对话 8 轮上限 + 两人冷却 800 步；物件没有视觉变化，只有头顶 emoji。论文 §7.2 自认"浴室多人进""5 点后进店"——纯文本状态没有容量/营业时间。
  我们已有结构化占用，这两类错在规则层就挡住了。

用户拍板"全部做完"，顺序：
1. 对话出口（**已完成，已验收**）：
   - **规则**：`ConversationLifecycle.isTokenAcknowledgement`（去掉括号里的动作和标点后 ≤4 字，且原文无问号）与 `isNearDuplicate`。同一人自己的最近两轮都是敷衍或近似重复，就走和 `leave=true` 同一个 `finish()` 结束，`endReason` 为"对方接连只是应一声，不再多问，各自去忙"。
   - **冷却**：不另加，沿用现有两层：`encounterCooldowns`（40/20 分钟）和 `lastSocialAt` + 15 分钟社交恢复期；`finish()` 对所有结束方式都会写 `lastSocialAt`。
   - **prompt**：对话 prompt 补了一句"对方明显在忙、只用一两个字应付，就收尾 leave=true"；搭话 prompt 第 317 行保留。
   - **测试**：`ConversationLifecycleTest` 新增 4 项（线上原话逐句）；`ResidentDirectorDialogueTest` 新增 1 项（真实 director 管线里假模型始终 leave=false，第 5 轮若被请求就断言失败）。
   - **全量**：后端 664 项，0 失败、0 错误、9 跳过。
   - **决定**：已写入 docs/04。
2. 物品使用状态（**已完成，已验收**）：
   - **只加一个字段**：`ResidentState.activitySince`，在 `replaceActor` 活动名变化时写入；22 处生产调用都传了时间，仅测试用的 5 参重载不写。
   - **使用视图是现算的**：`ResidentSimulation.positionUses(w,id,now)` 从 `Position.occupantIds`、`activitySince`、`WorldObject.holderId` 算出 `PositionUseView(positionId,label,occupantId,occupantName,activity,minutesSoFar,heldObjectLabel)`。
     - 范围：同屋（沿用 `sameRoom`，含街上听得见的距离）。
     - 上限 8 条，自己用着的那条总保留。
   - **进 perspective**：所有模型调用的 perspective 都带上它，但只有搭话 prompt 加了一行说明。
   - **容量**：已有的容量规则在测试里确认生效，一人位不会显示第二个人。
   - **前端**：世界 JSON 新增 `residentStates[].activitySince`。
   - **测试**：`PositionUseViewTest` 4 项；director 管线测试 1 项，断言假模型收到"阿禾 在那张长桌 看书 40分钟"。
   - **全量**：后端 669 项，0 失败、9 跳过。
   - **已知局限**：被拉进对话后活动变成 talk、`activitySince` 重置，"看了 40 分钟书"只在搭话之前可见。对话里的敷衍由规则 1 兜底。
3. 前端：头顶动作 emoji（**已完成**）。原来 `residentStatus()` 算出 emoji 却没用上，现在 `actionEmoji()` 覆盖后端全部活动名，`CompanionScene.vue` 在头顶显示 20px 小徽标：
   - 已经在显示对话 emoji 的人不重复显示；`chrome:false` 的街道条模式隐藏；减少动效时不上下浮动。
   - 首轮截图家家头顶都是 ☕，像全镇在喝咖啡，打回修正：只有在咖啡馆里的 rest 才用 ☕，其他地方的 rest 用 🛋️；`observe` 用 👀，`water` 用 💧。
   - 验收：前端 263 项通过，vue-tsc 通过，town-v2 浏览器测试 4/4。
   - **用户看后撤回**："所有小人都显示 emoji 状态太乱了，恢复原本的样子"。已派 agent 只移除头顶徽标，地图回到 HEAD 的样子（只在说话时显示对话气泡）；其他未提交改动保留。
     **教训**：25 人同屏时，人人头顶常驻标记就是噪音。斯坦福小镇只有 25 人且没有房间墙体，照搬不合适；以后若要，只在选中或悬停时显示。
4. 物件使用画面（**已完成，已验收**）：
   - `companion-position-props.ts`：纯函数 `positionUseProps` 从已有数据推导出三种效果，签名比对后才重建：
     - 学院书桌前看书 → 桌上摊开的书（沿用现有程序化书本图标；图集里只有竖放的书脊和整个书柜，不合适）；
     - 真睡着（`visibleActivity==='sleep'`）→ 床上盖冷蓝色被子；
     - 咖啡馆营业且有人在柜台 tend/prepare → 咖啡机冒热气（减少动效时静止）。
   - `CompanionView.vue` 人物面板在当前动作后显示"已经 N 分钟"。
   - 验收时打回两次：
     - 被子起初是脚下的灰椭圆，像影子，而且白天坐在床边 rest 的人也画；改为只给睡着的人画，并盖在床上。
     - 睡着的人头画在墙上。这是原来就有的 bug：睡姿统一偏移 -40，没对床的几何校准。新增 `sleepSpriteOffset(positionId)`（合住户 -4，其余 -15），裁图确认头枕在枕头上。
   - 存档里没有正在看书或睡觉的人，所以书、热气、被子是用临时 Playwright 探针加单元测试验证的（探针已删）。
   - 前端 broad 范围 284 项通过，vue-tsc 通过，浏览器测试 4/4。
   - 前端全量曾有 1 项失败：寻路可达性测试在 72 个文件并行时 5.8s，超过 5s 上限（单独跑 2.0s）。原因已量清（420 次整镇寻路，瓶颈在逐矩形碰撞检测），给这一项单独设 30s 超时并写明原因。

**收尾验收（2026-09-14 17:30）：**
- 头顶徽标已移除：`CompanionScene.vue` 与 HEAD 一致，`companion-presentation.ts/.test.ts` 整体恢复到 HEAD（`actionEmoji` 已无调用方）。截图确认 25 人头顶无徽标。
- 前端全量 `npx vitest run` 72 文件 450 项全过（emoji 测试随之删除，所以比 507 少）；vue-tsc 通过；town-v2 浏览器测试 4/4。
- 后端最近一次全量 669 项 0 失败（物件使用状态之后后端没有再改）。
- 全部未提交。
这些都改变行为，是新基线，不混进进行中的对照实验；决定写入 docs/04。

## 地图重做 M1–M2：进行中（2026-09-14 夜，未提交）

用户选了"先地图、完全脚本生成"。对照斯坦福小镇（Tiled 地图同时是渲染和环境树的来源），我们走三步：

**A 步 已完成（纯重构，零行为变化）。** 新增 `frontend/src/modules/companion/town-layout.json`（建筑原点、16 户住宅、
小巷 walkways/paths），`town-layout.ts` 的 `shift(building,x,y)` 让原本写死绝对坐标的咖啡馆/花园/街前广场内部
随建筑整体平移。`companion-art/navigation/stage/scene/geometry` 改为读布局。验收：布局等于旧坐标时前端 190 测试全绿、
距离一致性测试通过（距离一个数没变）。

**B 步 已完成。** 世界 2048×1408 → 2560×1856：西区核心 (+128,+96)、东区学院/公告板/健身房 (+288,+96)、
商店 (+384,+96)、南巷第一排 (+128,+192)、第二排 (+128,+256)，所有门与小巷连通逐一核对。
用 `TOWN_PRINT_DISTANCES=1` 在真实寻路器上重生 `TownDistances.java` 253 条：组内距离完全不变，跨组平均 +128px。
测试里的旧字面坐标改为 `west(x,y)`；`TownDistancesTest` 的回退值改为按最长距离计算。
后端 `TownDistancesTest/StreetEncounterDistanceTest/ResidentDutiesTest/ResidentSimulationTest` 15 项全绿。

**C 步 进行中。** `scripts/build-town-map.py` 从布局 + LimeZu Modern Exteriors 生成 `public/assets/town/maps/town.tmj`
（ground/paths/water 瓦片层 + decor 对象层）与 `town-tiles.png`（统一调暖、降饱和）。瓦片坐标由 agent 逐个验证过无缝，
存档在 `tmp/handoff/terrain-palette.json`。场景 `buildGround()` 读地图，读不到就退回纯色底。
**C 步第一轮迭代（compact 后，改文件交 sonnet agent，主线验收）：**
- 看了瓦片版截图，发现两个问题：默认镜头按宽度适配后，多出的高度越过地图上沿，露出约 200px 空底；草地 (110,144,109) 偏冷、偏暗。
  - 修法：`frameCamera()` 在非总览模式下把中心夹在世界范围内。
  - 生成器新增 `MATCH`，把草地和人行道的平均色平移到纯色画法原来的 0x96a486 / 0xc5bfa8；池塘岸边的草与草地同用一个偏移。
- 生成器新增东南角带铁栅栏的公园：x 1792–2464、y 896–1760，11×6 池塘，泥土环路与通向门口的小径，长椅、花坛、鸭子，瓦片层 `fences`。
  - 装饰按离建筑的远近分级：地图边缘成林，开阔草坪零星种树，房屋附近只放小草和花。
  - 抽查：装饰落在建筑或人行道上 0 个。池塘和围栏按 3 倍放大裁图看过，没有缝。
  - 公园小径只是装饰，寻路不知道它，居民不会进公园。
- 寻路超时。agent 实测，查询数没变，一直是 105 个位置 × 4 = 420 次，变慢全因世界变大；开放集每步整体重排是已有的低效。
  - 修法：`shared/scene/pathfinding.ts` 改为最小堆，按 (score 升序, 插入序号降序) 出队，与原来"稳定排序后 pop"的平局规则逐位一致。
  - 验证：1048 条路径改前改后 JSON 完全相同，`companion-walk-parity` 通过。
  - 该测试单独跑 2.55s → 2.0s，只快了约 20%，说明主要开销在 `collision.ts` 的逐矩形检测；目前余量够，暂不再动。

**实测（2026-09-14 15:20 左右）：**
- vue-tsc 通过。
- 前端 `src/modules/companion src/shared/scene src/app` 206/206 通过。
- 后端 `mvn -o clean test` 659 项，0 失败、0 错误、9 跳过。
- town-v2 浏览器测试 desktop + mobile 4/4 通过，截图已看，默认镜头不再越界。

**第二轮装饰修整（已验收）：**
- `grass_*` 全部 22 帧其实是不透明的草–泥过渡地形瓦片，不是植物，所以画成了深绿方块；已从种植列表移除，小植物改用透明底的 `flowers_1..5` 和 `shrub_1`。
- 路灯改为沿长巷每 340–380px 放一盏，放在巷边而不是巷上，避开墙和门口，共 17 盏。
- 公园南侧两角补了树和花坛，门两侧各一盏灯。
- 所有装饰的坐标夹在世界范围内。
- 抽查：装饰落在建筑内 / 人行道上 / 越界都是 0，共 314 个。
- town-v2 浏览器测试 4/4 通过；我看了桌面总览和默认视角截图，方块消失，路灯沿巷排列，镜头不越界。

C 步到此可以收：地面、公园和种植都由脚本生成，风格和原来的纯色画法一致。剩下的是生成说明文档（已派 agent）和决定是否提交。
下一阶段 M3：建筑外墙和屋顶（拉近时淡出），住宅做 3–4 种模板。

## 午饭、午休与计划提示（2026-09-14 夜）

**线上看到的：** 12:26 有 11 人在家、5 人在睡。7 人是模型自己排的"在家补觉/午休"——种子里中午是空档；
苏晚、顾雁是新人出生时"刚搬来，还在认路"被读成"倒时差"，11:17 就去睡；小吉、陆敏、老白把
"接下来的打算：12:30 去咖啡馆"读成"先在家等"。

**改了：** 职责表升到 v2，23 人加午饭（咖啡馆 6、花园 8、广场 4、学堂 3，老宋在家歇），旧世界按版本补上、
换过职业的人不补、种子记忆不重复；"刚搬来"满 6 小时后回到"如常"；"接下来的打算"写明步行分钟与还剩几分钟，
快到点时点名选项；在家歇的段写明"歇一会儿，不是睡整觉"。白天小睡的人醒来后照常先补排日计划（加了测试）。
companion 547 tests 全绿。**没量：** 真模型改完后中午的实际分布。

## 初始基因：职责种子 + 自己排的日计划（2026-09-14 晚）

决定和理由在 [04「初始基因」](docs/04-decisions.md)。这里只记做了什么、量到什么。

**做了：** `ResidentDuties` 给 25 人各种 2–3 条带时段、地点、交给谁的职责和各自作息（草稿由 agent 起、按规则审过：
咖啡馆 09:00 前进不去，所以老宋天没亮在家烤、开门后送到店；`tend` 改 `work`），第一次推进时种成 `seed` 记忆，旧存档自愈。
日计划改成结构化 3–6 段（HH:mm、knownPlaces 里的地点、动作、一句话），醒来后先于普通决策排；
决策上下文加"今天这会儿的打算……选项 dX 就是这件事"与"接下来的打算"；在那个地点待过这段就记 `done`，没做的第二天成"没顾上"。
`ResidentDutiesTest` 断言每条职责在自己时段里真能被选到——第一次跑就抓出阿禾 `tend` 排不进计划。

**量到（桩模型，规则层，不是真模型行为）：** 凌晨 4 点起跑一天，桩模型照职责排计划、照提示选。
05:00 全员在家睡；10:00–18:00 **23–25 人在外**，分布在咖啡馆 5–6、花园 4–5、健身房 4、公告板 4、商店 3–4、学堂 2–4；
**25/25 人有计划，75 段完成 64 段**（未完成的是桩补的 21:00 回家段）。

**顺带量出并修掉的两处：** 人一聚起来，react 一天被问 **3,170 次**（一对人 223 次）——被拒的回答没消费待处理相遇，
90 秒寿命里每拍重问；修后回答合法时一天 **315 次 / 66 对 / 单对最多 18**。另外计划回答碰上版本过期时，
会被误判成"今天不排"（阿满一整天没计划），现在过期只重问。

**没做到的：** 真模型会不会照着自己的计划过、会不会偏离得有意思，没有跑——需要 token 授权。
按桩模型的节奏，一天约 1,500 次决策 + 25 次日计划 + 数百次 react，用真模型前应先估一次预算。
live 世界在用户 12:18 打开时已经种下职责、23 人排出了日计划（真模型），中午的问题见上一节。

## 线上两个 bug：隔空对话、没人出门（2026-09-14 下午）

**没人出门是一个死循环，不是居民不想动。** 世界离线约 7 小时后追帧，所有人的睡眠计划在同一 tick 结束，
全镇同时收到 `change_work`。模型答 `none` 算“采用”，`Occasions.apply` 在内存里删了待问事件，
但 `w.revision` 没动，而 `JdbcWorldStore` 只在 revision 变了时写库，所以删除从没落盘，下一 tick 同一批人又被问。
线上 17 分钟 **1,083 次 consider、336 万 input token**，6 个并发位被 6 个人占满，其余 19 人一次决策都没拿到。
测试用的 store 握着活对象，所以一直没暴露。用卡住那一刻的真实快照 + 桩模型回放 20 模拟分钟对照：
修复前 consider 2,956 / 决策 0 / 出门 0；修复后 consider 12 / 决策 85 / 25 人都拿到决策。
修法：结果和失败两个写回 lambda 一律 bump revision；回答过的待问事件不管采用与否都消费掉。
同时 `pendingOccasions`（12）、`askedOccasions`（32）、`pendingEncounters`（12）按人口放宽——
之前 21 人被记“已问”、只剩 12 条问题，另外 9 人当天再也不会被问；`objects` 超 20 时不再先挤掉公告板/花圃/长桌这些固定物件。

**隔空对话。** 走路的人 `place` 整程都是 `street`，而整条街只有一个房间 `street-outdoors`，
所以青叔、阿禾各自刚出家门（两家相距 737px）就在同一 tick 被判“面对面”。现在行人记下 `travelFrom`，
用已走/剩余路程和 `TownDistances` 的三角不等式求两人距离下界，下界超过 96px 就不算同处；站在街上的人照旧。

回归：`ConsiderAnswerPersistsTest`（模拟按 revision 写库的 store，修前 40 次轮询问 38 次）、
`StreetEncounterDistanceTest`、`OccasionPopulationTest`；`OccasionsTest` 里钉死的 12 改为按人口。
companion 包 clean 跑 **538 tests 全绿**。**没做的：** 真模型下居民会不会选择出门没有重新量；
前端气泡仍按各自精灵位置画、不检查同处（后端不再产生这种对话后不会出现，但前端本身没有兜底）；
`events`(80)、`serviceRequests`(60)、`conversations`(24) 的 FIFO 上限在 25 人下余量变小，未改。

## 第二版地图与系统收尾（2026-09-14，工程验收完成）

本轮按 [01 的第二版](docs/01-requirements.md#第二版二十五个人的镇子)继续完成 25 人小镇。
对标 Smallville 的空间与生活复杂度：六处公共地点、分房间的住所、可操作且有后果的物件、
有限知识和真实相遇；不照搬 2023 年模型所需的逐层多次定位、小时级反复规划与非法输出静默替换。
规则仍只拥有物理事实和合法性，居民的选择、解释与社会规范仍由各自模型产生。

**开始时的现状。** 25 人种子、家庭门锁、借赠、两层记忆和按人并发已经存在，
但这些地基不等于地图和生活系统已经完整。此次继续检查新增公共地点的动作落地、老存档扩建、
前后端地点与行走距离一致、25 人地图导航与外观，以及调度和验收工具在扩人口后的可靠性。
本段之后的旧测量保留其原始人口、模型与时间范围，不能当作本次第二版的结果。

**基线已重新量过。** 本机默认 Node 26 的原生 `localStorage` 会干扰 jsdom，
所以前端固定用本机已有的 Node 22 验收：**417 个测试全绿**。后端在可访问
Docker/本机端口的权限下跑 `clean test`：**606 tests，0 failure，0 error，9 skipped**。
这两个数是本轮改动前的有效基线，后面只能与它们比。

**已实现。** 世界侧已把公共建筑、房间、可争用位置和物件动作接成
四层候选；它自身的聚焦检查已报 **23 个地点 / 253 组距离与前端常量一致**，
已通过下述最终全量和两天规则验收。心智编排已修正全类型过期结果、跨日预算退回、
模型未作答时保持沉默，不再由规则代为问候，并加了 25 人公平轮转与饱和时的认知车道；
接入四层 `decisionOptions` 后主代码、schema、应用复核和聚焦回归已经接通。

**地图可视化已有一次人眼检查。** 五张新角色 sprite 已产出；主代理在 Chrome 里看过
小镇全图和合租房，之后又在真实 HTTP 响应上看过 desktop/mobile 四个场景。
合租房的门洞与共享起居室、浴室、厨房现在都能分辨；独立 browser E2E **4/4 通过**。
共享开发栈的 backend/frontend 容器仍在运行，`/actuator/health` 返回 `UP`，
`http://localhost:5173/town` 返回 200；本轮没有为验收重启它、改配置或登录写真实世界。

**结构验收已经能重跑。** 心智侧不再把 `action × place × room × target` 四张表交给模型自由拼接；
每个 `choiceId` 对应一条规则先算好的完整合法候选，应用时再对同一快照复核。
房间内的人与物也按 `roomId` 隔开；自己家、职业场所、亲眼进入的房间逐步记入已知，
未知公共建筑只暴露可进入的入口，不先把里面的位置和物件透给每个人。
三名代表居民一次决策的实测展开量为 **62–98 个完整候选**，context **9,345–14,171 字符**，
instruction（含 context）**13,235–18,103 字符**，schema **681–897 字符**；schema 只重复短 id，没有再展开一份四层矩阵。

**25 人 × 2 天的纯规则验收已通过。** 最终冻结源码又独立重跑 `AcceleratedTownRunnerIT`，
产物在本机 `/tmp/town-v2-accepted-rules`。它跑了 **2,880 ticks**，
终态是 25 NPC + avatar、23 locations、66 rooms、108 positions、9 objects、16 doors，
26 个 state、所有 position/object 的 `roomId` 都能解析到实际房间；435 条记忆、333 条累计事件，
**0 模型调用 / 0 token**。这只证明世界结构能稳定跑过两天，不是社会涌现证据。

**当前分项回归。** companion domain **348 个测试全绿**；最后将 application/adapters/
runner、本机 HTTP contract 与相关 domain 合在同一个 Maven 进程里重跑：**154 tests，0 failure/error**。
真实 TCP + auth + MySQL 31 迁移的 `CompanionV2HttpIT` 独立跑了两次，join / advance / reload /
idempotent join 与四层断言都通过。前端在 Node 22、`maxWorkers=2` 下 **71 files / 429 tests 全绿**；
默认并发时唯一一个 5s 寻路超时在限制 worker 后消失，是与后端并行验收时的 CPU 竞争，没有业务断言失败。

**人设预置已做真正的独立盲扫。** 执行者只读了导出的 25 人原始人格文本，没读仓库和现有结论；
结果冻结在 [docs/05 的「固定人设的独立盲扫冻结」](docs/05-notes.md#固定人设的独立盲扫冻结2026-09-14)。
它列出的是我们预先写进人设的社会预期，后续验收命中这份清单的规矩一律不算涌现。

**最终全量已通过。** 所有代理停止编译后，单一 Maven 进程跑 `clean test`：
**119 suites / 641 tests / 0 failures / 0 errors / 9 skipped**。日志中没有 `AssertionError` 或 `NoClassDefFoundError`。
Surefire 在 `System.exit(0)` 30 秒后结束一个仍有 Hikari shutdown 消息的残留 JVM，这和本轮前的基线一样，
本次 Maven 进程实际退出码是 0。

**真模型短探针已执行（2026-09-14）。** 在用户对本次向官方 DashScope/Qwen 发送纯虚构、隔离快照的明确授权下，
从 `/tmp/town-v2-accepted-rules/world-snapshot.json` 继续跑到
`/tmp/town-v2-choice-probe`；Maven exit 0，`AcceleratedTownRunnerIT` 1 test 通过。配置为 Qwen
`qwen3.7-flash-2026-07-15`、4 并发、请求 0.02 天；实际模拟从 `2026-01-03T00:00:00Z` 到
`00:32:08Z`，241 ticks、wall time 23.28 秒。导出记录 **24 次逻辑模型调用**，返回 usage 合计
**121,096 input / 2,040 output tokens**；其中 dayplan 8、turn 7、react 5、summary 2、explain 2，
20 次应用、4 次拒绝（3 条 `react/greet`、1 条 `summary`）；导出的 outcome 行没有拒绝原因，
不能把它们归因于某一条复核规则。623 条 timeline entries（11 diary、99 event、476 memory、7 dialogue），
规范检测为 0 candidates / 0 beliefs；这是这段 32 分钟短跑的实际结果，不是长期社会结论。

**这次探针没有验证 `choiceId`。** 导出的 `model-calls.json` 有 **0 条 `decision`**，所以真实网关只贯通了上述
五类结构化调用；不能据此声称决策候选或 `choiceId` 的端到端路径已经由真模型验证。

**调用上限的记录口径有漏洞，且本次无法回溯核实。** runner 的 24 是 `RecordingMind` 开始的**逻辑调用**数；
`QwenHttpProvider.generateStructured()` 在首答不是合法 JSON 时会额外发一次 JSON repair HTTP 请求，
该内部请求不单独进入这份计数或导出。因此本次实际 wire 请求数不能从产物核实，理论上可高于 24；
121,096 / 2,040 也仅是最终服务商响应回报的 usage，不能作为所有 wire 请求的完整账本。
此前自动审批两次拒绝过同类操作：即使快照来自纯源码生成的 InMemory 合成小镇，向
`dashscope.aliyuncs.com` 发送虚构测试数据仍需用户具体授权；本次是在该授权后执行，并没有绕过它。
input token 阈值同样只能在服务商回报后阻止下一次逻辑调用，不是调用前 token 硬限。

**后续保障已修复（不回溯本次）。** `WireRequestBudget` 现在在每次 `HttpClient.send` 前原子占用额度，
JSON repair 也计入，4 并发下不会越过 wire 上限；成功 repair 的 usage 会把首答和 repair 相加。
生产默认仍使用无界 no-op budget，不保存调用记录或引入生命周期限制；probe 才显式注入有限 budget。
后续产物的 manifest 会分列 logical/wire 调用数和预算，并导出 `model-wire-requests.json`（ordinal/kind）；
runner 结束前 seal budget，避免迟到 repair 漏记。Sol 的本地 127.0.0.1 stub/离线验证为
`QwenContractTest` 13 + `AcceleratedTownRunnerBudgetTest` 3 + `AcceleratedTownRunnerAutonomyIT` 9，
共 **25 tests / 0 failure / 0 error / 0 skipped**；随后独立验收
`QwenContractTest` + `AcceleratedTownRunnerBudgetTest` + `ResidentMindJsonContractTest` 为
**20 tests / 0 failure / 0 error / 0 skipped**（日志 `/tmp/town-v2-wire-budget-acceptance.log`）。
这项修复没有新增任何外部模型调用，不能回溯本次 wire 数未知，也不改变本次 `choiceId` 未经真模型覆盖的事实。
第一次合并后全仓运行不作数：它在 Surefire 运行时又有另一个 Maven 进程重编同一份
`target/classes`，产生 `NoClassDefFoundError`和连带 HTTP 500。那一轮同时暴露的真回归（旧提示词断言、
测试物件缺 `roomId`、六人时代的测试隔离、帮工不知道吧台）已在上面的 154 项联合回归里修复并复验；
后续已在完全无竞争的单一进程里重跑 `clean test`，最终数字以上面 641 项那次为准。

**工程收尾不等于长期社会验收。** 记忆的三组对比（原始流 / 纯自述文档 / 文档+工作集）、
同种子的真模型双跑与纯规则减法、跨多天的信念与规范形成仍然没有验。
它们需要新的真实 token 预算和明确授权，不能用本次 24 条逻辑调用的短探针、单元、HTTP、浏览器或纯规则通过来替代。
没有把纯规则跑或本次短探针当作社会涌现证据；真实模型双跑与记忆三组对比也不能由编译和单元测试代替。

## 小镇活起来了

同一套加速跑法（8 秒一 tick，13.3:1，一个模拟日约半小时，qwen3.8-flash）：

| | 起点 | 中途 | **现在** |
|---|---|---|---|
| 对话段数 | 0 | 17 | **22** |
| 对话回合 | 0 | 28 | **77** |
| 服务请求 | 0 | 0 | **27（25 杯被喝掉）** |
| 人格漂移 | 全 false | 全 false | **六人全 true** |
| 盲测可抽台词 | 0 | 12 条 / 4 人 | **20 条 / 5 人** |
| 记忆 | 64 | 167 | **388** |
| 独处比例 | 82.5% | 80.0% | 72.9% |

**决策触发分布**（比调用总数有用得多）：`plan_ended` 68% → **25%**，
`encounter` 0 → **45%**。以前居民是个计时器，现在驱动他们的主要是"碰到人了"。

盲测池里的原话：

> 「别，死结拆不开。你手稳的话帮我绕个活扣就行。」
> 「顺什么呀，颜色调来调去都灰扑扑的。你理你的线，我盯着这块色块发呆，咱俩谁也别嫌弃谁。」
> 「周野，正好。我打算去咖啡馆靠窗那个老位置复习，家里太安静反而容易走神。」

最后一句是他自己想起了"靠窗那个老位置"——从记忆里长出来的偏好，没有任何规则替他数过。

## 这一轮建成的

**先做，事后再解释。** 规则是下意识、模型是解释。规则替某人做了一件事就记下来
（`note` 只写旁观者看得见的，绝不写动机），攒够几条由模型给出他自己的说法。
说法写成 `reflection`（事后的建构，不是观察），并覆盖他的当前想法——从此就是
他关于自己知道的东西。实测阿禾（超我是"不该让人看出我在意"）对自己反复擦桌子的
说法是「反正闲着也是闲着，看着心里踏实点」，不是"我心里不舒服"。**他会读回这条并且相信它。**

**六个人的人设文本变成了行为。** `actingSelf` 本来就是下意识动作的说明书。
现在每人有一条微习惯（不改位置的小动作）和一条场所习惯（会真的把他带去某个地方），
由规则确定性触发、不问模型、不推进任何世界状态。

**相遇由规则制造、由模型回答。** 规则把两个人放到面对面，模型答 greet / join / none。
"看见熟人而没有走过去"第一次成为一个能被表达的答案。

**记忆分了层。** raw → `reflection` → `belief`，检索改成乘法打分，
旧结论被新结论取代时只标记不删除——"他曾经这么想过"永远查得到。

**人格会因经历改变。** 四维各有正反驱动，每次 1 分、4 小时冷却、终身上限 ±12。
一天看不出来，一周能看出来。有测试断言人格永远不做门。

**咖啡馆真的在运转。** 从一天卖出零杯到 27 单 25 杯。

**前端：** 纸质边框、夹在书里的人物志（书签翻出）、日记按真实日期翻页、Phaser 视差层。

## 第二版地基（2026-09-13 开工）

计划在 [01](docs/01-requirements.md) 的「第二版：二十五个人的镇子」，节奏是**地基先行、人口暂时不动**。
这一段只记已经落地并且全套测试过了的，**没有量过的一律写在下面「还没做到的」里**。

**居民可以同时想事情了。** `ResidentDirector` 以前是一个世界同时只有一次模型调用在飞
（`inFlight.add(userId)`）。并发的单位现在是**人**：一个人同时只有一次调用（`thinking` 声明，
在 `reserved()` 里拿、在 `run()` 的 finally 里还，每条路径都还），几个人可以同时想。
写世界仍然串行——每次写都在一个短短的 `store.update` 事务里，和改之前一模一样。
冲突还是老办法：`r.revision` 过期就丢掉这次结果，没有引入任何新的锁。

顺手拆掉的一处硬假设：`applyDecision` 里的 `w.modelSequence != work.sequence()`。
那个计数器**全镇每次预约都加一**，所以并行之后它会否掉几乎每一个决定。真正在做并发控制的
一直是各人自己的 `r.revision` 和 `w.intentRevision`，90 秒的过期检查留着。

**上面那一版是"一次估出几个、一次全撒出去"，被换成了链式。** 估算必须把"冷却期到了"的人全算上，
而一个闲着的镇子那就是所有人——于是每一次轮询都会开 6 个 `select ... for update` 事务，
只为了让 `reserve()` 一个一个告诉它没事可做。现在 `consider()` 只发一个，
**真的预约到活儿的那个 worker 才发下一个**。闲镇子的开销和改之前完全一样（一次探针），
忙的时候几毫秒就爬满——对一次 4 秒的调用来说不算什么。

**改主意这件事以前活不过一次重启。** `FileMemoryStore` 从来没把 `supersedesKey` / `superseded`
写进记忆文件头，读回来时用的是不带这两个字段的构造器。同一个进程里有 `cache` 握着活对象看不出来；
一旦冷启动从盘上读，**每一次"他后来不这么想了"都被抹平成"他一直就是这么想的"**。
而文件是权威（[02](docs/02-modules.md)）。补了两行文件头，旧文件照样读得回来（没有这两行 = 没改过主意）。

**动作审计在并行下会报错数。** 加速跑用"我刚追加的最后一行"把 applied/rejected 认回到具体调用上，
这只在单飞时成立。两个 worker 交错就会张冠李戴，**而且不会抛异常，只会给出一个错的数字**。
`OutcomeListener` 现在带 `residentId`——它是真正的身份，因为一个人同时只有一次调用在飞。

**测试：529 个全绿**（基线 524 个全绿，新增的 5 个是 3 个并发测试 + 2 个 supersession 落盘测试）。
有 6 个老测试改了：它们钉的是"一次 consider 等于一次调用"，而自己问的其实是别的事
（用量记账、outcome 回调、过期回复被丢弃、打烊、缺能力不算失败）。这 6 个钉到 `parallelism=1`，
让它们继续问自己那一问，并发另有自己的测试。

## 第二版：人口、记忆、世界都上了（2026-09-13）

**记忆改成两层。** `Context.memories` 以前是一次按处境排序的 `retrieve()`，现在是
**自述文档**（先放还活着的信念，不够就用 seed 身世补到 12 条）+ **小工作集**
（这个人自己 `lastAskedAt` 之后的原始记忆，10 条封顶）。`CompanionRecall.retrieve` 退出决策路径。
**一次性的 `reflection` 从此永远进不了决策提示词**——不是靠份额上限压着，是结构上进不去。
量过的病根就是它：召回里 51–66% 是居民自己以前写的句子，而且换成"按处境查询"也没解决，
因为在它底下**新鲜度和层级永远一致地指向同一批东西**：一次决策写一条 reflection，
所以它永远是最新的那条。

**`lastAskedAt` 是原来缺的那个字段。** `lastDecisionRequestedAt` 只在决策时盖章、
`lastReflectionAt` 只在反思落地时动、`lastSocialAt` 只在对话结束时动——**没有任何一个记录"这个人被问过"**。
现在在 `reserved()` 这一个所有调用类型都要过的收口处盖章，工作集的宽度就是
"距离上一次真有理由问他，过了多久"，而不是一个时长常数。

**但它一开始把对话吃掉了。** 每一轮对白都写两条原始记忆（`appendSpeech`），而每一轮也都盖章，
于是**一段对话结束时，关于这段对话的每一条记忆都落在水位线之前被滤掉**，
而总结那条是 `reflection`，新的自述文档也不收。结果是：居民和人聊完一整场，
接下来决定做什么时，提示词里一点痕迹都没有。这不是概率问题，是每次都会发生。
所以 `turn`/`summary` 现在不盖章——对话看的是转录本，不该消费工作集。

**二十五个人搬进来了。** 十九个新居民（人设、职业、住所、初始关系全部冻结在
`ResidentPersonas` 源码里），老六个一个字没动。**初始关系按住所／职业聚类**：
密度 60/300 = **0.20**（论文起点 0.167），而不是原来"每对双向 40"的 1.0。
新人**默认谁也不认识**——`newcomer` 那句"和所有人互相 40"对十九个人关掉了，
不然扩到 25 人就是 300 对全部相识，不对称率天生被稀释成 0。
`HOME_SHARED_WITH` 改成从 `households()` 推出来，否则六个新合租的
`homeOf` 会指向一间没盖的屋子——而且是静默指错。

**改测试时的原则：改数字，不改断言在说的那件事。** 一共只有 5 个测试受影响。
`hasSize(6)` 说的是"纯规则的一个下午不会凭空多出人来"，改成钉当次种出来的花名册；
离线补算的 `≤16` 是按六个人校准的界，改成随人口缩放（**四十天不在仍然不能补出四十天**）；
共同行动那两个指标里真正承重的是 `chosenTotal==0`（巧合不许进头条），那条原样没动，
变的只是"顺带被报出来"的计数——改成断言**被测的那一对**在不在，而不是全镇总数。

**新增 `TwentyFiveResidentsTest`**，钉的是形状不是数量：合租的人回的是室友那间真实存在的屋子
且各有各的床（"四个人一张床"那个老 bug 故意做一遍长得一模一样）、开局图是稀疏且有不对称的、
十九个人的人设文本从老入口读得到。

## 第二版收尾：进别人家 + 合住争用物件（2026-09-13）

世界树那一批当时诚实地把这两件事标成"没做"——不是忘了，是主动砍掉。这一轮把它们补上。

**进别人家由所有权和门决定（docs/01 第二版「世界」）。** 每户新增一扇门（`DoorService.homeDoor`），
**默认锁着**——和咖啡馆的门相反，咖啡馆默认开门迎客，家不是。三条路径进得去：住在这儿、
自己锁的自己能开、**或者**有人当面请过你（`invite_home`/`visit_home`，一次性通行证，三小时有效）。
这是真正的 OR，不是 AND：锁着也挡不住一张有效的邀请，邀请也不需要门恰好开着。
一个人**永远**进得去自己家，不管谁锁的——这正是第一版那句"home doors were deliberately left out：
锁住用户自己的化身是产品事故"想避免的，现在门加回来了，那条保护也原样留着。

**借与赠已经在提示词里，这两个动作差点重蹈覆辙——一个字都没解释就会是下一个 `celebrate`。**
补了两行指导，`targetId` 填人不填物，和 lend/gift 的"填物不填人"正好相反，专门写了一句提醒。

**锁不锁家门走 Occasions，不进普通菜单。** `lock_door`（咖啡馆）470 次摆上菜单选中 0 次的教训
是这个项目的月经贴——一个动作要么永远在菜单上、要么挂在自己的时机上单独问，没有第三种。
`lock_home` 完全照抄同一个配方：最后一个在家、门还开着、正准备走，才问一次；`OccasionMenuExclusionTest`
（跑一整个模拟日、收集每一次菜单）会在它混进普通菜单的瞬间让整个构建变红。**没有做"开门"这个单独动作**
——"不锁"本身就是把门留给任何人，这正是 OR 的另一半；真想单独留给一个人进，用 invite_home。

**来客到门口，主人有且只有一次反应的机会。** 复用 `PendingEncounter`/`applyReaction`，不是新写一套
"有人敲门"机制——陌生人（或很久没见的人）忽然出现在自己家客厅，正是这套机制本来就该接住的那种时刻。
故意只问主人不问客人：客人几分钟前已经用 `visit_home` 表过态了，再问一遍等于逼他重新决定一件他刚决定过的事。
家没有并入 `PUBLIC_PLACES`——合租的人不需要每次同处一室都被迫"重新认识"一次。

**合住争用物件：一口锅，真的会被抢。** `TownPlaces.ensureCommonRoom` 现在真的放一个 stove
（容量 1，谁也不独占），只在**真正合住**的家庭才有——独居/两人搭伙的老人没有，因为自己家的厨房
不构成"两个人会不会同时想要"。新动作 `cook`，narrow 到"这户人家真有炉子才给"，不会重蹈
lock_door 的覆辙。

**这里踩到了一个更深的坑，值得记下来。** 第一版实现让 `cook` 走和床/工位/工具台完全一样的通用
`preferredKind`+`TownPlaces.claim()`。结果两个室友都选 `cook`，第二个人**没有等**，而是被
"能站的地方都能去"那条通用降级规则**悄悄塞去了自己的书桌**——决定看起来成功了（activity="cook"），
物理位置却是错的。原因是这条通用规则对"床/窗边座位/花圃"永远安全（这些都归属单人，
`claim()` 的"认领自己名下的"分支直接命中，永远碰不到降级逻辑），对"商店工具台"也安全
（那栋楼里只有这一个位置，没有别的可退而求其次）——**唯独家，因为室友自己的床和书桌就在原地空着**，
是全镇唯一一处两个保护都不生效的地方。加了 `TownPlaces.claimExact`——只认领这一个位置，
绝不降级替换，其余每一处争用物件原样不动。补了一条直接钉在物理占用状态上的回归测试，
不只测活动标签。

**测试：606 个全绿**（会话开始基线 524 → 上一轮 585 → 这一轮 606，一次干净跑，机器空闲，
`mvnw` 退出码 0）。这一轮改测试改了一处，但不是放松断言：`perResidentThrottleLetsA...`
钉的是"节流是按人不按全镇"，验证方式是"两次单独调用各决一人"——这个验证方式是单飞时代写的，
并行链式派发下第一次调用就可能顺带把两个人都决完，链子本身就是更强的证据，只是废了这条测试
原来的两次调用结构。钉到 `parallelism=1`，断言不变。

**第二版到这里，代码层面的清单空了。** 还欠的只剩要花 token 的实测：记忆新形状的三组对比、
跨三小时的信念对照跑、25 人 × 2 天的验收跑（文档自己估的量级是约 3300 万输入 token）——
这些不是我能替你按下去的开关。

## 还没做到的

**信念改不回习惯。** 规则侧通了（`belief` 的 supersedesKey 命中 `habit:<id>` 时拉长冷却），
但**模型不知道这个约定**，所以永远不会自己产生这样一条信念。
这是"下意识 → 解释 → 记忆 → 信念 → 改变下一次下意识"这个环唯一缺的一段。

**相遇的重问还是按时间（30 分钟），不是按变化。** 那个数字是拍的，不该由我们定。
应该改成"上次看到他时的样子"变了才重问。

**三个人的微习惯一天一次都没触发**（门开得太窄），所以事后解释一天只被问 5 次。

**项目做不完**，所以"因为做成一件事而改变"这条驱动一天触发 0 次。
不是没接线，是没有人去参与别人的事。

**看不到缓存命中。** 输入是输出的 33 倍，省钱的杠杆全在输入上，但 `usage` 里没有
cachedTokens，等于在盲飞。

### 并发量出来了（2026-09-13，同种子对照跑）

同一个 `worldId`、同一段 32 分钟仿真、同一个模型（qwen3.7-flash），**只改 `COMPANION_RUN_PARALLELISM`**
（=1 精确复现旧的单飞行为）。为此给加速跑加了这个环境变量，并写进 `manifest.json`——
没有这个旋钮就没有对照，只有一次改完之后的数。

| | 单飞（=1） | 并行（=6） |
|---|---|---|
| 模型调用 | 23 | **39** |
| 过期被丢弃 | 2 / 23（8.7%） | **3 / 39（7.7%）** |
| `consider`（时机） | **0** | **4** |
| 原始记忆 | 16 | 28 |
| 世界事件 | 37 | 60 |
| 真实耗时 | 56.8s | 74.6s |
| 输入 token | 62K | 110K |

**最要紧的一个数是过期率没有涨**（8.7% → 7.7%）。并行的整个安全性押在"冲突就丢掉重来"上，
如果丢弃率翻上去，那省下来的调用就是白花的。它没有。

**第二个数是 `consider` 从 0 变成 4。** 时机（开关店这类"只有此刻才成立"的问句）在单飞下
**一次都没轮到过**——它排在对话、相遇之后，而前面的队永远不空。机制早就建好了，
一直在被饿死，而我们从这个方向上看不出来：日志里它不报错，只是不出现。

**一个副作用要记下来：分布变歪了。** 阿禾（店主）拿走 39 次里的 10 次决策（26%），
对照组里是 3/23（13%），别人都是 2–4 次。原因大概是六个人配六条并行槽：
别人都在"想"的时候，唯一空着的人就被反复问，只剩每人 12 秒的冷却在刹车。
[05](docs/05-notes.md) 记过一次同类的病（一个人拿走 300 次里的 123 次）。
**二十五个人时候大概率会自己好**（空闲候选一直够多），但这是个猜测，扩人口那一跑要专门看这个数。

**信念仍然是 0，但这一跑证伪不了任何事。** `needsReflection` 的门槛是三小时，
而这一跑只有 32 分钟——两边 `reflect` 都是 0 次。所以"并发能不能自己把信念解饿"
**这条预言仍然没被检验**，既没证实也没证伪。要答它得跑够长的一段。
（两边 `reflection` 从 2 涨到 8，那是 `applyDecision` 写决策理由，不是反思，别拿它当数。）

### 第二版四条并行车道（2026-09-13）

四个 sonnet agent 各占一条互不相交的文件线，我做整合与验收。**先说一个我自己踩的坑：**
harness 自带的 worktree 隔离把四个 agent 全部放在了 `4d1098e`（9 月 11 日）——
连 `2f719f4「第二版计划落盘」`都没有，也就是它们正在读一份没有第二版章节的 docs。
这正是 [05](docs/05-notes.md) 已经记过的那条。全部停掉，按 05 自己给的解法重开：
自己 `git worktree add` 到 master，再把未提交的改动同步进去，核对过四个工作区都拿到了
第二版文档和这一轮的代码，才重新派活。**没有动 git 历史。**

**四层世界树。** `Location → Room → Position/WorldObject` 接在原来那对下面，
`Position.place` 原样保留——所以 `ResidentSimulation` 里约九十处 `"cafe".equals(place)`
和整个前端一行没改。六栋公共建筑（学院、健身房、公告板广场、商店）只进 `w.locations`，
静态目录 `TownPlaces.places()` 不动，于是 `TownPlacesTest`/`TownDistancesTest` 零改动。
旧存档靠 `seed()` 自愈，没有迁移脚本。商店的工作台是真的占用互斥（容量 1，第二个人真的要等）。
借与赠是三个新动作，规则只记旁观者看得见的：谁把什么借给了谁、几点、还没还——**没有 why**。

**借这件事差点白做。** 三个动作进了 `DECISION_ACTIONS`，但提示词里**一个字都没有**——
这正是 `celebrate` 摆上桌 1658 次选中 0 的形状。补了两行说明，并且补了一条测试钉住
**提示词里不许出现"欠 / 人情 / 应该还 / 回报 / 感激"**：docs/01 把这件事的量法定成
「有没有人写下了一笔规则从没告诉过他的债」，**提示词一旦提到"欠"，后面谁写"我欠他一次"
都只是在复述我们**，这个发现就在开跑之前就死了。

**reflect 提示词从九比零改成三比三。** 原来九句往"别写"推、零句列出"可以写"的那一侧，
而它是唯一一个没有 `PromptBalanceTest` 的选择型提问。现在两条路并列，两侧护栏都在：
「没看出来就别硬凑一个」和「看出来了也别因为"这么快就下结论"而不写」。
**要留意的一处**：旧文里 「大多数时候翻完不会有什么特别的结论」 这句基准率被删了。
删得有道理（没有反向的基准率，它本身单边），但它大概率是真的——所以后面信念数如果涨，
**只能看 信念数 ÷ reflect 调用数**，公平的样子是"离开零但远不饱和"。

**把居民自己的长期看法还给他，让他"选"而不是"起名"。** `supersedesKey` 只按字符串精确匹配，
而阿满为同一个想法写过 `weaver配合者` 和 `weaver-配合者` 两个 key，于是谁也没替换谁。
现在 reflect 会带上他自己最近 5 条未被取代的信念（key + 原话），提示词说：
还是那件事就把原 key 填回去，**"又一次印证了它"和"让你不再那么想"并列**，都不算新起一条。
调研过的四个真会修改信念的系统全都这么做，没有一个让模型凭空发明标识符。

**尺子。** `sharedBeliefKeys` 改成比归一化后的文本（`habit:fixer:x` 和 `habit:gardener:x`
现在会合并），并且把折叠过程导出成可审计的一段；`MIN_SUPPORT` 改成 `max(4, ceil(人数×4/6))`，
人数从跑出来的数据里数；新增**独立性检验**——顺着 `evidenceIds` 递归穿过 reflection/belief，
直到碰到一条非 `heard` 的原始记忆才算"自己得出的"；新增**「改了主意」**指标
（同一个归一化 key 出现两次以上而文本不同）。它自己列了三种能骗过独立性检验的方式，
其中要紧的一条：**只要证据堆里有一条沾边的 `observed`，整条信念就算独立**。

**前端。** 静默兜底改成会喊——`scenePlace()` 的 `?? 'street'`、`POSITION_SLOTS` 找不到落点、
未登记的家，现在都会在 dev 下告警一次，并有测试断言正常地点**不会**产生噪声。
四处各自硬编码的地点表收敛成 `STAGE_PLACES` 一处。`RESIDENT_ART` 从 7 个扩到 20 个。

**四条车道合完的验收：564 个测试全绿**（会话开始时基线 524 全绿）。这 40 个新增对得上账：
并发 3、supersession 落盘 2、地点 3+2、reflect 平衡 2、尺子 9、世界树 8+8、借与赠够不够得着 2、
长期看法回传 1。前端 416 全绿、lint 干净。

**素材那个"没人试过的坑"，试了，确实有。** 打开真包逐张读 PNG 头：
现成角色 20 张、发型 200、衣服 132、配饰 84、身体 9、眼睛 7——**docs/01 的数字全部准确**；
1792×1312 = 56 列 × 41 行也对，**除了身体是 1854×1312，而 1854/32 = 57.94，不是整列**。
关键问题是"不同网格"还是"只是多几帧"：逐格量过，共享的 56×41 区域里
460 个衣服有墨格子、337 个眼睛有墨格子**全部落在身体也有墨的格子上，零例外**——是后者。
agent 写的脚本把任何尺寸不一致当硬错误，读起来像谨慎，**实际是第一次跑真素材就拒绝**；
它猜的路径 `1_Bodies/` 实际是 `Bodies/`，猜的 `FRAME=(32,64)` 会把每帧切一半（1312/64=20.5）。
改完真跑通：合成出来的图是 1792×1312，和已有的 c01 同规格。

### 记忆上限改成按人算（2026-09-13）

`while(w.memories.size()>200)` 是**全世界共享**的，`WITNESS_MEMORY_SHARE` 的分母也是全局 200。
六个人平摊每人约 33 条；**二十五个人时每人只剩约 8 条**——也就是说，第二版扩人口这件事，
按原来的写法会让"关于别人的信念"严格地更难出现，正好和扩人口的目的相反。

改成每人 200 条。**上限提高不花 token**：没有任何地方把 `w.memories` 发给模型——
普通决策检索 10 条，反思浏览 20 条，承诺那一问取 8 条。提示词由检索封顶，不由仓库封顶，
所以更深的仓库只多占内存和存档体积。换来的是证据密度：200 的见闻份额约 80 条，
摊到 24 个邻居上约每对 3 条——低于这个数，"某人总是坐窗边"就和巧合分不开。
旧写法在六人时是每对约 2 条，二十五人时会远低于 1。

**但它确实改变了一件事，要单独量：**同样的 10 个位置，检索现在是在更深的池子里排序，
所以*哪些*记忆进提示词会变，尽管*多少*条不变。这不是白拿的。

534 个测试全绿，一个都没改——说明这条改动没有踩到任何现有断言。

### 信念为什么是 0：四条代码里核对过的事实（2026-09-13）

翻了两篇论文的官方实现、AI Town、Graphiti/Zep、Mem0、Letta、Concordia、CRSEC、Hypothetical Minds
之后回头查我们自己的代码。下面四条**都在本仓库里逐行确认过**，不是转述。

**一、分母一直算错了，转化率不是 1–3%，是 6–16%。** 八处代码写 `sourceType="reflection"`
（决策理由、对话小结、职业变更、当班感想……），但**只有 `applyReflection` 一条路能带
`supersedesKey`**，也就是只有它能产出信念。`ResidentReflectionTest` 已经量到实测
**每人每天 2.7 次 reflect**，结构上限 8 次（`REFLECTION_MIN_GAP_SECONDS` = 3 小时）。
所以两天六人只有约 32 次"有资格变成信念"的调用，不是 146 次。
**问题主要不在转化率，在能产信念的调用太少**——而这件事几乎不花钱。

**二、200 条记忆上限是全世界共享的，不是每人的。**
`ResidentSimulation.java:2578` 是 `while(w.memories.size()>200)`，
`WITNESS_MEMORY_SHARE` 的分母也是全局的 200。六个人平摊每人约 33 条，见闻类每对
（观察者，被观察者）只剩两三条能活下来——而"某人总是坐窗边"至少要三次以上的重复才看得出来。
**二十五个人时每人只剩约 8 条。** 也就是说：**第二版扩人口这件事，按现在的写法会让
"关于别人的信念"严格地更难出现。**这条是这次调研里最要紧的发现，而且它不在任何计划里。

**三、`reflect` 是唯一一个没有 `PromptBalanceTest` 的选择型提问，而且它九比零地劝人别写。**
数了一下提示词里的分句：「大多数时候不会有什么特别的结论」「不必每次都提炼出道理」
「也不必强求有收获」「只有当……才」「没看出反复就不要勉强编一个」「不要占用长期看法这一层」
「只有真正认定……才给它起名字」「绝大多数次都跟它无关，不要为了用它而用它」「只有当……才」
——**九句往"别写"推，零句列出"可以写"的那一侧**。对照 `promiseSettled`：两侧都列了，七条断言钉着。
`PromptBalanceTest` 有 8 个用例，`reflect` 出现 0 次。
这正是那个类的注释自己写的病：「每一个都编译干净、读起来像审慎、量出来是 0」，
而它长在了唯一一道通往信念的门上。

**四、按人检索的代码已经有两处，但从来没有一问以"别人"为主语。**
`PersonHereView` 和 `promise_settled` 的 `aboutThem` 都在按 `sourceId==对方` 或
`text.contains(对方名字)` 过滤。`Memory.sourceId` 本来就是对方。
而 `reflectionSource` 是 `retrieve(..., "", now, 20)`——**空查询**，纯按时间和重要度取一个横截面，
一半配额还留给居民自己写过的话。**提示词里问的是「某个人总是坐在某个位置」，
而给它看的材料在结构上不可能显示出关于任何一个人的重复。**
另外 `validHabitKey` 只约束 `habit:` 开头的 key，**自由 key 本来就合法**——天花板在提示词，不在校验。

### 论文那边的负面结果（省得我们去撞）

**别指望照抄 Generative Agents 的"焦点问题"那一步。** 官方仓库里那份跑了一个模拟日的
记忆导出：Isabella 120 条 insight，`subject` 全部是她自己；**只有 4 条（约 3%）是以别人为语法主语的**。
`run_reflect(persona)` 根本不接受目标参数。这个实验已经有人跑过了，对我们这个 0 没有帮助。

更有意思的是：Park 的模板文件 `insight_and_evidence_v1.txt` **自己的变量说明里写着
`!<INPUT 1>! -- target persona name or "the conversation"`，而实际调用传进去的是一个数字**
（渲染成「What 5 high-level insights…」）。主语插槽在参考实现里存在，被接到了计数上。
旁边还躺着一个**零调用**的 `convo_to_thoughts_v1.txt`：
「Summarize what X thought about Y in one short sentence.」
AI Town 也一样——schema 里声明了 `relationship` 记忆类型，全仓库再没有第二处提到它；
它甚至拿「What do you think about ${otherPlayer.name}?」当过向量检索的 query，
**问了，但从不索取答案**。DeepMind 的 Concordia 同样：按人名过滤的问题问了，`get_state()` 只存名字列表。

**三个互相独立的参考实现，同一个形状：以别人为主语的问题都存在，答案都被丢掉了。**
这块地是空的。

**真正会修改信念的四个系统（Graphiti、Mem0、Affordable Generative Agents、Hypothetical Minds）
共有一件我们没做的事：把当前那条信念放进修改它的提示词里，让模型从列表里"选"，而不是凭空"起名"。**
Graphiti 给整数下标，Mem0 给伪装成 "0".."9" 的 id，AGA 直接打印当前关系问要不要更新。
我们让模型凭空发明一个 80 字以内的代号——**没有任何一个系统这么干**。
而 Graphiti 的分工恰好就是我们的红线：模型只回 `contradicted_facts: list[int]`，
**由规则的时间戳判断决定这个否决到底生不生效**。

**CRSEC（IJCAI 2024）架构上最接近我们的目标，但按我们的判据它是一台假发现机器：**
它的提示词里直接给出了 norm 的定义和范例答案（「No smoking indoors.」这种），
而且**明令禁止 norm 里出现人名**（还有代码检查，命中就扣 4 分）——正好是我们最想要的东西。
它 10 个 agent 跑 2 天、5 次、每次 500 美元以上，**没有收敛指标、没有基线、没有消融**。

### 要盯的那个数

不是 `beliefs`，也不是 `beliefsAboutOthers`（后者一旦我们开始指定主语就会自己变好看）。
是**同一个 `supersedesKey` 出现过两次以上、而文本不同**的条数——**有人改了主意**。
它现在在每一跑里都是 0，任何规则都造不出它来。

**那条关于信念的预言还欠一次够长的跑。** [05](docs/05-notes.md) 量到"六小时 200 条原始记忆、
0 条信念"，而 `reserve()` 把 `reflect` 排在**所有调用类型的最后一位**——单飞之下它只有在全镇
无事可问时才轮得到。上面那次 32 分钟的对照跑已经证明**同一层饥饿是真的**（`consider` 从 0 到 4），
但 `reflect` 的三小时门槛没到，所以信念那一半还没验。**要一段至少跨过三小时的对照跑。**

**四层世界树的第一刀不是 `Location`/`Position`，是那个写死的 enum。**
`QwenResidentMind.java:216` 把 `"place"` 钉成 `["home","cafe","street","garden"]`——
而紧挨着的 `action` enum 已经是按 `availableActions()` 动态生成的了。
所以**第七栋楼模型根本叫不出名字**。同理 `ResidentDirector.knownPlaces()` 是一张静态的四条清单，
每个居民每次决策拿到的都一样，[01](docs/01-requirements.md) 说的「存在全知、内容部分知」没有落脚处。
另外两处会安静出错的地方：`ResidentSimulation.placeName` 的 `default -> "小街"`，
和前端 `companion-geometry.ts` 的 `scenePlace()` 兜底 `?? 'street'`——新楼会被画成街上。
（前端反而已经有 `ACADEMY_ROOM` / `GYM_ROOM` / `BOARD_AREA` 的房间几何，只是 `scenePlace:'street'`。）

**记忆新形状还缺一个"上次被问是什么时候"。** `lastDecisionRequestedAt` 只在 `decision` 那一种
调用上盖章，`lastReflectionAt` 只在反思落地时动，`lastSocialAt` 只在对话结束时动——
**没有任何一个字段记的是"这个人上一次被问"**。而工作集要按"上次被问之后发生了什么"切。

**`livelihoodPressure` 不在那道墙的名单里。** 挡内部数值进上下文的测试是**按字段名**写死的
（`ResidentPersonaContextTest` / `ResidentDirectorTest` / `ResidentMindJsonContractTest`），
而 `livelihoodPressure` 从来没被任何一条列进去。名单不是从某个白名单推出来的，是手抄的。

**（已不成立，2026-09-13 实测）** 这里原来写着"仓库本来就是红的：`AcceleratedTownRunnerAutonomyIT`
5 个 + `PersonalityTest` 1 个"。那 6 个其实在 `7142796`（5 个）和 `6552569`（最后 1 个）就清掉了，
这一页没跟上。现在在 `2f719f4` 上跑 `./mvnw -q clean test`：**524 个测试全绿**。
于是"相比动手前没有新增"这条判据从此是把利刀——红就是这一轮弄红的。

## 服务

开发环境跑在 docker compose 上。后端曾经因为**容器是配置更新之前起的、之后只被
`restart` 过**而整天起不来——`docker compose restart` 不重读 compose 配置。

## 早前版本的历史记录：第一次真模型小规模探针（2026-09-14）

这节是工程收尾前的代码、时间和预算下的历史运行；其 120 次记录与本次收尾后的 24 次逻辑调用不能直接比较。
“第一次”仅指当时版本语境下的首次探针。

没有碰共享的 docker 开发栈——用仓库自己现成的 `AcceleratedTownRunnerIT`（`COMPANION_RUN=true
COMPANION_RUN_MODEL=true COMPANION_RUN_MODEL_BUDGET=60 ./mvnw test -Dtest=AcceleratedTownRunnerIT`，
默认关闭、走真实凭据、和 Spring 应用完全分开的一次性跑法），预算硬顶在 60 次调用。
实际跑到 120 次——**用例自己的断言假设一次跑不跨日**，而这次 `days=1` 从
`2026-01-01T00:00` 跑到了 `2026-01-02T00:03`，跨了一次日历日边界，`modelCallsToday` 清零重算了一遍，
于是变成两个 60 顶格。断言失败了（`AcceleratedTownRunnerIT` 本身，不是应用代码），但产出物在断言
之前就已经写完，探针本身没受影响；这是我这次选参数选出来的边界情况，没有去改这条测试。

真实花费：120 次调用，输入 344,612 token + 输出 9,485 token，钱包时间 6 分 9 秒（其中真正等网络
的 67 个 tick，其余 10,758 个 tick 是规则时间，飞速跑过）——**新架构落地后第一次有真实数字**，可以
拿来估更大规模跑的成本量级（对比 [01](docs/01-requirements.md) 估的 25 人×2天 ≈3300 万 token，
这次 25 人×1天只有约 34 万输入 token，量级差了两个数量级，因为这次预算硬顶很紧、大部分居民一天
只轮到一两次决策，根本没攒够时间触发 reflect）。

信念产出：72 条记忆带着「反思层」的 `sourceType=reflection` 标记，其中真正走到
`ResidentSimulation.applyReflection`、能带 `supersedesKey` 的 `reflect` 调用只有 5 次——
其余的 67 条是决策理由、"一天过去了没顾上"这类别的写入点（[01](docs/01-requirements.md)583
行已经指出这 8 个写入点不能混着当分母）。这 5 次里 1 次真的落成了 `belief`：
知夏（artist）"一闲下来就回头去弄自己那件没做完的事，没跟谁说"，覆盖了 `habit:artist:own_thing`——
**还是关于自己习惯的自我描述，和改造前 run A/run B 的老毛病同一个形状**。

**诚实的结论：机制端到端跑通了（自述文档 + 工作集 + 对称提示词，真实模型下没有崩、没有 JSON
不合规），但样本太小（n=5 个 reflect 调用）说明不了产出率有没有真的变——25 人平摊 120 次调用，
没有一个人攒够跨过三小时反思间隔所需的时间和决策量。要看到"信念是否开始关于别人"这个真问题，
下一次要么把预算集中砸给 2-3 个人（像 `HomeVisitingTest` 那样把其余人晾在一边），要么真的批
[01](docs/01-requirements.md) 552 行提到的那次没跑的探针。这次只回答了"方向对不对"的最低要求：
没有跑崩，产出了一条形状眼熟但确实存在的信念。**
`scripts/dev-server.sh` 的 restart 已经改成 `up -d`。
