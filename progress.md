# 当前进展

当前分支 `master`。这一页只记两件事：**量出来的现状**，和**知道但还没做的**。
定过的原则在 [docs/04](docs/04-decisions.md)，测法和教训在 [docs/05](docs/05-notes.md)。

## 当前主线

25 人第二版的世界结构、并发、记忆两层与门锁/占用机制已经落地并通过工程验收（详见归档「第二版地图与系统收尾」）；对话出口、物品使用状态与地图重做 A–C 步也已提交（`797e78e`、`29d3f35`，2026-09-14 17:37）。真正悬而未决的不是代码，而是要花真实 token 的记忆效果验证与 25 人 × 2 天验收跑——第二版到目前为止代码层面的清单已经空了。

## 进行中

### 房间灯改成居民亲手开关（2026-09-15，未提交）

用户："目前是房间的灯自动亮起，应该是让小人到房间可以操作开关"。原来后端没有灯的状态，前端 `companion-scene.ts sync()` 按时钟猜：家 = 夜晚且有人醒着，咖啡馆 = 夜晚且营业，学堂/健身房/铺子 = **只看夜晚**。

- **后端**：新增 `LightService`；每个卧室与 `cafe-main` / `academy-reading-room` / `gym-training-room` / `shop-workroom` 各一个 `WorldObject(kind="light", id="light:<roomId>", state on/off)`，初始 off，`TownPlaces.seed` 里幂等补齐旧存档。天黑（<06:00 或 ≥19:00，与前端同阈值）进入灯灭的房间 → 先做 120 秒 `switch_light`（"摸黑找到开关"）再接原计划、灯变 on、事件 `light_on`；最后一个醒着的人离开或睡下 → 关灯、事件 `light_off`；咖啡馆打烊顺带关灯。无人在场灯永不自己变。开关建模成短活动而非可占用 `Position`（开关不稀缺，不需要排队）。**纯规则层，没有改模型提示词或动作枚举。**
- **前端**：`litRooms(objects, minutes)` = state on 且天黑；没有灯对象就不亮，不回退到旧猜法。`SceneObject` 带上 `roomId`；harness 夜景补了两个亮灯对象。
- **验证**：后端全量 677 项 0 失败 0 错误 9 跳过（含 `LightServiceTest` 8 项）；前端 vitest 73 文件 462 项全过，vue-tsc 通过。town-v2 浏览器测试与真实画面尚未看。
- **已知局限**：已经待在房间里的人跨过 19:00 时不会立刻开灯，要等他下一次 `schedule` 才触发；`switch_light` 活动与 `light_on/off` 事件会进入居民感知，行为基线变了，不和之前的实验混比。

## 最近完成

### 对话出口 + 物品使用状态 + 头顶动作 emoji（2026-09-14 下午，已提交 `797e78e`）

用户看到线上对话陷入"（翻过一页，没抬头）嗯。"式敷衍，问物品状态怎么更真实。调研斯坦福小镇后用户拍板"全部做完"：

1. **对话出口（已完成，已验收）**：新增 `ConversationLifecycle.isTokenAcknowledgement`（敷衍应答判定）与 `isNearDuplicate`（近似重复判定），命中后走和 `leave=true` 同一个 `finish()`。沿用现有两层冷却，不新增。测试新增 5 项，后端全量 664 项 0 失败。决定已写入 docs/04。
2. **物品使用状态（已完成，已验收）**：只加一个字段 `ResidentState.activitySince`；`ResidentSimulation.positionUses` 现算出"谁在用什么、用了多久、手里拿着什么"的视图，进 perspective。已知局限：被拉进对话后 activity 变成 talk、`activitySince` 重置，"看了 40 分钟书"只在搭话之前可见，靠对话出口规则兜底。
3. **头顶动作 emoji（已完成，用户看后撤回）**：前端 `actionEmoji()` 曾覆盖全部活动名显示 20px 徽标；首轮截图家家头顶都是 ☕，像全镇在喝咖啡，打回修正后用户仍反馈"所有小人都显示 emoji 状态太乱了，恢复原本的样子"——已移除，地图回到 HEAD 的样子（只在说话时显示对话气泡）。**教训**：25 人同屏时，人人头顶常驻标记就是噪音；斯坦福小镇只有 25 人且没有房间墙体，照搬不合适，以后若要只在选中或悬停时显示。
4. **物件使用画面（已完成，已验收）**：书桌看书画摊开的书、真睡着盖被子、咖啡机 tend 时冒热气；验收打回两次（被子曾像影子、睡着的人头画在墙上，后者是原有偏移 bug，顺手修了）。

**收尾**：头顶徽标移除后前端全量 72 文件 450 项全过，vue-tsc 通过，town-v2 浏览器测试 4/4；后端最近一次全量 669 项 0 失败。已提交，是新基线——这些都改变行为，不和提交前的对照实验混比；决定已写入 docs/04。

### 地图重做 M1–M2（2026-09-14，已提交 `29d3f35`）

用户选了"先地图、完全脚本生成"，对照斯坦福小镇分三步：

- **A 步（已完成，纯重构零行为变化）**：布局迁移到 `town-layout.json` + `town-layout.ts`，前端改为读布局；验收距离一个数没变。
- **B 步（已完成）**：世界 2048×1408 → 2560×1856，所有门与小巷连通逐一核对；`TownDistances.java` 253 条距离在真实寻路器上重生成，组内距离不变、跨组平均 +128px。后端相关测试 15 项全绿。
- **C 步（进行中，基本可收）**：`scripts/build-town-map.py` 从布局 + 素材生成地图瓦片与贴图；历经两轮打回修整（镜头越界、草地颜色偏冷、寻路变慢、装饰落在建筑上、灯柱错放等）逐一修正，抽查装饰落点 0 个越界。地面、公园和种植现在都由脚本生成，风格与原来的纯色画法一致。测试：后端 659 项 0 失败，前端 206/206，town-v2 浏览器测试 4/4。

下一阶段 M3 见"下一步"。

## 下一步

- **记忆三组对比探针**（原始流 / 自述文档 / 文档 + 工作集）与**25 人 × 2 天验收跑**——两者结构都已改完，只缺真实模型验证。
  - 25 人 × 2 天文档自己估的量级约 3300 万 input token，需要 token 预算授权，不是我能替自己按下去的开关。
- 真模型下居民会不会照着自己排的日计划过、会不会偏离得有意思，没有跑过。
  - 按桩模型节奏估算一天约 1,500 次决策 + 25 次日计划 + 数百次 react，用真模型前应先估一次预算。
- 真模型改完午饭/午休提示词后，中午的实际人流分布没有量过（种子里中午原是空档，改动只在桩模型上验证过结构）。
- 一盆花的反馈环：镇上除自己没有可评价的东西，第一刀只切一盆花，量收到负评之后照料有没有变。
  - 三条红线：不做数字字段、正负一样容易发生、按人方式传递。
- 说话要先走过去、就地开口算大声：设计已定（见 docs/04），代码还没动。
  - `greet` 改走过去到了再开口；新增"就地喊一声"给在场人一条 `salientPerceptions`。
- 谁来管公共的事：`beliefsAboutOthers` 恒 0，没有分歧的投票是仪式，需要先有公共的事（种花、打烊时间、苗圃管理）可定才有意义。
  - 25 人正好越过论文式"12 人以上"密度门槛。
- members（答应参与项目）与 contributors（实际动手）仍没有通用比较：一个人可以答应参与然后一根手指都不动，落空还不会成为镇上可感知的事实。
  - "天眼"预警那条线也在这里合流：它要能预警、能被不信，靠的就是同一套承诺-兑现机制。
- 地图 M3：建筑外墙和屋顶（拉近时淡出）、住宅做 3–4 种模板。
  - C 步随 `29d3f35` 已提交；地图生成说明文档是否写完未核实。

## 卡住 / 等决定

- 调用上限的记录口径有漏洞：`QwenHttpProvider` 首答不是合法 JSON 时会额外发一次 JSON repair 请求，该请求不进逻辑调用计数。
  - 真模型短探针的实际 wire 请求数无法回溯核实，理论上可高于记录的 24 次（后续保障已修复但不回溯本次：`WireRequestBudget` 现在按每次 wire 请求原子占用额度）。
- 温度 0.2 让"同种子两次要不同"这条判据没有鉴别力；要不要为验收单独调高温度没定，调高本身可疑，因为会让结果更容易通过。
- "谁最先动手"仍是写死的（纯规则对照里阿满最先动手 0 件→自我叙事"我好像总是那个配合的人"）。
  - 和相遇顺序是同一个病：环路跑通了，起点是我们放的。只在代码加了风险注释，未修。
- 相遇的重问还是按时间（30 分钟），不是按变化；那个数字是拍的，不该由我们定，应该改成"上次看到他时的样子变了才重问"。
- 项目做不完，所以"因为做成一件事而改变"这条驱动一天触发 0 次——不是没接线，是没有人去参与别人的事。
- 三个人的微习惯一天一次都没触发（门开得太窄），事后解释一天只被问 5 次。
- 信念改不回习惯：规则侧通了（`belief` 的 `supersedesKey` 命中 `habit:<id>` 会拉长冷却），但模型不知道这个约定，永远不会自己产生这样一条信念。
  - 这是"下意识 → 解释 → 记忆 → 信念 → 改变下一次下意识"这个环唯一缺的一段。
- 前端气泡仍按各自精灵位置画、不检查同处（后端不再产生这种对话，对话后不会出现，但前端本身没有兜底）。
  - `events`(80)/`serviceRequests`(60)/`conversations`(24) 的 FIFO 上限在 25 人下余量变小，未改。

## 要盯的数

- 同一 `supersedesKey` 出现两次以上而文本不同的条数（"有人改了主意"）——现在每一跑都是 0，任何规则都造不出它来。
- 六人并行时一人（店主阿禾）占决策 26%，对照组单飞时是 13%，别人都是 2–4 次。
  - 猜测二十五人时空闲候选一直够多会自愈，但这是个猜测，扩人口那一跑要专门看这个数。
- `beliefsAboutSocialRole` 这个新指标是被一个例子提示写出来的，还没在新数据上验过是否过拟合那一个例子。
- `usage` 里没有 `cachedTokens`——输入是输出的 33 倍，省钱的杠杆全在输入侧，却看不到缓存命中率，等于在盲飞。
- 独立性检验能被骗：只要证据堆里混进一条沾边的 `observed`，整条信念就会被算作独立（它自己列了三种能骗过独立性检验的方式，这条最要紧）。

## 服务

开发环境跑在 docker compose 上。后端曾经因为**容器是配置更新之前起的、之后只被
`restart` 过**而整天起不来——`docker compose restart` 不重读 compose 配置。

---

历史记录见 [docs/archive/2026-09-15/progress-history.md](docs/archive/2026-09-15/progress-history.md)。
