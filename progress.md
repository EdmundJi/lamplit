# 最近在做什么

更新：2026-09-09。

## 现在

`/town` 是一条六名居民加一个用户小人的小街，服务端保存他们持续变化的生活。这一轮的问题只有一个：**它看起来像屏保。** 我们先把这个感觉量成了数字，再动手，最后又量了一次——结果是没做到，但第一次知道了为什么。想清楚的事写在 [01](docs/01-requirements.md)，定过的决定在 [04](docs/04-decisions.md)，量出来的事实和踩过的坑在 [05](docs/05-notes.md)。

## 这一轮量出来的

改动之前，在未改动的 master 上带真模型跑满一个模拟日（512 次调用、153 万输入 token）：

| | 改之前 | 改之后 |
| --- | --- | --- |
| 对话 | **0** | **0** |
| 服务请求 | 0 | 0 |
| 人格漂移 | 全 false | 全 false |
| 独处比例 | 82.5% | 80.0% |
| 记忆总数 | 64（五人） | 77（七人） |
| 模型调用 | 512 | 439 |
| 盲测可抽台词 | **0** | **0** |

一整天没有任何两个人说过一句话，`modelConversationsEnabled` 确认是 true，他们也确实同时在场过。`invite` 被摆上桌 45 次，模型一次都没选；`observe` 占了 67%。**盲测在这种数据上根本产不出基线**——没有一句台词可抽。所以这一轮的对照组是上面那五个数，不是盲测正确率。

原因不是管道坏了。看真实提示词就明白：上下文 28 个字段全是物理的和事务性的，居民不知道自己有多久没跟人说过话，也不知道谁答应过自己什么。同时每个人的 label 都在劝退（"在窗边复习，想守住一点安静"对所有人、所有时刻都在说别打扰），而 `observe` 是一个永远合法、永远安全的空动作。

## 查出来的一层死机器

沿路发现这个小镇积了一层"看起来做了、其实没接上"的代码，而其中几项 progress 里当成果写过：

- `proactivelyServe` / `anticipatesRefill` / `repeatVisitStreak`（学到的预期）——从未运行，`anticipatesRefill` 从未被写入过
- `decide` / `decideInterrupt`（责任感派工）——只有测试在调
- `newWish`（自动生成新活动）——从未运行
- **`reflect`（反思）——从未运行。这个小镇到现在一次反思都没发生过。** 指标里那 50 次"反思"是 `applyDecision` 把模型给这次动作写的理由原样存成了 reflection 类型的记忆
- `ResidentSimulation.retrieve`——第二套检索实现，没人调用
- `MetricsExporter.learnedExpectations`——统计的是结构上不可能发生的事，永远 0

全部删掉了。删的理由不只是死代码：那条"学到的预期"是规则数到两次巧合就替店主倒水，违反 04 自己定的两条（不能把社会模式写成自动动作链、规则不按阈值替人行动）。规律应该由居民在反思里自己找。

## 这一轮做了什么

**地基**（编排者亲自做，agent 开工前）：删掉上面那层死机器；把记忆从世界存档搬到磁盘，一人一目录、一个事实一个文件、文件为准，SQLite 只存元数据，注销连目录带索引一起删、导出带上记忆、容器挂命名卷；把"谁住在这里"从 817 行的循环里拆成 `ResidentSeed`。核心循环那 350 行没有拆——`step`/`complete`/`schedule`/`applyDecision` 是一个真正的整体，按"日程"和"离场"切开只会造出一个为了并行而发明的接缝。

**三个并行 agent**：每居民独立决策节流；走路按真实距离（3–20 秒）；`away` 真的从地图消失并带回一条只有本人有的记忆；`join` 拼桌；日程分解成一天三四段；触发源加时间锚点和低频走神；**每次决策记录被什么触发**。周野住进花园下面那栋新房（通往它的巷子也补了），阿满和知夏合租——各自的床和书桌、同一个 location。六人都有本我／超我／自我三层文本，过路人只被看见和记住、永远不调模型，小川多了一条只有他知道的异常记忆。前端：倒咖啡看得见了，夜里窗户按"有人在且没睡"亮灯，街上有鸽子和落叶。

## 没做到，以及为什么

改完之后仍然是 0 对话。但这次有诊断：

**决策触发原因分布：`plan_ended` 68%，`encounter` 0。** 这个分布本身就是判据——九成靠计时器触发，说明世界对居民没有影响，他还是个计时器。相遇一整天一次都没触发。

两个具体故障已定位：

- **日程从未被调用。** `ResidentMind` 上新增的 `planDay` 是一个会抛异常的 `default` 方法，于是 `RoutingResidentMind` 和加速跑的 `RecordingMind` 两处都静默弃权，两处都编译干净。**同一个错误在一轮里犯了两次。**
- **相遇只覆盖"走进一个已经有人的地方"**，而 80% 的时间大家各自在家或独处，这个条件几乎不成立。街上擦肩没做，因为编排者给 agent 的口径自相矛盾（"街上照面"与"走路中不触发"不可能同时成立）——agent 选择停下来说而不是猜，这是对的。

另有一个异常没查：`fixer` 一个人拿走 300 次决策里的 123 次（41%），其他人 27–48。每居民节流现在是不公平的。

## 接下来

先修这三处再测一次，不加码：两处转发 `planDay`、把相遇扩到街上擦肩、查 fixer 的决策饥饿。

人格三层接进模型上下文、以及真正的反思与信念，**押后**。在小镇还不说话的时候做反思，它没有素材可反思，会重复这一轮的错误——机制建好了，跑出来还是 0。

更长期的故事感仍然要靠一起开着学习、连续回来几天以及第 1/3/7 天盲测判断，而盲测在有台词之前无法开始。自然辞职、接手、错误预期、关系恶化与人格复利是否会真实发生，仍未证明。

用户侧的左清单右小镇工作台、五维能力与任务标题安全边界继续排在自主生活体验之后。**建筑师**和 Tiled 地图地基排在下一轮：等居民真的挤不下，扩张才带着世界内部的理由。

验收不看测试是否全绿，看盲测——从时间线里抽二十句去掉名字，猜是谁说的。猜不出来就是没做到。然后一起开着学一个下午，再连续回来几天。

[想做什么](docs/01-requirements.md) · [代码思路](docs/02-modules.md) · [以后想试试](docs/03-ideas.md) · [定过的事](docs/04-decisions.md) · [实测与环境](docs/05-notes.md)
