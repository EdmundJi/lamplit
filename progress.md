# 最近在做什么

更新：2026-09-08。

## 现在

`/town` 是一条陪伴式小街：四名居民、一个用户小人、服务端存档与持续生活。这一轮把注意力从"小镇能不能自己活下去"转到"它能不能让我看见自己"。想清楚的事都写进了 [01](docs/01-requirements.md)，定过的决定列在 [04](docs/04-decisions.md)，量出来的事实和踩过的坑在 [05](docs/05-notes.md)。

**已经做完的：**

美术管线的裁切错误查到了根因并修好——生成脚本选错了源角色表的一行，五位居民三个动作共 210 帧全是漂浮的半身；咖啡馆的"桌子"实际是立式衣柜、"书架"实际是一丛灌木；一批家具没裁到内容边界，于是全部悬浮在该在的位置上方。摆放也跟着修了：人坐到桌前，椅子不再被身体完全盖住。校验脚本原来对这些全部误报通过，换成检查帧底部那行不透明像素的宽度之后能真正拦住。

旧小镇整体下线。它的单聊会把真实 Todo 标题原文拼进发往模型的提示词，只靠一句"绝不能提"约束，正文一个字符都不扫描；前端虽已无入口，但任何已登录用户都能直接发请求触发——实测改动前 `GET /api/v1/town` 返回 200 而不是 404。删掉 43 个主源类、35 个测试类，以及它留下的 24 张孤儿表（顺带补上了一个从来没清理过这些表的注销空洞）。前端那边删掉两万五千行不可达代码，四个仍在使用的工具搬到了 `shared/scene`。

模型用量开始被记录——之前 usage 拿到就丢，谈降频只能靠猜。补上了一直缺的四个基础组件（Button / Modal / EmptyState / Skeleton），全部走 token，深浅色都验过。

地基改完了：每位居民有自己的家（此前全镇只有一个 `home`，四个人晚上挤在同一个房间），地点变成两层结构，位置有主人、有容量、会被真正占用。用户的小人成为第五个居民，和四人共用同一套状态与位置机制，会和 NPC 抢同一个座位，也出现在居民的感知里——但它不带任何用户文本进去，有一条塞标记串进念头再断言整个 Context 里找不到它的测试守着。开发期放开了线程池、每日预算和决策节流三条限制。

素材建了目录：四个包十万个 PNG 去重到 13132 条，85 张联系表和一页能翻的索引，从此不必再盲填数字索引。

## 接下来

按依赖排，一批做完合一次 master。

**正在做**（三件同时进行）：让四个人真的不一样——性格写成数值、接进规则闸门、并且**作用在感知与记忆写入上**，同一件事四个人记下不同的事实；好感不再对称，A 对 B 和 B 对 A 各算各的，单恋才有地方存在。同时前端在补两块：`/town` 挪回统一外壳（它一直是顶层路由，暗色模式和主题色从来没生效过，外壳里那四处针对 /town 的判断全是死分支），以及场景改读后端的地点数据、不再按序号硬算坐标——五个人这才会真的各回各家。

分工是按**文件所有权**切的，不是按功能。原本想拆成四五个人并行，但 `ResidentSimulation.java` 是所有后端条目的必经之地，几个 agent 同时改同一个文件只会互相覆盖；人格、偏置、好感、记忆归属本来也是同一个机制，拆开反而打架。

**下一批**接着做剩下的后端条目，都落在 `ResidentSimulation` 一带，所以排在人格那批之后而不是并行：

*咖啡馆的服务链条*——店主从"被标成店主的居民"变成真正的服务者。咖啡机是他的位置，别人不能动；渴的人走到吧台等，或者在他出门时走到街上把他找回来。不给他排班，只给一份会随人等待而累积的责任感，让它和他自己的事竞争。等待、打断、排队、失败四件事给出咖啡馆的节奏，最后一件就是会断的链条。详见 [01](docs/01-requirements.md)「坐在咖啡馆里的那种感觉」。

*其余*：生活链条让物品有真实状态并允许断；上班的人从地图上消失；以及备考邻居的觉醒线——一条从真实系统事件（重启、模型失败退避、部署）通到他记忆里的通道。

**顺序变了一次。** 原来"加速跑几天 + 上帝视角"排在最后当验收手段，现在它是前置条件：人格要靠经历改写、预期要从重复里学出来，而模式得重复五到十次才能和巧合区分开——四个人的小街在真实节奏下一天只产生个位数的咖啡事件，这些东西根本没机会长出来。数值又是悄悄漂移的，不给视图就永远看不见它发生。所以这一批往前挪。

**再下一批**是能看见：带模型无头跑若干天，导出一条可读的时间线，同一套数据配一个开发期的上帝视角页面；跑出来的那几天直接当作开局。视觉异常检查并进这一批——先用坐标和遮挡顺序做程序断言，再用画面比对定位，**最后一步不要指望视觉模型**：四种问法测完最好 62%，书架五种方法二十次作答全答"椅子"，详见 [05](docs/05-notes.md)。

**当前的重点很明确：harness 和涌现。** 先把这个小世界真正跑起来、让它能慢慢长出东西，做出一个有意思的东西之后再考虑怎么收敛成产品。所以下面这批用户侧的工作**排在涌现跑通之后**，不要提前做。

**然后**是接回用户这一侧：简洁模式的左清单右小镇工作台，五维能力接到小人身上（行为解锁、资料卡、居民态度），以及任务标题进 Context 加代码级拦截和逐条加锁。前端也要跟上新的地点数据。

**建筑师**排在地点数据之后：居民满足不了的需求攒够了，就有人从外面来重排房间。

验收不看测试是否全绿，看盲测——从时间线里抽二十句去掉名字，猜是谁说的。猜不出来就是没做到。然后一起开着学一个下午，再连续回来几天。

[想做什么](docs/01-requirements.md) · [代码思路](docs/02-modules.md) · [以后想试试](docs/03-ideas.md) · [定过的事](docs/04-decisions.md) · [实测与环境](docs/05-notes.md)
