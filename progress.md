# 最近在做什么

更新：2026-09-08。

## 现在

`/town` 是一条独立的陪伴小街：一个用户小人、四名居民、服务端独立存档与持续生活。支持真实 Todo 陪伴专注、明确安排、自由随想、取消和回来看日记；小镇不会自动完成真实任务。

居民有各自的需求、情绪、关系、计划和来源记忆。相遇会带来协商、改变看法与实际合作，海报、花草、书角和茶桌随行动变化。模型决策已接入异步流程，故障时有限退避，规则生活继续。已切换到 DeepSeek V4 Flash，真实验证了双方逐轮对话与各自的会后记忆。

这一轮把注意力从"小镇能不能自己活下去"转到"它能不能让我看见自己"。围坐讨论后确认了几件事，它们决定了接下来的形状。

**小镇是清单的反馈层。** 一个人只需要一份 todo list，但勾完一项什么都没发生，久了就不想勾了。小镇补的就是这一下：你勾完一项，小人也放下笔、伸个懒腰、去倒杯水，然后回到自己的节律。**你是因，它是果**——它永远不代你勾，也永远不反过来催你。

**小人是第五个居民，不是傀儡。** 它像楚门一样过着自己的日子——对它来说，小镇就是真实生活。用户不操纵它，用户是它脑子里突然出现的想法：明确的安排它会照做，"想看看花"这样的随想它择机实现。所以它和四位居民共用同一套状态与模拟，只是多了一路来自用户的念头。

已有的五维能力（知识、健康、事业、关系、心境）接到它身上：你的努力让它变得更好，能力改变它能做的事，资料卡里看得到它的样子，居民也会因此改变对它的态度。小镇本身不因谁的努力而改变——街道、房子、花草只属于居民自己的生活。

**居民可以知道你在忙，但不能把它说出口。** 任务标题会进入居民的决策，让它选择陪你去书桌而不是打断你；但代码会在动作、对话和记忆落库之前拦下含有标题原文的输出，不靠提示词约束。每项任务可以单独加锁，锁上的只以"一项任务"的身份存在。设置里会写明白：开启后标题会发给模型服务商。

**两种模式都留着。** 简洁模式是一张工作台：左边清单，右边小镇，其余收起来。完整模式保留现有导航，小镇是其中一页，那些模块一个都不删，慢慢往小镇里长，这一轮先接上能力这一条。

**几乎不打扰。** 无声、无弹窗、无骚动动效。想看的时候看得到，不看的时候它不喊你。

## 接下来

先修三处已经确认的毛病。

美术的裁切错误查清了根因：`build-companion-assets.py` 选错了源角色表的一行，导致五位居民三个动作共 210 帧全是漂浮的半身；腿部嫁接用了不看 alpha 的 `paste`，反而抠掉更多；咖啡馆的"桌子"实际是个衣柜，"书架"实际是一丛灌木；一批家具没有裁到内容边界，于是全部悬浮在该在的位置上方。现有的校验脚本断言太松，这些全都误报通过了，要一起加固。

结构的割裂来自一次没收尾的重构：`/town` 从 `UserLayout` 的子路由被挪成了顶层路由，于是深色模式、强调色、转场、侧边栏、跳转链接在小镇里全部失效，`UserLayout` 里还留着永远不会命中的分支。收回去，接上共同的 token，小镇保留自己更暗更暖的性格。

还有两笔账要清：前端 `modules/town/` 已经有约两万五千行不可达，只剩四个工具文件还在被小街引用；模型用量一直没有记录，`usage` 字段拿到就丢了，所以在拿到真实数字之前不谈降频。

地图这一轮一格不扩。现在的"地图"其实是后端四个字符串加前端三张写死的坐标表，`docs/01` 里写的"小街→咖啡馆→窗边→座位"一行都没实现。先把它变成一份数据驱动的定义，否则扩图只是把硬编码抄到更多地方。

最大的一块新工程不是界面，是让小人真正成为居民之一——它现在是个游离在模拟之外的 `avatar` 字段，居民根本感知不到它。做完之后一起开着学一个下午，再连续回来几天，看看是否仍愿意停留。

[想做什么](docs/01-requirements.md) · [代码思路](docs/02-modules.md) · [以后想试试](docs/03-ideas.md)
