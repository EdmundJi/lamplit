# 最近在做什么

更新：2026-09-09。

## 现在

`/town` 是一条陪伴式小街：现在从四名居民和一个用户小人开始，服务端保存他们持续变化的生活。这一轮集中处理居民自主生活、工作职责怎样在真实交流后改变，以及地图能不能把这些过程自然地表现出来。想清楚的事都写进了 [01](docs/01-requirements.md)，定过的决定列在 [04](docs/04-decisions.md)，量出来的事实和踩过的坑在 [05](docs/05-notes.md)。

**已经做完的：**

美术管线的裁切错误查到了根因并修好——生成脚本选错了源角色表的一行，五位居民三个动作共 210 帧全是漂浮的半身；咖啡馆的"桌子"实际是立式衣柜、"书架"实际是一丛灌木；一批家具没裁到内容边界，于是全部悬浮在该在的位置上方。摆放也跟着修了：人坐到桌前，椅子不再被身体完全盖住。校验脚本原来对这些全部误报通过，换成检查帧底部那行不透明像素的宽度之后能真正拦住。

旧小镇整体下线。它的单聊会把真实 Todo 标题原文拼进发往模型的提示词，只靠一句"绝不能提"约束，正文一个字符都不扫描；前端虽已无入口，但任何已登录用户都能直接发请求触发——实测改动前 `GET /api/v1/town` 返回 200 而不是 404。删掉 43 个主源类、35 个测试类，以及它留下的 24 张孤儿表（顺带补上了一个从来没清理过这些表的注销空洞）。前端那边删掉两万五千行不可达代码，四个仍在使用的工具搬到了 `shared/scene`。

模型用量开始被记录——之前 usage 拿到就丢，谈降频只能靠猜。补上了一直缺的四个基础组件（Button / Modal / EmptyState / Skeleton），全部走 token，深浅色都验过。

地基改完了：每位居民有自己的家（此前全镇只有一个 `home`，四个人晚上挤在同一个房间），地点变成两层结构，位置有主人、有容量、会被真正占用。用户的小人成为第五个居民，和四人共用同一套状态与位置机制，会和 NPC 抢同一个座位，也出现在居民的感知里——但它不带任何用户文本进去，有一条塞标记串进念头再断言整个 Context 里找不到它的测试守着。开发期放开了线程池、每日预算和决策节流三条限制。

素材建了目录：四个包十万个 PNG 去重到 13132 条，85 张联系表和一页能翻的索引，从此不必再盲填数字索引。

第一轮规则实现曾让人格数值、不同视角的记忆、不对称好感与咖啡馆服务链跑起来，也暴露了规则会替居民权衡责任、产生抱怨和主动续杯的问题。这些认知旁路已经由本轮模型入口取代；第一轮样本只保留作诊断历史。加速运行器仍能在独立内存世界中导出时间线、存档、模型用量和涌现指标，不改变产品里的真实日夜。

第一轮的自主生活能力已经接通并完成受控回归。长期职业方向 `careerIntent` 与眼前项目 `lifeIntent` 分开，居民能在公共项目之间持续给自己的职业留时间；当前动作被柜台或谈话打断时会保存剩余时长并恢复。原来的移动后固定 42 秒也已改成保留调用方要求的动作时长。

工作变化从真实谈话进入：一方本人提出帮工、委托或接手，另一方必须在自己的回合明确接受，之后才得到对应的吧台权限和服务压力；拒绝、结束安排与经营者单方面停止劳动也都有实际状态变化。非法或过期的模型结果不会留下半句同意、记忆或权限。规则不再在项目完成 240 秒后从固定标题自动制造新活动；后续活动只能由居民结合自己的记忆提出，模型不可用时继续已有生活。

手动设计的新居民可以由后端作者入口加入，得到动态住处、床、初始自我记忆和职业方向；模拟不会自动生成应聘者。前端的房间、美术与人物映射仍需随手写人物一起设计，这不是一行后端调用就会自动长出第六栋房子的系统。

地图第二轮也完成了：五户住处和咖啡馆重新构图，下排居民可被镜头找到；吧台服务不再画成园艺；人物面板分别显示长期职业、眼前线索与暂放动作，相同内容不重复；雨只落在街道和花园，不穿过住宅或咖啡馆。真实注册、加入小镇和推进接口已由当前源码服务跑通，实际页面截图在 [体验报告](output/town-life-review/frontend-experience.md)。

前一阶段独立验收的相关后端 39 项、加速生活专项 8 项与前端相关 54 项曾通过。其六小时纯规则样本产生 49 个事件、32 轮对话和 263 条记忆；同配置的中间诊断曾是 942、564、3575。样本里的 32 次服务请求及规则反思不代表当前模型大脑版本，只说明了为什么需要这次迁移。

## 当前阶段

**这一阶段已经完成：**

Qwen3.8-Flash 已成为小镇主力模型，DeepSeek 只作备用；provider、base URL、API key、model、timeout、stream timeout 与 JSON mode 组成完整且彼此独立的配置组。真实 canonical 调用确认实际模型为 `qwen3.8-flash`，决策、对白与回忆均可关闭思考输出；密钥仍只在本地忽略文件中，不进入仓库、日志或报告。

LLM 现在是每名 NPC 的认知入口。工作、休息、睡觉、社交、继续或恢复安排、开关店和点饮料都由模型从当前可执行动作中选择。内部 `energy`、`social`、关系与责任压力数值不进入模型；正常状态省略，明显疲惫才成为定性体感。规则只负责经过时间、移动、位置容量、设备权限与已经选择的动作结果。模型失败时继续合法旧安排或中性等待，不另造一个规则大脑。

咖啡馆保存可调的 9:00–21:00 初始惯例，经营者模型自行决定 `open_cafe` / `close_cafe`。真实关店后才拒绝新单、结束馆内谈话并留下现场通知；居民可以用 `continue_home` 或 `resume` 带着原动作、原因和剩余时长回各自书桌续工。所有 NPC 都有可调的 23:00–07:00 初始睡眠习惯；夜间只出现 routine 线索，不自动强睡，也不再给店主或画师永久夜猫豁免。精力按世界经过时间和动作强度变化，短休与饮料不能代替连续睡眠。

地图最终为 1248×768。咖啡馆窗边六张单人桌连成一排：原窗位仍归小川，另外五席是可独立占用的公共座位；中央讨论桌、员工通道与服务关系保留。每户书桌也有真实后端位置，work/read/make 在家能坐到桌前继续。相关画面见 [咖啡厅布局报告](output/town-life-review/cafe-layout/report.md) 与 [作息报告](output/town-life-review/daily-rhythm/review.md)。

对白已去掉规则复读、客服式赞同和强制感悟；Qwen 受控样本能用短回应处理眼前具体事情。咖啡馆加入一段经来源和校验和核验的 CC0 本地环境音，默认关闭、可随时关闭，只在镜头位于营业中的咖啡馆时轻声淡入。来源、衍生处理与未作主观试听的限制见 [对白与环境音报告](output/town-life-review/dialogue-ambience/review.md) 和 [音频许可说明](frontend/public/assets/audio/README.md)。

本轮后端联合检查 80 项、前端相关检查 33 项通过，类型检查、生产构建、六席路径与浏览器场景检查通过。真实 Qwen canonical 样本证明定性状态能进入主模型并产生不同选择；Qwen-only 短跑实际调用 8 次，7 个动作落地、1 个地点不合法的动作被 domain 拒绝，全部由 `qwen3.8-flash` 非思考模式完成。完整统计归档在 [阶段验收报告](output/town-life-review/qa-report.md)。这些都不能证明长期涌现。

暂停经营不再是不可逆状态。仍保有咖啡馆管理权的人会在平常开门时间看到店仍暂停的事实，可以由模型自主选择 `open_cafe`，走回店里并在开门完成后恢复经营；真实接手后，旧经营者不能绕过经营权夺回。停止经营也改为走 closing 收束请求和谈话，不自动发言或替顾客回家。最终相关联合小集 53/53 通过；两段真实 Qwen 受控样本 2/2 成功，实际模型均为 `qwen3.8-flash`、reasoning tokens 为 0：仍有经营权的停工者选择 `open_cafe`，普通居民在店已开后选择 `study@cafe`。后端只重启自身，容器编译与 Spring/Tomcat 启动健康，没有推进、重置或改写数据库世界。证据见 [自主复工报告](output/town-life-review/cafe-return/review.md)、[模型复核](output/town-life-review/cafe-return/model-review.md) 与 [体验复核](output/town-life-review/cafe-return/experience-review.md)。调试视图的新鲜度修复另有 20 项前端检查和类型检查通过。

**仍要长期观察：**

更长期的故事感仍要靠一起开着学习、连续回来几天以及第 1/3/7 天盲测判断。第一轮六小时纯规则样本只是历史诊断，不能用来证明当前“LLM 是大脑”的运行方式。自然辞职、接手、错误预期、关系恶化与人格复利是否会真实发生，仍未证明。

用户侧的左清单右小镇工作台、五维能力与任务标题安全边界继续排在自主生活体验之后。

**建筑师**排在地点数据之后：居民满足不了的需求攒够了，就有人从外面来重排房间。

验收不看测试是否全绿，看盲测——从时间线里抽二十句去掉名字，猜是谁说的。猜不出来就是没做到。然后一起开着学一个下午，再连续回来几天。

[想做什么](docs/01-requirements.md) · [代码思路](docs/02-modules.md) · [以后想试试](docs/03-ideas.md) · [定过的事](docs/04-decisions.md) · [实测与环境](docs/05-notes.md)
