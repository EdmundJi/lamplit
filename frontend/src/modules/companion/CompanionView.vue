<script setup lang="ts">
import { computed, onBeforeUnmount, onMounted, ref, watchEffect } from 'vue'
import { RouterLink } from 'vue-router'
import { storeToRefs } from 'pinia'
import { BookOpen, Flower2, Footprints, House, Volume2, VolumeX, Moon, X, Check, Play, ArrowLeft, ArrowUpRight, Send, Sparkles, ChevronRight, Maximize2, Minimize2, MessageCircle, MapPinned, UsersRound, Wrench, BedDouble } from 'lucide-vue-next'
import { disclose as vDisclose } from '../../shared/ui/interaction/disclose'
import SnapSlider from '../../shared/ui/interaction/SnapSlider.vue'
import { residentRoleFor, useTownClock } from './companion.presentation'
import { useCompanionWorld, focusRemaining, clockText } from './companion.store'
import { useTownUi } from './town-ui.store'
import { useTodayLogic } from '../today/today.logic'
import { useAuthStore } from '../auth/auth.store'
import type { Memory, Position, Project, WorldEvent, WorldObject } from './companion.types'

const town = useCompanionWorld()
const { world, loaded, loading, busy, error, feedback, activeIntents } = town
const { tasks, plannedTasks, error: taskError, feedback: taskFeedback, canComplete, act, last, reverse } = useTodayLogic()
const auth = useAuthStore()
// Panels/state the docked street strip also needs (to drive its own, always-mounted
// CompanionScene) live in the shared town-ui store; journal/story/focus disclosure stays local -
// TownStage never shows those panels, so there is nothing docked reading them.
const ui = useTownUi()
const { quiet, overview, textBubbles, soundEnabled, selectedResident, selectedConversation, selectedPlace, selectedProject } = storeToRefs(ui)
function toggleTextBubbles() { ui.toggleTextBubbles() }
const journalOpen = ref(false)
const storiesOpen = ref(false)
const focusOpen = ref(false)
const focusDismissed = ref(false)
const placeBrowserOpen = ref(false)
const thought = ref('')
const thoughtPriority = ref<'explicit' | 'passing'>('passing')
const taskId = ref('')
const duration = ref(25)
const durationOptions = [15, 25, 45, 60]
const name = ref(auth.user?.displayName || '新邻居')
const { now, timezone, minutes } = useTownClock(() => world.value?.timezone)
const time = computed(() => new Intl.DateTimeFormat('zh-CN', { timeZone: timezone.value, hour: '2-digit', minute: '2-digit', hourCycle: 'h23' }).format(now.value))
const cafeStatusLabel = computed(() => world.value?.cafeStatus === 'closing' ? '咖啡馆收店中' : world.value?.cafeStatus === 'closed' ? '咖啡馆已打烊' : '咖啡馆营业中')
const resident = computed(() => world.value?.residents.find(actor => actor.id === selectedResident.value))
const memories = computed(() => world.value?.memories.filter(memory => memory.ownerId === selectedResident.value).slice().reverse() || [])
const opinions = computed(() => {
  const seen = new Set<string>()
  return memories.value.filter(memory => {
    if (memory.sourceType !== 'reflection') return false
    const text = memory.text.trim().replace(/\s+/g, '')
    if (seen.has(text)) return false
    seen.add(text); return true
  }).slice(0, 2)
})
const experiences = computed(() => memories.value.filter(memory => memory.sourceType !== 'reflection'))
function evidence(memory: Memory) { return world.value?.memories.filter(item => memory.evidenceIds?.includes(item.id)) || [] }
const person = computed(() => world.value?.residentStates?.find(state => state.id === selectedResident.value))
const projects = computed(() => world.value?.projects || [])
const project = computed(() => projects.value.find(item => item.id === selectedProject.value))
// `careerIntent` is the durable direction. A public project or a short interruption can replace
// lifeIntent several times in one day, so it must never be used as the resident's profession.
const careerDirection = computed(() => person.value?.careerIntent?.purpose || person.value?.occupation || '还在慢慢找方向')
const personalGoal = computed(() => person.value?.lifeIntent?.purpose || projects.value.find(item => item.id === person.value?.goal)?.title || person.value?.thought || '眼前的小事')
const normalizedPurpose = (value: string) => value.replace(/\s+/g, '')
const showCurrentThread = computed(() => personalGoal.value !== '眼前的小事' && normalizedPurpose(personalGoal.value) !== normalizedPurpose(careerDirection.value))
const pausedAction = computed(() => person.value?.suspendedAction?.plan || null)
const events = computed(() => (world.value?.events || []).slice().reverse())
const storyEvents = computed(() => events.value.filter(event => !selectedProject.value || event.projectId === selectedProject.value))
const latestConversation = computed(() => (world.value?.conversations || []).filter(talk => talk.status === 'active' && talk.turns.length).slice().reverse()[0])
const conversation = computed(() => world.value?.conversations?.find(item => item.id === selectedConversation.value))
const conversationTitle = computed(() => projects.value.find(item => item.id === conversation.value?.topicId)?.title || `${placeLabel(conversation.value?.place) || '小街'}的一段闲聊`)
const residentConversations = computed(() => (world.value?.conversations || []).filter(talk => talk.participantIds.includes(selectedResident.value || '')).slice().reverse().slice(0, 2))
const relationships = computed(() => Object.entries(person.value?.relationships || {}).filter(([id]) => id !== selectedResident.value && world.value?.residents.some(actor => actor.id === id)).sort((a, b) => b[1] - a[1]))
const diary = computed(() => world.value?.diary.slice().reverse() || [])
const focusTask = computed(() => tasks.value.find(task => task.taskPublicId === world.value?.focus?.taskId))
const selectedTask = computed(() => tasks.value.find(task => task.publicId === taskId.value))
const remaining = computed(() => world.value?.focus ? focusRemaining(world.value.focus.endsAt, now.value, world.value.focus.startedAt) : 0)
const focusIntent = computed(() => activeIntents.value.find(item => item.kind === 'focus'))
const lastFocusTaskId = computed(() => world.value?.intents.slice().reverse().find(intent => intent.kind === 'focus' && intent.taskId && ['active', 'done'].includes(intent.status))?.taskId)
const completableTask = computed(() => focusTask.value || tasks.value.find(task => task.taskPublicId === lastFocusTaskId.value))
const focusVisible = computed(() => focusOpen.value || (!focusDismissed.value && (Boolean(world.value?.focus) || Boolean(completableTask.value && canComplete(completableTask.value)))))
function toggleFocusPanel() { const next = !focusVisible.value; focusOpen.value = next; focusDismissed.value = !next; if (next) { selectedResident.value = null; selectedConversation.value = null; journalOpen.value = false; storiesOpen.value = false } }
function closeFocusPanel() { focusOpen.value = false; focusDismissed.value = true }
// TownPlaces hands out one home per resident ("home-owner", "home-student", "home-self", ...)
// instead of a single shared "home", so this stays a prefix match rather than a literal key per
// resident - a new resident's home just works without touching this dictionary.
//
// Deliberately its own small table, not STAGE_PLACES: these labels are the colloquial names used
// in-conversation and in intent text actually sent to the backend as what the avatar is thinking
// ("我想去咖啡馆看看...") - see visitProject() below - while STAGE_PLACES's labels are the more
// formal building signage the docked strip's nav reads ("慢慢咖啡"). Unifying the two would
// quietly change user-facing wording sent as story content, not just deduplicate a lookup table -
// CompanionView.test.ts locks in the exact phrase for this reason. board/academy/gym fall back to
// their own raw place id here rather than silently borrowing a neighbour's label - honest, if
// plain, until this table gets a warmer colloquial name for each.
const places: Record<string, string> = { cafe: '咖啡馆', street: '门前小街', garden: '门前花园', academy: '梧桐学院', gym: '河岸健身房', board: '公告板广场', shop: '南巷商店' }
function placeLabel(place?: string | null): string {
  if (!place) return ''
  if (place === 'home-self') return '自己家'
  if (place === 'home' || place.startsWith('home-')) return '住处'
  return places[place] || place
}
function residentRole(id: string) { return residentRoleFor(world.value, id) }
function selectResident(id: string) { placeBrowserOpen.value = false; selectedConversation.value = null; closeFocusPanel(); if (id === world.value?.avatar.id) { selectedResident.value = id; overview.value = false; selectedPlace.value = ''; selectedProject.value = null; journalOpen.value = false; storiesOpen.value = false; return } if (world.value?.residents.some(actor => actor.id === id)) { selectedResident.value = id; overview.value = false; selectedPlace.value = ''; selectedProject.value = null; journalOpen.value = false; storiesOpen.value = false; quiet.value = false } }
const publicPlaceOrder = ['street', 'cafe', 'garden', 'academy', 'gym', 'board', 'shop']
const publicLocations = computed(() => {
  const registered = world.value?.locations?.filter(item => item.kind !== 'home').map(item => item.id)
  const ids = new Set(registered?.length ? registered : publicPlaceOrder)
  return [...ids].sort((a, b) => (publicPlaceOrder.indexOf(a) < 0 ? 99 : publicPlaceOrder.indexOf(a)) - (publicPlaceOrder.indexOf(b) < 0 ? 99 : publicPlaceOrder.indexOf(b)))
})
const homeLocations = computed(() => world.value?.locations?.filter(item => item.kind === 'home') || [])
const allActors = computed(() => world.value ? [world.value.avatar, ...world.value.residents] : [])
function actorsAt(place: string) { return [world.value?.avatar, ...(world.value?.residents || [])].filter(actor => actor?.place === place) }
function roomsAt(place: string) { return world.value?.rooms?.filter(room => room.buildingId === place) || [] }
function positionsAt(place: string) { return world.value?.positions?.filter(position => position.place === place) || [] }
function objectsAt(place: string) { return world.value?.objects?.filter(item => item.place === place) || [] }
function positionLabel(position: Position) {
  return ({ bed: '床位', desk: '书桌', seat: '座位', table: '共用桌', bench: '长椅', plot: '花圃', equipment: '设备', workbench: '工具台', stove: '炉灶', bathroom: '浴室' } as Record<string, string>)[position.kind] || position.kind
}
function positionState(position: Position) {
  if (position.condition === 'broken') return '需要修理'
  const parts = [position.occupantIds.length ? `${position.occupantIds.length}/${position.capacity} 正在使用` : '空着']
  if (position.waitingIds?.length) parts.push(`${position.waitingIds.length} 人在等`)
  return parts.join(' · ')
}
function objectState(item: WorldObject) {
  const holder = item.holderId && item.holderId !== item.ownerId ? actorName(item.holderId) : ''
  return holder ? `${item.state} · ${holder}正拿着` : item.state
}
function homeName(place: string) {
  const residents = [world.value?.avatar, ...(world.value?.residents || [])].filter(actor => actor && roomsAt(place).some(room => room.residentIds.includes(actor.id)))
  return residents.length ? `${residents.map(item => item!.name).join('、')}的住处` : `南巷住处`
}
function focusPlace(id: string) {
  selectedPlace.value = id; selectedResident.value = null; selectedConversation.value = null; selectedProject.value = null
  overview.value = false; journalOpen.value = false; storiesOpen.value = false; closeFocusPanel(); quiet.value = false
}
function togglePlaceBrowser() {
  placeBrowserOpen.value = !placeBrowserOpen.value
  if (placeBrowserOpen.value) { selectedResident.value = null; selectedConversation.value = null; journalOpen.value = false; storiesOpen.value = false; closeFocusPanel(); quiet.value = false }
}
function source(memory: Memory) {
  const who = world.value?.residents.find(actor => actor.id === memory.sourceId)?.name || (memory.sourceId === world.value?.avatar.id ? '你的小人' : memory.sourceId)
  return memory.sourceType === 'reflection' ? '从经历里慢慢形成的看法' : memory.sourceType === 'seed' ? '搬来前的生活 · 初始记忆' : memory.sourceType === 'heard' ? `听${who || '邻居'}说起 · 转述` : '亲身经历 · 观察'
}
function actorName(id: string) { return world.value?.residents.find(actor => actor.id === id)?.name || (id === world.value?.avatar.id ? world.value.avatar.name : '邻居') }
function relationshipLabel(value: number) { return value >= 70 ? '很聊得来' : value >= 40 ? '渐渐熟悉' : value >= 12 ? '还在认识' : value > 0 ? '有点疏远' : '有点不同意见' }
function followProject(item: Project) { placeBrowserOpen.value = false; selectedConversation.value = null; closeFocusPanel(); overview.value = false; selectedProject.value = item.id; selectedPlace.value = item.place; selectedResident.value = null; storiesOpen.value = true; journalOpen.value = false; quiet.value = false }
function followEvent(event: WorldEvent) { selectedConversation.value = null; closeFocusPanel(); selectedPlace.value = event.place; if (event.actorIds[0]) selectedResident.value = event.actorIds[0]; storiesOpen.value = false }
function openStories() { placeBrowserOpen.value = false; selectedConversation.value = null; closeFocusPanel(); storiesOpen.value = !storiesOpen.value; selectedProject.value = null; selectedResident.value = null; journalOpen.value = false; quiet.value = false }
function openJournal() { placeBrowserOpen.value = false; selectedConversation.value = null; closeFocusPanel(); journalOpen.value = !journalOpen.value; storiesOpen.value = false; selectedResident.value = null; quiet.value = false }
function openConversation(id: string) { const talk = world.value?.conversations?.find(item => item.id === id); if (!talk) return; placeBrowserOpen.value = false; closeFocusPanel(); selectedConversation.value = id; selectedResident.value = null; selectedPlace.value = talk.place; journalOpen.value = false; storiesOpen.value = false; quiet.value = false }
async function sendThought() { if (!thought.value.trim() || busy.value) return; if (await town.intend('thought', { text: thought.value.trim(), priority: thoughtPriority.value })) thought.value = '' }
async function visitProject() { if (!project.value) return; await town.intend('thought', { text: `我想去${placeLabel(project.value.place) || project.value.place}看看${project.value.title}`, priority: 'explicit' }) }
function dateTime(at: string) { return new Intl.DateTimeFormat('zh-CN', { timeZone: timezone.value, month: 'numeric', day: 'numeric', hour: '2-digit', minute: '2-digit', hourCycle: 'h23' }).format(new Date(at)) }
async function startFocus() {
  if (!selectedTask.value?.taskPublicId || !canComplete(selectedTask.value)) return
  await town.intend('focus', { taskId: selectedTask.value.taskPublicId, durationMinutes: duration.value })
}
async function completeTask() {
  if (!completableTask.value || !canComplete(completableTask.value)) return
  await act(completableTask.value, 'COMPLETED')
}
function toggleQuiet() { quiet.value = !quiet.value; if (quiet.value) { placeBrowserOpen.value = false; selectedConversation.value = null; closeFocusPanel(); soundEnabled.value = false; selectedResident.value = null; journalOpen.value = false; storiesOpen.value = false; selectedPlace.value = ''; selectedProject.value = null } }
// The scene itself (and its polling/loading/idle-loading lifecycle) belongs to TownStage now -
// it is mounted for the whole signed-in session, not just while /town is open. This page only
// mirrors the two bits TownStage's docked strip cannot see on its own: whether a panel is
// currently open over the scene (suppressHover), and - since the fullscreen CompanionScene's
// click handlers live in TownStage - this page's own selection logic, via the shared store.
watchEffect(() => { ui.hoverSuppressed = Boolean(placeBrowserOpen.value || resident.value || conversation.value || storiesOpen.value || journalOpen.value || focusVisible.value) })
onMounted(() => {
  // TownStage already owns the recurring poll (docked strip and /town share its one timer), but
  // it may not have mounted (or resolved its first load) yet by the time this page needs to know
  // whether the resident has joined - store.load() is idempotent/de-duplicated, so asking again
  // here is always safe, and keeps this page usable (and testable) on its own.
  void town.load().then(() => { if (world.value) void town.load(true) })
  ui.registerFullscreenHandlers({
    selectResident, selectConversation: openConversation,
    selectProject: id => { const item = projects.value.find(item => item.id === id); if (item) followProject(item) },
  })
})
onBeforeUnmount(() => { ui.registerFullscreenHandlers({}) })
</script>

<template>
  <main class="companion-page" :class="{ quiet }" aria-labelledby="companion-title" data-testid="town-v2">
    <header class="street-heading">
      <RouterLink class="back-link" to="/today" aria-label="回到今日"><ArrowLeft :size="17"/><span>今日</span></RouterLink>
      <div class="street-brand"><span class="brand-symbol">✳</span><div><h1 id="companion-title">梧桐小街</h1></div></div>
      <div class="street-tools"><span class="local-time">{{ time }} <span>· {{ world?.weather === 'rain' ? '细雨' : '晴' }} · {{ cafeStatusLabel }}</span></span><button class="tool-button" :aria-label="soundEnabled ? '关闭环境声' : '打开环境声'" :aria-pressed="soundEnabled" :disabled="!world" @click="soundEnabled = !soundEnabled; if (soundEnabled) quiet = false"><Volume2 v-if="soundEnabled" :size="17"/><VolumeX v-else :size="17"/><span>环境声{{ soundEnabled ? '开' : '关' }}</span></button><button class="tool-button" :aria-label="textBubbles ? '关闭文字气泡' : '打开文字气泡'" :title="textBubbles ? '关闭文字气泡' : '打开文字气泡'" :aria-pressed="textBubbles" @click="toggleTextBubbles"><MessageCircle :size="17"/><span>文字气泡</span></button><button class="tool-button" :aria-label="quiet ? '退出安静模式' : '安静模式'" :aria-pressed="quiet" @click="toggleQuiet"><Moon :size="17"/><span>{{ quiet ? '退出安静' : '安静模式' }}</span></button></div>
    </header>
    <p v-if="error" class="notice error" role="alert" data-testid="town-error">{{ error }} <button class="text-button" :disabled="loading || busy" @click="town.load()">重新连接</button></p>
    <div v-if="!loaded" class="arrival-loading" role="status" data-testid="town-loading">{{ loading ? '街角的灯，正在亮起来…' : '小街暂时无法打开，请重新连接。' }}</div>
    <section v-else-if="!world" class="arrival">
      <div class="arrival-art" aria-hidden="true"><span>☕</span><span>⌂</span><span>✿</span><i></i></div>
      <p class="kicker">你的新住处，就在咖啡馆旁边</p><h2>生活已经开始，<br>只差你搬进来。</h2><p>阿禾在准备咖啡，小川带着备考书。<br>知夏答应画一张海报，青叔的花也快开了。</p>
      <form @submit.prevent="town.join(name, timezone)"><label for="neighbor-name">大家怎么称呼你</label><input id="neighbor-name" v-model="name" maxlength="24" autocomplete="nickname" placeholder="新邻居"/><button class="main-button" :disabled="busy || !name.trim()">{{ busy ? '正在安顿…' : '搬进这条小街' }} <House :size="17"/></button></form><small>不必照料谁。忙几天再回来，也可以从当下开始。</small>
    </section>
    <template v-else>
      <section class="town-stage" aria-label="正在生活的小街">
        <div id="town-stage-slot" class="town-stage-slot" aria-label="正在生活的小街"></div>
        <button v-if="!quiet" class="scene-note" :disabled="!latestConversation" @click="latestConversation && openConversation(latestConversation.id)"><span class="living-dot"></span><span>{{ latestConversation ? `${placeLabel(latestConversation.place)}里，有人聊了起来` : '点一个小人，看看 TA 的一天' }}</span><ChevronRight v-if="latestConversation" :size="12"/></button>
        <nav v-if="!quiet" class="scene-navigation" aria-label="小街故事"><button class="view-toggle" :aria-pressed="overview" :aria-label="overview ? '回到街角' : '看整条街'" :title="overview ? '回到街角' : '看整条街'" @click="overview = !overview"><Minimize2 v-if="overview" :size="16"/><Maximize2 v-else :size="16"/></button><button :class="{ selected: placeBrowserOpen }" aria-label="查看地点与居民" @click="togglePlaceBrowser"><MapPinned :size="16"/>地点与居民</button><button :class="{ selected: storiesOpen }" @click="openStories"><Sparkles :size="16"/>小街正在发生<span v-if="projects.some(item => item.status === 'active')" class="living-dot"/></button><button :class="{ selected: journalOpen }" @click="openJournal"><BookOpen :size="16"/>我的日记</button></nav>

        <aside v-if="placeBrowserOpen && !quiet" class="story-panel place-browser" aria-label="地点与居民" data-testid="place-browser">
          <header class="panel-header"><div><p class="kicker">六处公共地方，十六户人家</p><h2>去镇上看看</h2></div><button class="close-button" aria-label="收起地点与居民" @click="placeBrowserOpen = false"><X :size="18"/></button></header>
          <div class="panel-scroll">
            <section class="place-group"><span class="kicker">公共地方</span><button v-for="id in publicLocations" :key="id" class="place-row" :class="{ selected: selectedPlace === id }" :data-place-id="id" @click="focusPlace(id)"><MapPinned :size="15"/><span><strong>{{ placeLabel(id) }}</strong><small>{{ actorsAt(id).length ? `${actorsAt(id).length} 人在这里` : '此刻很安静' }}</small></span><ChevronRight :size="14"/></button></section>
            <section v-if="selectedPlace" class="place-now"><span class="kicker">此刻所见</span><button v-for="actor in actorsAt(selectedPlace)" :key="actor!.id" class="resident-row" :data-resident-id="actor!.id" @click="selectResident(actor!.id)"><UsersRound :size="14"/><span><strong>{{ actor!.name }}</strong><small>{{ actor!.label }}</small></span><ChevronRight :size="13"/></button><p v-if="!actorsAt(selectedPlace).length" class="place-empty">这里暂时没人，物件和房间仍保持着刚才的样子。</p><p v-for="position in positionsAt(selectedPlace)" :key="position.id" class="place-object"><Wrench :size="13"/>{{ positionLabel(position) }} · {{ positionState(position) }}</p><p v-for="item in objectsAt(selectedPlace)" :key="item.id" class="place-object"><Sparkles :size="13"/>{{ item.label }} · {{ objectState(item) }}</p></section>
            <details class="home-list"><summary><House :size="14"/>住处 <span>{{ homeLocations.length }}</span></summary><button v-for="home in homeLocations" :key="home.id" class="place-row" :class="{ selected: selectedPlace === home.id }" :data-place-id="home.id" @click="focusPlace(home.id)"><BedDouble :size="15"/><span><strong>{{ homeName(home.id) }}</strong><small>{{ roomsAt(home.id).length }} 个房间 · {{ actorsAt(home.id).length }} 人在家</small></span><ChevronRight :size="14"/></button></details>
            <details class="home-list resident-directory"><summary><UsersRound :size="14"/>居民名册 <span>{{ allActors.length }}</span></summary><button v-for="actor in allActors" :key="actor.id" class="resident-row" :data-resident-id="actor.id" @click="selectResident(actor.id)"><span class="mini-portrait" :class="`portrait-${actor.id}`">{{ actor.name.slice(-1) }}</span><span><strong>{{ actor.name }}</strong><small>{{ residentRole(actor.id) }} · {{ placeLabel(actor.place) }}</small></span><ChevronRight :size="13"/></button></details>
          </div>
        </aside>

        <aside v-if="resident && !quiet" :key="resident.id" class="story-panel person-panel" aria-label="居民的记忆">
          <header class="panel-header"><div class="person-identity"><span class="portrait" :class="`portrait-${resident.id}`">{{ resident.name.slice(-1) }}</span><div><span class="kicker">{{ residentRole(resident.id) }} · {{ placeLabel(resident.place) }}</span><h2>{{ resident.name }}</h2></div></div><button class="close-button" aria-label="收起居民记忆" @click="selectedResident = null"><X :size="18"/></button></header>
          <div class="panel-scroll"><p class="current-action"><span class="living-dot"></span>{{ resident.label }}</p>
            <template v-if="person"><blockquote class="inner-thought">“{{ person.thought || personalGoal }}”</blockquote><div class="personal-plan"><span class="kicker">长期想走的方向</span><p>{{ careerDirection }}</p><template v-if="showCurrentThread"><span class="kicker current-thread">眼前在惦记</span><p class="life-thread">{{ personalGoal }}</p></template><span v-if="person.plan" class="plan-reason">{{ person.plan.reason }}</span><p v-if="pausedAction" class="paused-action">暂放着：{{ pausedAction.reason || '一件还没做完的事' }}</p></div><section v-if="opinions.length" class="opinions"><span class="kicker">现在的看法</span><article v-for="opinion in opinions" :key="opinion.id"><p>{{ opinion.text }}</p><details v-if="evidence(opinion).length" v-disclose><summary>为什么会这样想</summary><div v-for="item in evidence(opinion)" :key="item.id" class="evidence"><p>{{ item.text }}</p><small>{{ source(item) }} · {{ dateTime(item.at) }}</small></div></details></article></section><div v-if="relationships.length" class="relationship-list"><span class="kicker">在这条街上的牵挂</span><button v-for="[id, value] in relationships" :key="id" @click="selectResident(id)"><span class="mini-portrait" :class="`portrait-${id}`">{{ actorName(id).slice(-1) }}</span><strong>{{ actorName(id) }}</strong><span>{{ relationshipLabel(value) }}</span><ChevronRight :size="13"/></button></div></template>
            <section v-for="conversation in residentConversations" :key="conversation.id" class="conversation"><span class="kicker">{{ conversation.status === 'active' ? '此刻的对话' : '刚才聊起' }} · {{ placeLabel(conversation.place) }}</span><div v-for="(turn, index) in conversation.turns.slice(-4)" :key="index" class="conversation-turn" :class="{ self: turn.speakerId === resident.id }"><button @click="selectResident(turn.speakerId)">{{ actorName(turn.speakerId) }}</button><p>{{ turn.text }}</p></div></section>
            <details class="memory-detail" :open="!person" v-disclose><summary>那些留下痕迹的事 <span>{{ experiences.length }}</span></summary><ol class="memory-list"><li v-for="memory in experiences" :key="memory.id"><p>{{ memory.text }}</p><small>{{ source(memory) }} · {{ dateTime(memory.at) }}</small></li></ol><p v-if="!experiences.length">日子会慢慢留下痕迹。</p></details>
          </div>
        </aside>

        <aside v-if="storiesOpen && !quiet" :key="selectedProject || 'street-stories'" class="story-panel" aria-label="小街故事">
          <header class="panel-header"><div><p class="kicker">有人惦记，有人动手，有人刚刚听说</p><h2>{{ project?.title || '这条街，正在发生' }}</h2></div><button class="close-button" aria-label="收起故事" @click="storiesOpen = false; selectedProject = null; selectedPlace = ''"><X :size="18"/></button></header>
          <div class="panel-scroll"><template v-if="project"><div class="project-feature"><span class="project-symbol">{{ project.objectKind === 'flowers' ? '✿' : project.objectKind === 'poster' ? '▧' : project.objectKind === 'books' ? '▤' : '☕' }}</span><p>{{ project.description }}</p><span class="project-status">{{ project.status === 'ready' || project.status === 'celebrating' ? '已经准备好了' : project.status === 'idea' ? '一个刚冒头的念头' : '一点一点，在变成真的' }}</span><div class="project-progress" :aria-label="`${project.title}完成${project.progress}%`"><span :style="{ width: `${project.progress}%` }"/></div><div class="contributors"><button v-for="id in project.contributors" :key="id" @click="selectResident(id)">{{ actorName(id) }}留下了一点心意</button></div><button class="main-button" :disabled="busy" @click="visitProject"><Footprints :size="15"/>我也想过去看看</button></div></template>
            <div v-else class="project-list"><button v-for="item in projects" :key="item.id" class="project-story" @click="followProject(item)"><span class="project-symbol">{{ item.objectKind === 'flowers' ? '✿' : item.objectKind === 'poster' ? '▧' : item.objectKind === 'books' ? '▤' : '☕' }}</span><span><strong>{{ item.title }}</strong><small>{{ item.description }}</small><span class="project-stage">{{ placeLabel(item.place) }} · {{ item.status === 'ready' || item.status === 'celebrating' ? '准备好了' : item.status === 'idea' ? '还在酝酿' : '正在发生' }}</span></span><ArrowUpRight :size="15"/></button></div>
            <div class="story-timeline"><span class="kicker">{{ project ? '这件事，是怎样走到这里的' : '生活里的小小后续' }}</span><button v-for="event in storyEvents.slice(0, 14)" :key="event.id" @click="followEvent(event)"><time>{{ dateTime(event.at) }}</time><p>{{ event.text }}</p><small>去{{ placeLabel(event.place) || '街上' }}看看 <ArrowUpRight :size="12"/></small></button><p v-if="!storyEvents.length" class="empty-story">先坐一会儿。有人正在把心里的念头，变成一件小事。</p></div>
          </div>
        </aside>

        <aside v-if="conversation && !quiet" :key="conversation.id" class="story-panel conversation-panel" aria-label="完整对话"><header class="panel-header"><div><p class="kicker">{{ conversation.status === 'active' ? '正在聊' : '刚才的一段话' }} · {{ placeLabel(conversation.place) }}</p><h2>{{ conversationTitle }}</h2></div><button class="close-button" aria-label="收起对话" @click="selectedConversation = null; selectedPlace = ''"><X :size="18"/></button></header><div class="panel-scroll"><div class="conversation-participants"><button v-for="id in conversation.participantIds" :key="id" @click="selectResident(id)">{{ actorName(id) }} <ArrowUpRight :size="12"/></button></div><div v-for="(turn, index) in conversation.turns" :key="`${turn.speakerId}:${turn.at}:${index}`" class="conversation-turn"><button @click="selectResident(turn.speakerId)">{{ actorName(turn.speakerId) }}</button><p>{{ turn.text }}</p></div></div></aside>
        <aside v-if="journalOpen && !quiet" class="story-panel journal-panel" aria-label="小人的日记"><header class="panel-header"><div><p class="kicker">不用补读所有对话</p><h2>在这里度过的日子</h2></div><button class="close-button" aria-label="收起日记" @click="journalOpen = false"><X :size="18"/></button></header><div class="panel-scroll"><p v-if="world.offlineSummary" class="return-note">{{ world.offlineSummary }}</p><ol class="memory-list"><li v-for="entry in diary" :key="entry.id"><small>{{ dateTime(entry.at) }}</small><p>{{ entry.text }}</p></li></ol><p v-if="!diary.length">今天的故事，才刚刚开始。</p></div></aside>
      <section v-if="focusVisible" class="focus-card" aria-label="一起专注"><div class="card-heading"><span class="kicker">你做自己的事，小人就在旁边</span><button class="close-button" aria-label="收起专注" @click="closeFocusPanel"><X :size="16"/></button></div><template v-if="world.focus"><h2>{{ focusTask?.taskTitle || '正在陪你专注' }}</h2><p>{{ focusTask?.status === 'DONE' ? '这项 Todo 已记录完成。可以继续安静坐一会儿，或结束这段专注。' : remaining ? '小人已经找好座位。你做自己的事就好。' : '这段专注时间到了。做完与否，由你决定。' }}</p><div class="button-row"><button v-if="focusIntent" class="soft-button" :disabled="busy" @click="town.cancel(focusIntent.id)">结束这段专注</button><button v-if="focusTask && canComplete(focusTask)" class="main-button" :disabled="busy" @click="completeTask"><Check :size="16"/>我已完成这项 Todo</button></div></template><template v-else><form class="focus-form" @submit.prevent="startFocus"><label class="task-label">今天想专注什么<select v-model="taskId" aria-label="选择一项 Todo" :disabled="busy"><option value="" disabled>选择一项 Todo</option><option v-for="task in plannedTasks" :key="task.publicId" :value="task.publicId">{{ task.taskTitle }}</option></select></label><label class="duration-label">时长 · {{ duration }} 分钟<SnapSlider :model-value="durationOptions.indexOf(duration)" aria-label="专注时长" :disabled="busy" :min="0" :max="durationOptions.length - 1" :step="1" :value-text="`${duration} 分钟`" @update:model-value="i => duration = durationOptions[i]" /></label><button class="main-button" :disabled="busy || !selectedTask?.taskPublicId || !canComplete(selectedTask)"><Play :size="15"/>一起专注</button></form><p v-if="!plannedTasks.length">暂时没有待办，先让小人陪你坐一会儿。<RouterLink to="/today">去安排今天</RouterLink></p><div v-if="completableTask && canComplete(completableTask)" class="finished-focus"><span>刚才：{{ completableTask.taskTitle }}</span><button class="soft-button" @click="completeTask">我已完成这项 Todo</button></div></template><small>计时结束、小人读书，都不会替你勾选真实 Todo。</small><p v-if="taskError" class="notice error" role="alert">{{ taskError }}</p><p v-if="taskFeedback" class="task-feedback" role="status">{{ taskFeedback.text }} <button v-if="last" class="text-button" @click="reverse">撤销记录</button></p></section>
        <div class="scene-caption"><button class="self-mark" aria-label="看看我的小人" title="看看我的小人" @click="selectResident(world.avatar.id)">{{ world.avatar.name.slice(-1) }}</button><div><strong>{{ world.avatar.name }}</strong><span>{{ world.avatar.label }}</span></div><span class="caption-location">{{ placeLabel(world.avatar.place) }}</span><button class="text-button focus-toggle" :aria-expanded="focusVisible" @click="toggleFocusPanel"><span v-if="world.focus" class="timer" role="timer">{{ clockText(remaining) }}</span><Play v-else :size="14"/>{{ world.focus ? '正在陪你专注' : '留一段专注时间' }}</button></div>
      </section>

      <section v-if="!quiet" class="thought-dock" aria-label="捎一个想法"><form @submit.prevent="sendThought"><Flower2 :size="20" class="thought-icon"/><label class="sr-only" for="inner-thought">捎一个内心念头</label><input id="inner-thought" v-model="thought" maxlength="200" placeholder="脑海里突然冒出一个念头……比如，想去看看知夏的海报" :disabled="busy"/><select v-model="thoughtPriority" aria-label="念头优先级" :disabled="busy"><option value="passing">有空的时候</option><option value="explicit">现在就想</option></select><button class="send-thought" :disabled="busy || !thought.trim()" aria-label="捎给小人"><Send :size="17"/></button></form><div class="thought-shortcuts"><span>或者轻轻捎一句</span><button :disabled="busy" @click="town.intend('rest')">休息一会儿</button><button :disabled="busy" @click="town.intend('walk')">出去散散步</button><button :disabled="busy" @click="town.intend('home')">先回家吧</button><button :disabled="busy" @click="town.intend('flowers')">有空想看看花</button></div></section>
      <div v-if="!quiet" class="intent-area"><p v-if="feedback && !activeIntents.some(intent => intent.feedback === feedback)" class="feedback" role="status">{{ feedback }}</p><div v-for="intent in activeIntents.filter(item => item.kind !== 'focus')" :key="intent.id" class="intent-item"><span>{{ intent.feedback || (intent.status === 'pending' ? '念头先放在心里，等一会儿。' : '正在做这件事。') }}</span><button class="text-button" :disabled="busy" @click="town.cancel(intent.id)">收回想法</button></div></div>



    </template>
  </main>
</template>

<style scoped>
.companion-page{--street-ink:color-mix(in srgb, var(--ink) 88%, var(--amber) 12%);--street-paper:color-mix(in srgb, var(--surface) 90%, var(--amber) 10%);--street-muted:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper));--street-line:color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));--street-accent:var(--accent);--street-amber:var(--amber);--street-danger-bg:color-mix(in srgb, var(--danger) 14%, var(--street-paper));--street-danger-ink:color-mix(in srgb, var(--danger) 65%, var(--street-ink));min-height:100dvh;box-sizing:border-box;padding:0 28px 28px;color:var(--street-ink);background:color-mix(in srgb, var(--street-ink) 5%, var(--street-paper));font-family:inherit}.companion-page button{min-height:0;font-weight:400}.street-heading{height:83px;display:flex;align-items:center;gap:24px;max-width:1500px;margin:0 auto}.back-link{display:flex;gap:7px;align-items:center;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));text-decoration:none;font-size:12px;padding-right:20px;border-right:1px solid color-mix(in srgb, var(--street-ink) 20%, var(--street-paper))}.street-brand{display:flex;align-items:center;gap:11px}.brand-symbol{font-size:35px;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper))}.street-brand h1{font-family:serif;font-size:21px;letter-spacing:3px;margin:0;font-weight:600}.street-brand p{margin:4px 0 0;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));font-size:11px;letter-spacing:1px}.street-tools{display:flex;gap:10px;align-items:center;margin-left:auto}.local-time{font-size:14px;color:color-mix(in srgb, var(--street-ink) 80%, var(--street-paper));font-variant-numeric:tabular-nums;padding-right:12px}.local-time>span{font-size:11px;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.tool-button{display:flex;align-items:center;gap:7px;background:none;border:0;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper));font:inherit;font-size:11px;padding:9px;cursor:pointer}.tool-button[aria-pressed=true]{background:color-mix(in srgb, var(--street-ink) 10%, var(--street-paper));border-radius:var(--radius-card)}.town-stage{position:relative;max-width:min(1500px,126.55dvh,1340px);margin:auto;border:1px solid color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));border-radius:var(--radius-panel);overflow:hidden;background:color-mix(in srgb, var(--street-ink) 15%, var(--street-paper))}.town-stage-slot{position:relative;isolation:isolate;z-index:0;height:min(68dvh,720px)}.scene-note{position:absolute;top:20px;left:22px;display:flex;align-items:center;gap:8px;background:color-mix(in srgb, var(--street-paper) 85%, transparent);border:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));color:color-mix(in srgb, var(--street-ink) 75%, var(--street-paper));padding:9px 13px;border-radius:var(--radius);font-size:11px;cursor:pointer}.living-dot{display:inline-block;width:6px;height:6px;flex-shrink:0;border-radius:50%;background:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.scene-navigation{position:absolute;top:18px;right:18px;display:flex;gap:7px}.scene-navigation button{display:flex;gap:7px;align-items:center;background:color-mix(in srgb, var(--street-paper) 95%, transparent);color:color-mix(in srgb, var(--street-ink) 80%, var(--street-paper));border:1px solid color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));border-radius:var(--radius-card);padding:10px 12px;font-size:11px;cursor:pointer}.scene-navigation button.selected{background:color-mix(in srgb, var(--street-ink) 15%, var(--street-paper))}.scene-caption{min-height:64px;padding:10px 24px;box-sizing:border-box;background:var(--street-paper);display:flex;gap:12px;align-items:center;border-top:1px solid color-mix(in srgb, var(--street-ink) 25%, var(--street-paper));position:relative;z-index:2}.self-mark{border:0;padding:0;cursor:pointer;width:33px;height:35px;display:grid;place-items:center;border-radius:var(--radius-panel) var(--radius-panel) var(--radius-card) var(--radius-card);background:color-mix(in srgb, var(--street-ink) 40%, var(--street-paper));color:var(--on-forest);font-size:17px}.scene-caption>div{display:flex;gap:12px;align-items:baseline}.scene-caption strong{font-size:12px}.scene-caption div>span{font-size:12px;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper))}.caption-location{border-left:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));padding-left:12px;font-size:11px;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper))}.focus-toggle{margin-left:auto!important}.timer{font-variant-numeric:tabular-nums;letter-spacing:1px;font-size:17px;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper))}.thought-dock{max-width:1100px;margin:20px auto 0}.thought-dock form{display:flex;align-items:center;gap:12px;background:var(--street-paper);border:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));padding:9px 12px 9px 20px;border-radius:var(--radius-card);box-shadow:0 4px 16px transparent}.thought-icon{color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper));flex-shrink:0}.thought-dock input{flex:1;min-width:0;border:0;background:transparent;padding:10px 0;font:inherit;font-size:13px;color:color-mix(in srgb, var(--street-ink) 95%, var(--street-paper));box-shadow:none}.thought-dock input::placeholder{color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper));font-size:12px}.thought-dock select{border:0;border-left:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));background:transparent;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));padding:7px 9px;font-size:11px;width:auto}.send-thought{width:36px;height:36px;border:0;border-radius:var(--radius-card);background:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));color:var(--on-forest);display:grid;place-items:center;cursor:pointer}.thought-shortcuts{display:flex;align-items:center;justify-content:center;gap:18px;margin-top:12px;font-size:11px;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper))}.thought-shortcuts button{background:none;border:0;padding:3px 0;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper));font:inherit;font-size:11px;cursor:pointer}.thought-shortcuts button:hover{color:color-mix(in srgb, var(--street-ink) 95%, var(--street-paper))}.intent-area{max-width:1040px;margin:auto;font-size:12px;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper))}.intent-item{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:9px 0;border-bottom:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper))}.feedback{margin:12px 0 0;text-align:center}.story-panel{position:absolute;z-index:4;top:69px;right:18px;bottom:84px;width:min(355px,calc(100% - 36px));border-radius:var(--radius-card);background:color-mix(in srgb, var(--street-paper) 95%, transparent);border:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));box-shadow:var(--shadow);display:flex;flex-direction:column;overflow:hidden;backdrop-filter:blur(12px)}.panel-header{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;padding:20px 20px 15px;border-bottom:1px solid color-mix(in srgb, var(--street-ink) 10%, var(--street-paper));flex-shrink:0}.panel-header h2{font-family:serif;font-size:20px;font-weight:600;margin:4px 0 0;letter-spacing:.6px}.kicker{display:block;font-size:11px;letter-spacing:1px;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper));margin:0 0 7px}.close-button{display:grid;place-items:center;border:0;border-radius:50%;background:color-mix(in srgb, var(--street-ink) 10%, var(--street-paper));color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));width:27px;height:27px;flex-shrink:0;cursor:pointer}.panel-scroll{overscroll-behavior:contain;padding:16px 20px 24px;overflow:auto;min-height:0;scrollbar-width:thin;scrollbar-color:color-mix(in srgb, var(--street-ink) 20%, var(--street-paper)) transparent}.person-identity{display:flex;align-items:center;gap:12px}.person-identity .kicker{font-size:11px;margin:0}.portrait{height:45px;width:43px;border-radius:var(--radius-panel) var(--radius-panel) var(--radius-card) var(--radius-card);background:color-mix(in srgb, var(--street-ink) 45%, var(--street-paper));color:var(--on-forest);display:grid;place-items:center;font-family:serif;font-size:22px;flex-shrink:0}.mini-portrait{height:26px;width:25px;border-radius:var(--radius-card) var(--radius-card) var(--radius-card) var(--radius-card);background:color-mix(in srgb, var(--street-ink) 45%, var(--street-paper));color:var(--on-forest);display:grid;place-items:center;font-family:serif;font-size:13px;flex-shrink:0}.portrait-student{background:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper))}.portrait-artist{background:color-mix(in srgb, var(--street-ink) 45%, var(--street-paper))}.portrait-gardener{background:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper))}.portrait-fixer{background:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.portrait-weaver{background:color-mix(in srgb, var(--street-ink) 40%, var(--street-paper))}.current-action{font-size:11px;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper));margin:0 0 14px;display:flex;align-items:center;gap:8px}.inner-thought{margin:0 0 20px;padding:14px 16px;background:color-mix(in srgb, var(--street-ink) 5%, var(--street-paper));border-radius:var(--radius) var(--radius-card) var(--radius-card) var(--radius-card);font-family:serif;color:color-mix(in srgb, var(--street-ink) 80%, var(--street-paper));line-height:1.9;font-size:14px}.personal-plan{margin:16px 0 22px}.personal-plan p{font-size:12px;line-height:1.8;margin:7px 0}.plan-reason{color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));font-size:11px;line-height:1.8}.relationship-list{margin:18px 0 22px}.relationship-list button{display:flex;align-items:center;gap:9px;width:100%;border:0;background:none;color:color-mix(in srgb, var(--street-ink) 75%, var(--street-paper));padding:7px 0;font-size:11px;cursor:pointer}.relationship-list strong{font-weight:500}.relationship-list button>span:nth-last-child(2){margin-left:auto;font-size:11px;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.conversation{padding:16px 0;border-top:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper))}.conversation-turn{margin-top:12px}.conversation-turn>button{border:0;background:none;font-size:11px;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper));padding:0;cursor:pointer}.conversation-turn p{background:color-mix(in srgb, var(--street-ink) 10%, var(--street-paper));border-radius:var(--radius) var(--radius-card) var(--radius-card) var(--radius-card);padding:9px 12px;color:color-mix(in srgb, var(--street-ink) 80%, var(--street-paper));font-size:11px;line-height:1.85;margin:5px 18px 0 0}.conversation-turn.self p{background:color-mix(in srgb, var(--street-ink) 10%, var(--street-paper));margin:5px 0 0 18px}.conversation-turn.self{text-align:right}.opinions{margin:18px 0 22px}.opinions article{background:color-mix(in srgb, var(--street-ink) 10%, var(--street-paper));border-left:2px solid color-mix(in srgb, var(--street-ink) 35%, var(--street-paper));padding:10px 12px;margin-bottom:10px}.opinions article>p{font-family:serif;font-size:13px;line-height:1.9;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper));margin:0 0 9px}.opinions summary{font-size:11px;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper));cursor:pointer}.evidence{padding-top:9px;margin-top:8px;border-top:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper))}.evidence p{font-size:11px;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper));line-height:1.8;margin:0 0 5px}.evidence small{font-size:11px;color:color-mix(in srgb, var(--street-ink) 45%, var(--street-paper))}.memory-detail{border-top:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));padding-top:16px}.memory-detail summary{cursor:pointer;font-size:11px;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper))}.memory-detail summary span{float:right;color:color-mix(in srgb, var(--street-ink) 45%, var(--street-paper))}.memory-list{padding:0;margin:10px 0 0;list-style:none}.memory-list li{border-top:1px solid color-mix(in srgb, var(--street-ink) 10%, var(--street-paper));padding:13px 0}.memory-list p{font-size:12px;line-height:1.9;color:color-mix(in srgb, var(--street-ink) 75%, var(--street-paper));margin:4px 0 6px}.memory-list small{font-size:11px;line-height:1.7;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper))}.project-list{display:grid;gap:10px;margin-bottom:24px}.project-story{display:flex;gap:12px;align-items:flex-start;background:color-mix(in srgb, var(--street-ink) 5%, var(--street-paper));border:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));border-radius:var(--radius-card);padding:13px 12px;text-align:left;cursor:pointer;color:color-mix(in srgb, var(--street-ink) 80%, var(--street-paper))}.project-symbol{font-family:serif;font-size:34px;line-height:1.2;color:var(--street-amber);flex-shrink:0}.project-story strong{font-size:12px;display:block;margin:2px 0 6px}.project-story small{font-size:11px;line-height:1.7;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));display:block}.project-story svg{flex-shrink:0;margin-top:4px}.project-stage{display:block;font-size:11px;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper));margin-top:8px}.project-feature{text-align:center;margin-bottom:26px}.project-feature>.project-symbol{font-size:64px;display:block;margin:8px auto}.project-feature p{font-size:12px;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper));line-height:1.9}.project-status{font-size:11px;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper))}.project-progress{height:3px;border-radius:var(--radius);background:color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));width:75%;margin:12px auto}.project-progress span{display:block;height:100%;background:color-mix(in srgb, var(--street-ink) 45%, var(--street-paper));transition:width 1s}.contributors{margin:12px 0 18px}.contributors button{display:block;margin:5px auto;border:0;background:none;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper));font-size:11px;cursor:pointer}.story-timeline>button{display:block;position:relative;width:100%;background:none;border:0;border-left:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));padding:9px 0 17px 16px;text-align:left;margin-left:3px;cursor:pointer}.story-timeline>button:before{content:'';position:absolute;left:-3px;top:14px;background:color-mix(in srgb, var(--street-ink) 40%, var(--street-paper));width:5px;height:5px;border-radius:50%}.story-timeline time{font-size:11px;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper))}.story-timeline p{font-size:12px;line-height:1.85;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper));margin:6px 0}.story-timeline small{font-size:11px;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper));display:flex;align-items:center;gap:3px}.empty-story{font-family:serif;padding:30px 10px;text-align:center;line-height:2;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper))}.focus-card{max-width:720px;margin:20px auto;background:var(--street-paper);border:1px solid color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));border-radius:var(--radius-card);padding:20px 24px}.card-heading{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.card-heading .kicker{margin:0}.focus-card h2{font-size:17px;margin:0 0 8px}.focus-card p{font-size:12px;line-height:1.8;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper))}.focus-card>small{display:block;margin-top:15px;color:color-mix(in srgb, var(--street-ink) 50%, var(--street-paper));font-size:11px}.focus-form{display:flex;align-items:end;gap:12px}.focus-form label{display:flex;flex-direction:column;gap:7px;font-size:11px;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.task-label{flex:1;min-width:0}.duration-label{width:94px}.focus-form select,.focus-form input[type='range']{width:100%;border:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper));background:color-mix(in srgb, var(--street-ink) 5%, var(--street-paper));border-radius:var(--radius-card);padding:11px;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper));font-size:12px}.main-button,.soft-button,.text-button{display:inline-flex;align-items:center;justify-content:center;gap:7px;cursor:pointer;font:inherit;border-radius:var(--radius-card);font-size:11px}.main-button{background:var(--street-accent);border:1px solid var(--street-accent);color:white;padding:11px 14px;white-space:nowrap}.soft-button{background:color-mix(in srgb, var(--street-ink) 5%, var(--street-paper));border:1px solid color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper));padding:10px 12px}.text-button{background:none;border:0;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));padding:3px 5px}.button-row{display:flex;gap:10px;flex-wrap:wrap}.finished-focus{display:flex;gap:12px;align-items:center;flex-wrap:wrap;font-size:11px;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));margin-top:18px}.notice{font-size:12px;padding:10px 14px;border-radius:var(--radius-card);background:var(--street-danger-bg);color:var(--street-danger-ink);max-width:1450px;margin:0 auto 14px}.companion-page button:disabled{opacity:.45;cursor:default}.companion-page button:focus-visible,.companion-page select:focus-visible,.companion-page input:focus-visible{outline:2px solid color-mix(in srgb, var(--street-ink) 40%, var(--street-paper));outline-offset:3px}.sr-only{position:absolute;width:1px;height:1px;padding:0;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}.arrival-loading{padding:120px 20px;text-align:center;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.arrival{min-height:660px;max-width:1500px;box-sizing:border-box;margin:auto;border:1px solid color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));background:linear-gradient(160deg,color-mix(in srgb, var(--street-ink) 10%, var(--street-paper)),color-mix(in srgb, var(--street-ink) 5%, var(--street-paper)));border-radius:var(--radius-panel);text-align:center;padding:52px 24px;overflow:hidden}.arrival-art{position:relative;height:100px;display:flex;justify-content:center;align-items:end;gap:24px;margin-bottom:28px}.arrival-art span{font-size:62px;z-index:1}.arrival-art span:first-child{font-size:44px}.arrival-art span:last-child{font-size:54px;color:var(--street-amber)}.arrival-art i{position:absolute;width:260px;height:32px;border-radius:50%;background:color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));bottom:-8px}.arrival h2{font-family:serif;font-size:35px;line-height:1.6;font-weight:500;margin:14px 0}.arrival p:not(.kicker){font-size:12px;line-height:2;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper))}.arrival form{max-width:285px;display:grid;gap:12px;text-align:left;margin:24px auto}.arrival label{font-size:11px;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.arrival input{font-size:14px;background:var(--street-paper);border:1px solid color-mix(in srgb, var(--street-ink) 20%, var(--street-paper));padding:12px;border-radius:var(--radius-card);color:color-mix(in srgb, var(--street-ink) 75%, var(--street-paper))}.arrival .main-button{font-size:13px}.arrival small{font-size:11px;color:color-mix(in srgb, var(--street-ink) 55%, var(--street-paper))}.quiet .town-stage{max-width:min(1500px,145dvh,1560px)}.quiet .town-stage-slot{height:min(78dvh,840px)}.return-note{font-family:serif;line-height:2;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper));font-size:13px;padding:12px;background:color-mix(in srgb, var(--street-ink) 5%, var(--street-paper));border-radius:var(--radius-card)}
.place-group{display:grid;gap:7px}.place-row,.resident-row{display:flex;align-items:center;gap:10px;width:100%;padding:10px;border:1px solid color-mix(in srgb,var(--street-ink) 13%,var(--street-paper));border-radius:var(--radius-card);background:color-mix(in srgb,var(--street-ink) 4%,var(--street-paper));color:var(--street-ink);text-align:left;cursor:pointer}.place-row.selected{background:color-mix(in srgb,var(--street-accent) 12%,var(--street-paper));border-color:color-mix(in srgb,var(--street-accent) 32%,var(--street-paper))}.place-row>span,.resident-row>span{display:grid;gap:3px;min-width:0;flex:1}.place-row strong,.resident-row strong{font-size:12px;font-weight:600}.place-row small,.resident-row small{font-size:11px;color:var(--street-muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.place-now{margin:18px 0;padding:14px;border-radius:var(--radius-card);background:color-mix(in srgb,var(--street-ink) 5%,var(--street-paper))}.resident-row{padding:8px;margin-top:6px;background:var(--street-paper)}.place-empty{font-size:12px;line-height:1.7;color:var(--street-muted)}.place-object{display:flex;align-items:center;gap:7px;margin:8px 0;font-size:11px;line-height:1.5;color:var(--street-muted)}.home-list{margin-top:18px;border-top:1px solid var(--street-line);padding-top:14px}.home-list summary{display:flex;align-items:center;gap:7px;margin-bottom:9px;cursor:pointer;font-size:12px;font-weight:600}.home-list summary span{margin-left:auto;color:var(--street-muted);font-weight:400}.home-list .place-row{margin-top:7px}
@media(min-width:1500px){.town-stage{max-width:min(1500px,126.55dvh,1340px)}.companion-page{padding-inline:44px}.town-stage-slot{height:min(68dvh,720px)}}
@media(max-width:1000px){.town-stage{max-width:min(100%,121dvh,1340px)}.companion-page{padding-inline:16px}.street-heading{height:72px;gap:17px}.scene-note{top:16px;left:16px;font-size:11px}.scene-navigation{top:14px;right:14px}.scene-navigation button{padding:9px;font-size:11px}.town-stage-slot{height:min(65dvh,650px)}.story-panel{width:310px;right:12px;top:60px}.back-link{padding-right:13px}.thought-dock{margin-top:16px}}
@media(max-width:640px){.companion-page{padding:0 9px 20px}.street-heading{height:68px;gap:10px}.back-link{padding-right:9px}.back-link span{display:none}.street-brand{gap:6px}.brand-symbol{font-size:27px}.street-brand h1{font-size:17px;letter-spacing:2px}.street-brand p{font-size:11px;letter-spacing:0}.street-tools{gap:0}.tool-button{padding:8px}.tool-button span{display:none}.local-time{font-size:12px;padding-right:3px}.local-time>span{display:none}.town-stage{border-radius:var(--radius-panel);max-width:100%}.town-stage-slot{height:clamp(270px,54dvh,420px);min-height:0}.scene-note{top:11px;left:10px;padding:7px 9px;font-size:11px;max-width:45%}.scene-navigation{top:10px;right:9px;gap:4px}.scene-navigation button{padding:8px;font-size:0;gap:0}.scene-navigation button svg{width:16px;height:16px}.scene-navigation .living-dot{display:none}.scene-caption{padding:10px 12px;min-height:62px;gap:8px}.self-mark{height:30px;width:29px;font-size:14px}.scene-caption>div{display:block;min-width:0;max-width:48%}.scene-caption strong{display:block;font-size:11px;margin-bottom:3px}.scene-caption div>span{font-size:11px;display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.caption-location{display:none}.focus-toggle{font-size:11px!important;gap:4px!important}.timer{font-size:12px}.story-panel{position:fixed;top:auto;bottom:12px;right:12px;width:calc(100vw - 24px);max-width:none;max-height:72dvh;border-radius:var(--radius-panel);z-index:30;box-shadow:var(--shadow)}.panel-header{padding:15px 16px 12px}.panel-header h2{font-size:18px}.panel-scroll{padding:13px 16px 20px}.thought-dock form{gap:7px;padding:7px 8px 7px 11px}.thought-icon{display:none}.thought-dock input{font-size:12px}.thought-dock input::placeholder{font-size:11px}.thought-dock select{font-size:11px;padding:6px 2px;max-width:80px}.thought-shortcuts{gap:12px;flex-wrap:wrap;margin-top:10px}.thought-shortcuts>span{display:none}.thought-shortcuts button{font-size:11px}.mini-portrait{height:22px;width:20px;font-size:11px}.focus-card{padding:17px;margin-top:16px}.focus-form{flex-wrap:wrap;gap:8px}.focus-form .main-button{width:100%}.thought-dock{margin-top:12px}.intent-item{font-size:11px;padding-inline:5px}.arrival{min-height:590px;padding:36px 18px}.arrival h2{font-size:30px}.quiet .town-stage-slot{height:clamp(320px,67dvh,540px)}}

/* Reading stays in the DOM; the street itself only carries brief, contextual labels. */
.scene-note:disabled{opacity:1;cursor:default}.scene-navigation .view-toggle{padding-inline:10px}.inner-thought,.opinions article>p{font-family:inherit;font-size:13px}.personal-plan p,.conversation-turn p,.memory-list p,.evidence p,.project-feature p,.story-timeline p,.focus-card p{font-size:13px;line-height:1.85}.plan-reason,.memory-list small,.evidence small,.project-story small,.project-stage,.story-timeline time,.story-timeline small,.relationship-list button>span:nth-last-child(2),.person-identity .kicker{font-size:11px;color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper))}.kicker{color:color-mix(in srgb, var(--street-ink) 65%, var(--street-paper))}.panel-header h2{font-family:inherit;font-size:19px}.scene-caption div>span{font-size:12px;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper))}.scene-caption strong{font-size:12px}.thought-dock input{font-size:14px}.thought-dock input::placeholder{font-size:12px;color:color-mix(in srgb, var(--street-ink) 60%, var(--street-paper))}.thought-shortcuts{font-size:11px}.focus-toggle{font-size:12px!important}.scene-note{max-width:50%;font-size:11px}.scene-navigation button{font-size:11px}.close-button{height:30px;width:30px}.scene-navigation button{min-height:32px}
@media(max-width:640px){.scene-note{max-width:calc(100% - 140px);left:9px;padding:8px;gap:5px;text-align:left;line-height:1.4}.scene-note>span:nth-child(2){overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.scene-navigation button{font-size:0;min-height:32px}.scene-navigation{gap:4px}.scene-navigation .view-toggle{padding-inline:8px}.scene-caption>div{max-width:46%}.scene-caption div>span{font-size:11px}.focus-toggle{font-size:11px!important}.thought-dock input::placeholder{font-size:12px}.thought-dock input{font-size:13px}.thought-shortcuts button{font-size:11px}.story-panel{max-height:76dvh}.panel-header h2{font-size:18px}}

/* Panels share one anchored layer: opening controls never shifts the street or steals page scroll. */
.town-stage>.focus-card{position:absolute;z-index:8;right:18px;bottom:82px;width:min(590px,calc(100% - 36px));max-height:calc(100% - 110px);margin:0;overflow-y:auto;overscroll-behavior:contain;box-shadow:var(--shadow)}.conversation-participants{display:flex;flex-wrap:wrap;gap:12px;padding-bottom:12px;margin-bottom:12px;border-bottom:1px solid color-mix(in srgb, var(--street-ink) 15%, var(--street-paper))}.conversation-participants button{display:inline-flex;align-items:center;gap:4px;border:0;background:none;color:color-mix(in srgb, var(--street-ink) 70%, var(--street-paper));font-size:12px;padding:3px 0}.conversation-panel .conversation-turn{margin-top:18px}.conversation-panel .conversation-turn p{margin-right:0}.scene-note,.scene-navigation{z-index:2}.story-panel{z-index:6}
@media(max-width:640px){.town-stage>.focus-card{position:fixed;z-index:32;bottom:12px;right:12px;width:calc(100vw - 24px);max-height:76dvh;padding:18px;margin:0;border-radius:var(--radius-panel)}.story-panel{z-index:30}}
</style>
