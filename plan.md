# 成长小镇 · 虚拟世界 MVP 规划

> 把「成长小镇」从一条可以走动的像素街景，变成一个**有人住、有事发生、信息会走样地传开、留得下痕迹**的小世界。
> 参考：Stanford Generative Agents、[a16z-infra/ai-town](https://github.com/a16z-infra/ai-town)。
>
> **核心内核：NPC 限知 + 信息传播模拟。**
> 没有限知，NPC 说什么都像广播；有了限知，同一个镜头里两个 NPC 知道的东西不一样，观众才会信。
>
> **验收标准：能录一段 30 秒长镜头——镜头扫过小镇，NPC 各自活动、彼此搭话，说的内容互不相同且都说得通。**

---

## 0. 现状盘点（代码调研结论，已修正早期误判）

| 维度 | 现状 | 结论 |
| --- | --- | --- |
| 世界形态 | `town.engine.ts` 单条横向基线（`BASELINE_ROW=28`），房屋按 `PLOT_START + index × 336` 排开 | 是走廊，不是世界 |
| 居民来源 | `TownService.java:66` = 自己 + 已接受好友 | 没好友 = 空街 |
| NPC | 仅 2 个：小助（GUIDE）、邮递员（POSTMAN） | 本质是两个功能入口的拟人化 |
| NPC 后端底子 | `TownPersonas` / `TownNpcPerception` / `TownFacts` / `TownPromiseDetector` / `TownNpcCadence` / `TownReflectionService` | **比预期好得多**，见 §0.2 |
| 前端 NPC 行为 | `npc-schedule.ts`：时段 + 地点 + 活动 + 心情 + 避雨 | 已写好，等着被复用 |
| 功能面板 | `immersive/panels/manifest.ts` 已有 9 个模块面板 | 「功能进小镇」其实已做完 |
| 面板与地点绑定 | 9 个中只有 4 个填了 `anchor` | 其余与地点无关，dock 沦为导航栏 |
| 房屋 | `building-kit.ts` 按 level / 主维度 / streak 纯函数生成外立面 | 玩家改不了一个像素 |
| 宠物 | `town.engine.ts` 内 grep 不到 pet | 宠物不在世界里 |
| 室内场景 | `interior.scene.ts` + `map-loader.ts` 已完成 | 只有「成长学院」用上了 |
| 在场同步 | `presence.ts` 上报 + `TownPresenceClamp` 服务端裁决，轮询 | 够 MVP 用，不改 |

### 0.1 三处必须修正的早期误判

| 早前的说法 | 实际 |
| --- | --- |
| 「`map-loader.ts` 已支持 Tiled」 | **错**。是项目自研的极简 schema（`cols/rows/layers/collisions/doors/furniture/slots/seats`），`map-loader.ts:7-10` 明说「刻意比 Tiled 小」。没有 object layer，没有自定义属性 bag |
| 「`home-living-room.json` 已存在」 | **严重低估**。26 件家具 + 门 + 成就墙 slot 全部完工，和已上线的自习室是同级完成度。`public-gym.json` 同理（22 件家具 + `healthDone` slot） |
| 「宠物 sprite 在屋里跑过来迎接」 | **做不到**。宠物是 Rive 矢量动画（28 个 `.riv`、8 物种 36 品种），Phaser 拿不到它的纹理，风格也与 LimeZu 32×32 像素冲突 |

### 0.2 最重要的发现：限知的地基已经在了

`TownNpcPerception.java` 的类注释：

> 「把原始数字变成一个**偶尔见到你的人**真实会有的粗略印象——绝不给数字本身，也绝不给某天的任务标题和时间。和 `TownFacts` 分开放，是为了让『测到了什么』和『NPC 被允许说什么』之间的边界一眼可见。」

**`TownFacts`（测量层）→ `TownNpcPerception`（可说层）** 这条隐私边界已经建好。
`TownPersonas.java` 里 POSTMAN 已写明「不评价别人，不透露别人的隐私细节」，GUIDE 已写明「你认识镇上的每一个人」。

**缺的只有两个维度：per-NPC（现在所有 NPC 共享同一份模糊视图）和传播。**

### 0.3 死代码：`enterRoom()`

`town.engine.ts:1320` 支持 `home` / `academy` / `gym` 三个房间，三张地图都在磁盘上，**但全仓库没有任何 UI 调用它**。
即使接上，`town.engine.ts:1329` 调用 `createInteriorScene` 时**只传了 `{ room, onExit }`**——没传 `player`（`spawnPlayer()` 在 `interior.scene.ts:192` 直接 return，房间里没有角色）、没传 `metrics`（slot 恒为 0，成就墙一个都不画）、没传 `residents`。

---

## 1. 诊断：五个短板

1. **空间是走廊，不是世界。** 一条直线街，没有拐角、没有视野遮挡。速度翻倍后更暴露——3 秒跑到街尾，然后呢？「移动慢」是症状，「路上没东西」才是病。
2. **世界里没有人在活着。** 居民只有好友，好友不上线就是空街；两个 NPC 不互相说话、不自主行动。
3. **功能是「贴」上去的。** 9 个面板挂在 dock 上，点图标开窗 = 换皮导航栏，世界退化成壁纸。
4. **玩家留不下痕迹。** 房子是数据的纯函数；最强的情感锚点（宠物）完全不在世界里。
5. **NPC 说的话没有来源。** 现在 NPC 说什么都是系统直接喂的，没有「他为什么知道这件事」的解释，所以怎么写都像广播。

---

## 2. 设计决策总表

### 2.1 定位

| # | 决策 |
| --- | --- |
| D1 | 协作关系，本文档写成**任务拆分**，不做说服性论证 |
| D2 | 小镇 = **平行入口**（功能自洽），分两期。一期必须自洽的面板：**今天 / 伙伴 / AI 助手**，不允许跳 `fullPage` 回传统页 |
| D3 | **纯单人 + NPC 撑场**，好友的小人不出现在你的街道上 |
| D4 | demo 的「哇」= **NPC 活着长镜头** → 做**观察模式**：镜头脱离玩家，在若干兴趣点之间自动缓慢巡游 |
| D5 | 活动与信件**不入数据、只入叙事**——成长只能来自真实行动，不能靠在游戏里逗留刷出来 |

### 2.2 限知与传播（核心）

| # | 决策 |
| --- | --- |
| D6 | 事实原子 = **玩家事件 + NPC 自己的事**（不含世界事件——人人目击得到，传不起来） |
| D7 | 目击 = **物理在场 + 玩家主动告知**。小助**全知但不八卦**：私人秘书人格，替用户着想、给正反馈、不过度干预、不 push、不传播隐私 |
| D8 | NPC 记忆 = **后端持久化 + 时间衰减**，跨会话连贯 |
| D9 | NPC 兴趣 = **五维度分配 + 1~2 个不相干的个人癖好** |
| D10 | 性格 = **分享欲 × 好奇心** 两维（造出大喷子 / 倾听者 / 八卦枢纽 / 独行侠四型） |
| D11 | 关系图：**玩家也是节点**。亲密度由「相遇累积 + 时间衰减」「聊得投不投机」「兴趣重合度」加权，含**随机扰动、简单情绪系统、预置的隐秘癖好** |
| D12 | 转述 = **每晚离线跑完整条传播链**，LLM 批量生成每一手的走样版本写入表，前端相遇时只取出来播。相遇行为按亲密度分档：熟人停下多说，生人点头即过 |

### 2.3 事件与信件

| # | 决策 |
| --- | --- |
| D13 | NPC **自主**办活动，办不办、办什么由性格 + 关系 + 心情决定 |
| D14 | 活动 = **高密度相遇场**，信息在这里爆发。不做小游戏——活动本身就是那个 30 秒长镜头 |
| D15 | 信件三轨：**树洞笔友的私密长信** + **镇上 NPC 的短笺** + **活动请柬**。全部由邮递员送 |
| D16 | 活动场地**复用现有广场 / 公园 / 咖啡馆门口**，不新建地图 |

### 2.4 NPC 三层结构

「招架不住」的根源不是 NPC 数量，是**需要玩家回应的 NPC 数量**。斯坦福小镇有 25 个 agent，观众不累，因为观众不需要回应任何人。所以分层而非砍量：

| 层 | 数量 | 定义 | 约束 |
| --- | --- | --- | --- |
| **第一层** | **2（锁死）** | 会向玩家索取回应，有正式对话 UI | 小助 + 邮递员。**永不增加**。新 NPC 想进这层，必须先回答「它替代了哪个已有入口」 |
| **第二层** | **6** | 可以搭话，但从不索取 | 有性格、兴趣、关系图、会办活动、会路遇闲聊。**永不弹窗、不打断、不派任务**，头顶最多一个气泡 |
| **第三层** | **10** | 纯背景生命 | 复用 20 个现成居民 sprite。只有日程 + 走动 + 偶尔的路遇气泡。**不可对话**（点击只回一句环境化的话）。**唯一可迁徙的一层**，见 §2.6 |

**总数 18，玩家认知负担只有 8**（一层 2 + 二层 6），因为第三层根本不请求注意力。

**第二层的 6 个必须绑死五维度**（知识 / 健康 / 职场 / 关系 / 心境 + 1 个自由位）。这样 NPC 越热闹，五维度的存在感越**强**而不是越淡——这是把社交系统焊回产品核心的钩子。

**两条护栏（比控制数量可靠得多）：**

- **护栏 A · 全镇主动性预算**：每天**所有 NPC 主动找玩家的次数共享一个上限**（建议 3 次）。小助优先，其余抢剩余额度。从机制上封死「招架不住」。
- **护栏 B · 密度随时段浮动**：`npc-schedule.ts` 已是时段制——早晨 4~5 人，午间咖啡馆高峰 10~12 人，深夜 2~3 人。**同屏峰值控制在 12 以内**。

### 2.5 牵挂系统（暗恋 / 三角关系）

`town_bond` 已有**对称**的 `affinity`（相处熟络度）。再加一个**有向、非对称、隐藏**的维度：

- **`regard`**：在意程度 0-1
- **`regard_kind`**：`CRUSH` 暗恋 / `ADMIRE` 仰慕 / `RIVAL` 暗中较劲 / `OWE` 亏欠 / `MISS` 挂念

两个维度正交，戏全在错位里：

| affinity | regard | 结构 |
| --- | --- | --- |
| 高 | 低 | 熟人。天天见，不放心上 |
| **低** | **高** | **暗恋的经典结构**：不熟，但一直在意 |
| 高 | 双向高但互不知道 | 最有戏的那种 |

**三角关系不用编剧本，是数据结构自己长出来的**：A 对 B 有 regard，C 对 B 也有，而 A 和 C 的 affinity 恰好很高（他俩是朋友）。

#### 它和限知系统的咬合（这是全案设计上最妙的一处）

A 对 B 的 regard 是**一条只有 A 知道的事实**，且 `no_relay`——**本人绝不会说**。那它怎么泄漏？

**只能靠第三方目击 + 猜测**：C 看到 A 在咖啡馆多待了半小时，而 B 恰好也在 → C 猜出来了 → 这条**猜测**（不是事实）进入传播网络 → 逐手走样 → 到 D 那儿变成「听说 A 和 B 在一起了」 → 有一天传回 B 耳朵里。

一行新逻辑都不用发明，全部复用 §3 的机制。**转述走样第一次有了它最擅长的题材。**

**加分实现**：让 `quirks` 与 regard 联动——某个 NPC 的预置癖好是「总记错别人名字」，那他传出去的暗恋传闻就会张冠李戴。

#### 四条红线（硬约束，不可协商）

1. **NPC 绝不对玩家产生 regard。** 玩家是图上节点，但**只参与 `affinity`**。这条一破就是另一个品类的产品。
2. **只做「在意」，不做「结局」。** 不表白、不成对、不失恋、不撕破脸，永远停在暧昧的观察层。一旦有结局就有输家，而这个产品的原则是「允许不完美，中断不是失败」，不该有输家。附带好处：未完结的关系可以无限期产生八卦，不会「演完了」。
3. **玩家只能「听说」，不能「确认」。** 系统**不提供任何界面**告诉你「A 确实暗恋 B」。只能从互相矛盾的传闻里自己拼。八卦的乐趣本来就在不确定——既保住乐趣，又天然规避内容风险。
4. **不占主动性预算。** NPC 不会跑来跟玩家讲这些，只能靠玩家自己站在旁边听到。

### 2.6 小镇群岛：一人一镇 + NPC 迁徙

**每个用户一个独立小镇。** 不做共享世界——共享下每个 NPC 的知识库要同时处理多个玩家的事实，限知模型立刻失控，隐私边界也守不住。一人一镇下，每个 NPC 的知识只关于**一个玩家 + 其他 NPC**，模型干净。

跨用户的连接靠**迁徙**——受控、延迟、走样，比直接显示好友的小人好玩得多。

| 参数 | 决策 |
| --- | --- |
| 谁能迁徙 | **只有第三层背景居民**。第一层（小助是你的私人秘书、邮递员是跨镇的）和第二层（你熟悉的面孔）永不迁徙 |
| 往哪迁 | **只在互为好友的用户之间**。这是唯一的安全阀，也让「好友」第一次有了机制上的回报 |
| 人口 | **交换制**：A 的一个居民搬去 B，B 的一个搬来 A。每镇恒定 18 人，但面孔在变 |
| 频率 | 稀疏，1~2 周一次。**稀疏才有分量** |
| 带走什么 | 性格、兴趣、癖好（不变）；**他的 knowledge**（已走样过几手的 `retold_text`）；他对原小镇的 bond → 自动转为 `MISS` |
| 传闻带名吗 | **带名，但仅限互为好友**。且**内容仍经 `TownNpcPerception` 模糊化**——你会知道是谁，但不会知道具体数字和任务标题。名字和细节是两层独立授权 |
| 跨镇八卦 | 保留（他刚来时会提「我以前那边有两个人……」，这让他真的像从别处来），但 **`salience` 加速衰减**，一两周后不再提起 |
| 冷却 | 一个 NPC 搬完至少驻留 N 周，防止反复搬家 |
| 孤身用户 | 加**「路过的旅人」**：不属于任何人的 NPC，停留几天，带来泛化的、不涉及真实用户的传闻，然后离开 |

**效果**：你的小镇来了个新面孔，某天你路过听见他说——

> 「我以前住的那个镇上，小吉好像连着好几周每天都往健身房跑。」

那是你朋友。你知道是谁，但听到的是走样过、去掉了具体数字的版本。想确认？去问他，或者直接去问你朋友。

**这比显示好友的小人强太多**——传闻自带距离和不确定，反而更像真实的人际关系。你本来就是这样知道朋友近况的。

### 2.7 树洞笔友

一个**独立角色**，地图外，永远见不到。玩家写出去的内容保密，回信也保密。

| | 小助（GUIDE） | 树洞笔友 |
| --- | --- | --- |
| 知道你多少 | **全知**：目标、任务、连续天数都在它手里 | **一无所知**。你说什么它才知道什么 |
| 做什么 | 给建议、给草稿、推进任务 | **只倾听和回应，不给建议** |
| 在哪 | 小镇里能碰到，即时对话 | 地图外，**隔天送达，邮递员亲手送来** |
| 内容去向 | 不外传（`no_relay`） | **绝不进入 `town_fact`，不参与任何传播，存储上与小镇八卦完全隔离** |

**为什么「一无所知」是关键**：对一个完全不了解你的人说话，比对一个知道你一切的助手说话容易得多。这既是树洞的全部价值，也是它和小助的根本区别。

**为什么「慢」是特性**：写完就收到回信那叫聊天，和 AI 助手面板完全重叠，「信」这个形式就白选了。隔天收到，你才会真的惦记着。

**这是整个小镇里唯一一个「你说了不会传出去」的地方。** 在一个装着用户真实成长数据的产品里，这个位置的情感价值很高。

### 2.8 邮递员的三份工

至此邮递员终于有了真活干，不再只是「未读消息」的图标：

1. 好友群聊未读（原有）
2. 活动请柬（§2.3 D15）
3. **树洞回信**（§2.7）

---

## 3. 核心系统设计：小镇社会模拟

### 3.1 数据模型（新增表）

| 表 | 用途 | 关键字段 |
| --- | --- | --- |
| `town_npc` | NPC 档案 | `npc_code`, **`home_user_id`（当前属于哪个小镇）**, `layer(1/2/3)`, `sprite`, `share_drive`（分享欲 0-1）, `curiosity`（好奇心 0-1）, `interests`（五维度权重 JSON）, `quirks`（隐秘癖好 JSON）, `settled_at`（用于搬家冷却） |
| `town_fact` | 可传播的事实原子 | `subject_kind(PLAYER/NPC)`, `subject_id`, `kind`, `dimension`, `payload`（已模糊化）, `occurred_at`, `origin_town_user_id` |
| `town_npc_knowledge` | 谁知道什么（限知的核心） | `npc_code`, `fact_id`, `learned_at`, `learned_from`, `hops`（第几手）, `salience`（显著度，随时间衰减）, `retold_text`（这一手的转述文本）, `no_relay` |
| `town_bond` | 关系图（含玩家节点） | `a_kind/a_id`, `b_kind/b_id`, `affinity`（对称）, `resonance`（历史投机度）, `last_met_at`, **`regard`（有向、隐藏）**, **`regard_kind`** |
| `town_npc_mood` | 每日情绪 | `npc_code`, `local_date`, `valence`, `energy` |
| `town_event` | NPC 自主活动 | `host_npc`, `kind`, `venue`, `starts_at`, `dimension` |
| `town_invitation` | 请柬 | `event_id`, `recipient_kind/id`, `delivered_at` |
| `town_migration` | 迁徙记录 | `npc_code`, `from_user_id`, `to_user_id`, `moved_at`, `paired_with`（交换的对方） |
| `town_letter` | 信件 | `sender_kind(CONFIDANT/NPC)`, `sender_id`, `recipient_user_id`, `kind(LONG/NOTE/INVITE)`, `body`, `deliver_at`, `read_at` |
| `town_confidant_thread` | 树洞往来（**与上面所有表隔离**） | `user_id`, `direction`, `body`, `written_at`, `deliver_at` |

> **隔离要求**：`town_confidant_thread` 不得与 `town_fact` / `town_npc_knowledge` 发生任何关联，查询路径也不共享。

### 3.2 每晚定时任务的七步（复用 `TownReflectionJob` 的现成模式）

```
1. 采集事实
   玩家事件 ← task_event 派生，必须经 TownNpcPerception 模糊化后才能入 town_fact
   NPC 事件 ← 由日程 + 情绪生成（今天去了哪、心情如何、遇到了谁）
   regard 事实 ← 标记 no_relay，本人绝不外传

2. 目击判定 → 写入 hops=0 的 knowledge
   物理在场 = presence 轨迹 × NPC 日程的时空交集
   玩家主动告知 = 玩家在对话里说出来的（显式入口）
   regard 的目击 = 第三方看到「A 和 B 同时在同一地点且 A 停留异常久」→ 生成一条「猜测」事实
   小助：全知（拿到全部 fact），但 knowledge 标 no_relay —— 不参与传播

3. 传播模拟（系统的心脏）
   按日程重合生成当日的 NPC 相遇序列
   逐条候选事实计算传播概率：
     P = f(affinity, share_drive_of_speaker, curiosity_of_listener,
           interest_match, salience, quirk_modifier) × random_jitter
   传成功 → 写入 hops+1 的 knowledge

4. LLM 批量生成转述（一次调用生成多条，成本可预测）
   输入：说话人的人设 + 他知道的这条事实的上一手文本 + hops + 他的 quirks
   输出：这一手的走样版本（hops 越大越模糊、越夸张；quirks 可造成张冠李戴）

5. 亲密度更新
   相遇次数累积 + 时间衰减 + 投机度（分享是否被接收）+ 兴趣重合度
   加权 + 随机扰动 + 情绪修正

6. 事件与信件
   P(办活动) = f(share_drive, 朋友数, mood.valence)
   请柬按 affinity 降序发出
   树洞回信生成 + 排到次日投递

7. 迁徙判定（稀疏触发）
   仅在互为好友的小镇之间，仅第三层，交换制
   携带 knowledge 一并迁移，跨镇事实的 salience 设为加速衰减
   对原小镇的 bond 转为 MISS
```

### 3.3 前端运行时

- 两个 NPC 相遇 → 按 `affinity` 分档：
  - 高：停下，依次播 2~3 条 `retold_text` 气泡
  - 中：停下，播 1 条
  - 低：不停，只播一个招呼气泡
- 玩家靠近 → 若该 NPC 对玩家 `affinity` 够高，主动播一条它今天想说的（**受护栏 A 的每日 3 次预算约束**）
- **观察模式**：镜头脱离玩家，在若干兴趣点之间缓慢巡游。Phaser 相机自带 pan/zoom 缓动，代码量不大。既是 demo 录制工具，也是真实功能

### 3.4 隐私边界（贯穿全系统的硬约束）

1. 玩家事实进入 `town_fact` **前**必须过 `TownNpcPerception` 模糊化——绝不出现数字本身、任务标题、具体时间
2. 小助全知，但 `no_relay`：只对玩家本人说，不进传播网络
3. 玩家主动告知是**唯一**能让具体细节进入网络的通道，且由玩家显式触发
4. 跨镇传闻**带名仅限互为好友**，且内容仍是模糊化版本——名字与细节是两层独立授权
5. 第三层 NPC 不持有本镇玩家的事实（迁徙时携带的是**上一个镇**的、已模糊化的传闻）
6. 树洞往来**完全隔离**，绝不进入任何传播路径

---

## 4. MVP 任务拆分

顺序原则：**demo 的「哇」在哪里就先做哪里；风险最高、未知最多的先做。**

### M0 · 地基（阻塞项，必须先做）

| 步 | 内容 | 验收 |
| --- | --- | --- |
| M0-1 | **重跑 `python3 scripts/build-town-assets.py`** | 磁盘 atlas 现有 57 帧，脚本定义 65 帧，`home-living-room` 引用的 `loveseat_wood` / `armchair_wood` / `rug_pattern_1` / `painting_1` 磁盘上**不存在**，客厅会大面积缺图 |
| M0-2 | 扩裁切框：Farm 包动物 | 从 `tmp/Modern_Farm_v1.2.zip` 的 `32x32/Animals_32x32/` 取狗（10 变体，**含 `Dogs_Doghouse_Sleeping`**）、兔（9 变体）、鸡鸭 |
| M0-3 | 扩裁切框：Farm 包劳作动画 | `32x32/Characters_32x32/` 的劈柴 / 浇水 / 钓鱼 / 收割 / 挖掘 |

### M1 · 限知与传播（核心，风险最高，先做）

| 步 | 内容 | 验收 |
| --- | --- | --- |
| M1-1 | 建全部表。**`town_bond` 一次性带上 `regard`/`regard_kind`，`town_npc` 带上 `home_user_id`/`settled_at`** | 改表最便宜的时机就是建表那次 |
| M1-2 | 18 个 NPC 档案落地（性格 / 兴趣 / 癖好 / 分层） | 能查出每个 NPC 的分享欲、好奇心、五维度权重 |
| M1-3 | 事实采集：玩家事件经 `TownNpcPerception` 模糊化入 `town_fact` | 单测：`town_fact` 里查不到任何原始数字与任务标题 |
| M1-4 | 目击判定：presence 轨迹 × NPC 日程 → `hops=0` | 单测：深夜无人时完成任务，只有小助知道且 `no_relay` |
| M1-5 | 关系图 + 亲密度更新（纯函数） | 单测：日程重合上升；久不见面衰减；随机扰动有界 |
| M1-6 | 传播模拟（纯函数） | 单测：固定随机种子下传播链可复现 |
| M1-7 | LLM 批量转述生成，接入每晚 job | 人工检查：同一事实的第 1/2/3 手文本逐级走样且都通顺 |
| M1-8 | `GET /api/v1/town/npc/{code}/talking-points` | 返回该 NPC 今天想说的若干条 |

**M1 做完可以脱离画面验证**：直接查表就能看出「谁知道什么、从谁听来、说成了什么样」。

### M2 · 18 个 NPC 落地 + 观察模式（demo 的表现层）

| 步 | 内容 | 验收 |
| --- | --- | --- |
| M2-1 | 第三层 10 个背景 NPC：现成 sprite + 日程配置 | 街上有人走动，密度随时段变化，同屏 ≤ 12 |
| M2-2 | 第二层 6 个：人设 + 五维度兴趣绑定 + 可搭话（不弹窗） | 走近有气泡，点击有对话，但从不打断 |
| M2-3 | 劳作动画接入（M0-3 的素材） | NPC 在浇水 / 劈柴 / 钓鱼，不再只是站着和走路 |
| M2-4 | 路遇对话气泡：按 `affinity` 分档 | 复用 `walkers.ts` 的 `shouldGreet` / `registerGreet` 冷却 |
| M2-5 | **护栏 A**：全镇每日主动性预算（3 次），小助优先 | 单测：预算耗尽后没有 NPC 再主动搭话 |
| M2-6 | **观察模式**：镜头脱离玩家，兴趣点之间缓慢巡游 | 一键进出；镜头稳、不晕 |

**M2 做完 = demo 可录。第一个交付里程碑。**

### M3 · 我的家（现成资产接线，风险最低）

| 步 | 内容 | 验收 |
| --- | --- | --- |
| M3-1 | 把 `enterRoom('home')` 接到 UI | 走到自家门口 → 门开 → 淡入；能进能出 |
| M3-2 | 补齐 `createInteriorScene` 入参：`player` / `metrics` / `residents` | 屋里有自己的角色能走动；成就墙按 `homeAchievements` 真实点亮 |
| M3-3 | 可交互家具（**要改三处**） | ① `map-loader.ts` 的 `RoomFurniture`（`:36-46`）加交互字段；② `parseRoomMap`（`:245-252`）白名单构造加解析（现在未知字段被静默丢弃）；③ `interior.scene.ts` 的 `drawFurniture`（`:156-160`）绑事件 + `InteriorOptions` 加 `onInteract`。可仿 `doorAt`（`:360`）写 `interactableAt` |
| M3-4 | 三件可交互物：书桌 / 成就墙 / 宠物窝 | 走 `world-actions.ts` 的注册表 |
| M3-5 | 宠物进屋 | 狗 / 兔 / 鸟用 M0-2 素材，能走动能睡；**猫 / 仓鼠 / 蛇 / 龟 / 狐无素材** → 占位窝，点击弹现有 Rive 面板 |

### M4 · 事件、信件、迁徙、牵挂

| 步 | 内容 | 验收 |
| --- | --- | --- |
| M4-1 | NPC 自主发起活动：`P(办) = f(share_drive, 朋友数, mood)` | 不会连续一周冷场，也不会天天办 |
| M4-2 | 活动场景布置：现有广场 / 公园 / 咖啡馆加灯串、摊位、聚集的 NPC | 零地图改造 |
| M4-3 | 活动 = 高密度相遇场 | 玩家站在人群里能同时听到好几组对话 |
| M4-4 | 请柬：按 `affinity` 降序发出，邮递员送 | 亲密度高的先收到 |
| M4-5 | **牵挂系统表现层**：regard 的第三方目击 → 猜测入网 → 走样传播 | 玩家能听到互相矛盾的传闻，且系统不提供任何确认界面 |
| M4-6 | **迁徙**：好友镇之间、仅第三层、交换制、稀疏触发 | 新面孔出现，带着走样的跨镇传闻；跨镇事实一两周内衰减掉 |
| M4-7 | **路过的旅人**：孤身用户的替代机制 | 停留几天，讲泛化的、不涉及真实用户的故事，然后离开 |
| M4-8 | **树洞笔友**：写信 → 隔天邮递员送回信 | 内容完全隔离，不进 `town_fact`；回信不给建议只回应；它不知道你的任何数据 |

### M5 · 三个面板做到自洽（平行入口一期）

| 步 | 内容 |
| --- | --- |
| M5-1 | 今天：看 / 开始 / 完成 / 延期任务，全部在小镇内完成 |
| M5-2 | 伙伴：看宠物 / 互动 / 切换 |
| M5-3 | AI 助手：完整 SSE 对话（`AiPanel` 已接 `SseRequestError`） |
| M5-4 | 其余 6 个面板补 `anchor` 绑到地点；dock 默认收起 |

### M6 · L 形街道（可选，最后）

单基线布局加纵深支路是**真改引擎**，全案风险最高的一块。做不做取决于 M1~M5 的实际进度。转角带来的「发现感」很值，但不值得阻塞前面任何一步。

---

## 5. 明确不做

| 排除项 | 原因 |
| --- | --- |
| 共享世界 / 陌生人可见 | §2.6：一人一镇 + 迁徙，模型更干净且更好玩 |
| WebSocket 实时多人同步 | 现有轮询 presence 够用，非瓶颈 |
| **NPC 对玩家产生 regard** | §2.5 红线 1。一破就是另一个品类 |
| **任何感情线的结局**（表白 / 成对 / 失恋 / 撕破脸） | §2.5 红线 2。有结局就有输家 |
| **「谁暗恋谁」的确认界面** | §2.5 红线 3。八卦的乐趣在不确定 |
| 第一二层 NPC 迁徙 | §2.6。小助是私人秘书，第二层是你熟悉的面孔 |
| 玩家自由装修编辑器 | 先给按等级解锁的预设房间风格 |
| NPC 向量记忆 + reflection（AI Town 那套） | 本案的限知 + 传播是更适配的模型 |
| 活动小游戏 | D14：活动是相遇场，不是玩法 |
| 活动 / 信件给经验或成就 | D5：成长只能来自真实行动 |
| Office 内景（`tmp/Modern_Office_Revamped_v1.2.zip`，339 个已切好的 32×32 单品） | 二期弹药。文件名纯编号，挑选是体力活，会阻塞 M3 |
| 补齐猫 / 仓鼠 / 蛇 / 龟 / 狐的像素素材 | 二期。MVP 用占位窝 |
| 卧室场景 | atlas 里**没有任何床的帧**，要先扩裁切框 |

---

## 6. 执行顺序

```
M0 地基（阻塞，必须先做）
  ↓
M1 限知与传播 ──────► 可脱离画面验证（查表即可）
  ↓
M2 18 个 NPC + 观察模式 ──────► ★ demo 可录，第一个交付里程碑
  ↓
M3 我的家（现成资产接线，风险最低）
  ↓
M4 事件 / 信件 / 迁徙 / 牵挂
  ↓
M5 三面板自洽
  ↓
M6 L 形街道（可选）
```

M1 与 M3 技术栈几乎不重叠（后端建模 vs 前端接线），若有第二人加入可并行，但**两边都不要同时改 `town.engine.ts`**。

---

## 附：已完成的改动

移动速度翻倍（前后端必须一致，否则服务端 presence 校验会把跑动位置夹回去）：

| 位置 | 原值 | 现值 |
| --- | --- | --- |
| `frontend/src/modules/town/walkers.ts` `WALK_SPEED` | 56 | **112** px/s |
| `frontend/src/modules/town/walkers.ts` `RUN_SPEED` | 132 | **264** px/s |
| `frontend/src/modules/town/interior.scene.ts` `DEFAULT_SPEED`（室内） | 90 | **180** px/s |
| `backend/.../town/TownPresenceClamp.java` `RUN_SPEED_PX_PER_SEC` | 132 | **264** |

同步更新 `docs/成长小镇.md`、`docs/成长小镇-接口约定.md`。
验证：前端 town 模块 341 个测试通过，后端 `TownPresenceClampTest` 通过。

> 注：M6 若实施，街道变长后可能需要再次调整速度常量。
